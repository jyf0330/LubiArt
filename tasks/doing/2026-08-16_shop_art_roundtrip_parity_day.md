# 商店美术双向同步、逐操作对照与持续错位修复

- status: completed
- owner: codex-root-20260816-shop-art-roundtrip
- user_authorization:
  - 2026-08-16 用户要求“美术包的图和我们的对比图，按一个操作接一张图，持续一天持续修复”
  - 2026-08-16 用户补充“错位那些记得改；先用我们的数据和一些图先同步到美术项目，然后它们再同步回来”
  - 2026-08-17 用户确认按审计结论“弄吧”：停止继续扩张 86 步微交互矩阵，默认只验收固定种子的 9 次权威操作和 5 个必要视觉补充；同时核清正式图片全集、当前种子闭包、缺失真图和历史残留
- objective: 建立正式项目到独立美术项目、再从美术项目回到正式项目的可审计商店交付链；使用同一份正式只读展示快照和实际所需图片，在隔离真实窗口按一次操作一组图对照美术项目与正式项目，并持续修复确认的错位
- write_scopes:
  - `tools/art/sync_shop_art_workspace.py`
  - `tools/export_public_shop_art_capture.gd`
  - `tests/features/smoke_shop_art_workspace_sync.gd`
  - `tests/visible/capture_formal_shop_operation_parity.gd`
  - `tools/qa/build_shop_operation_comparison.py`
  - `tools/qa/test_shop_operation_comparison.py`
  - `tools/qa/check_shop_art_geometry.py`
  - `tools/qa/test_shop_art_geometry.py`
  - `art/manifests/shop/formal_resource_inventory.json`
  - `core_ui/scripts/shop/views/legacy_code_shop_view.gd`（仅修复截图确认的正式表现错位）
  - `core_ui/scripts/shop/views/shop_shelf_layout.gd`（新增独立货架表现模块；只消费公开商品字典并发出语义命令）
  - `core_ui/scripts/shop/views/shop_shared_rail.gd`（新增独立底部队伍栏表现模块；不复制美术项目 Scene/脚本）
  - `art/images/shop/screen_shop_godot_v1/shared_party_shadow.png`（美术项目烘焙回传的商店专用阴影 PNG；正式项目不移植美术 shader）
  - `core_ui/scripts/shop/views/shop_art_backdrop.gd`（仅修复截图确认的正式表现错位）
  - `art/images/shop/screen_shop_godot_v1/shared_bag_closed.png`
  - `art/images/shop/screen_shop_godot_v1/shared_bag_open.png`
  - `art/images/shop/screen_shop_godot_v1/shared_bag_inventory.png`
  - `art/images/shop/screen_shop_godot_v1/shared_bag_item_highlight.png`
  - `art/images/shop/screen_shop_godot_v1/shared_party_shelf.png`
  - `art/images/shop/screen_shop_godot_v1/shared_coin_panel.png`
  - `art/images/shop/screen_shop_godot_v1/shared_exit_normal.png`
  - `art/images/shop/screen_shop_godot_v1/shared_exit_hover.png`
  - `tests/features/smoke_shop_art_visual_integration.gd`（补充货架孔位、分页与独立性回归）
  - `tasks/doing/2026-08-16_shop_art_roundtrip_parity_day.md`
  - `output/validation/shop-art-roundtrip/**`（验证产物，不提交）
  - `/Users/ywh/Documents/godot-battle-ui-mock/data/formal_shop_art_preview.json`
  - `/Users/ywh/Documents/godot-battle-ui-mock/data/formal_shop_art_runtime.json`（商店专用有界运行投影；不再覆盖战斗回放入口）
  - `/Users/ywh/Documents/godot-battle-ui-mock/data/mock_battle_snapshot.json`（战斗任务独占，只读核对，商店同步不得写入）
  - `/Users/ywh/Documents/godot-battle-ui-mock/art/images/formal_sync/shop/**`
  - `/Users/ywh/Documents/godot-battle-ui-mock/art/images/shop/screen_shop_godot_v1/shared_party_shadow.png`
  - `/Users/ywh/Documents/godot-battle-ui-mock/art/manifests/shop/formal_sync_manifest.json`
  - `/Users/ywh/Documents/godot-battle-ui-mock/art/manifests/shop/formal_resource_inventory.json`
  - `/Users/ywh/Documents/godot-battle-ui-mock/art/manifests/shop/merchant_map.json`
  - `/Users/ywh/Documents/godot-battle-ui-mock/art/manifests/shared/pets/sheets/pet_id_map.json`
  - `/Users/ywh/Documents/godot-battle-ui-mock/session/mock_game_session.gd`（只离线回放同一导出内的商店操作 Snapshot）
  - `/Users/ywh/Documents/godot-battle-ui-mock/core_ui/scripts/inventory/presenters/inventory_presenter.gd`（只适配正式公开 `bag_slot` 的 1 基展示坐标；不计算背包权威规则）
  - `/Users/ywh/Documents/godot-battle-ui-mock/tests/smoke_mock_battle_project.gd`（补充 1 基正式快照与 0 基旧美术预览兼容门禁）
  - `/Users/ywh/Documents/godot-battle-ui-mock/tests/smoke_formal_shop_art_sync.gd`
  - `/Users/ywh/Documents/godot-battle-ui-mock/core_ui/scripts/route/prefabs/route_shared_ui.gd`（只修美术项目纹理投影与拖拽源显隐；不回传正式项目）
  - `/Users/ywh/Documents/godot-battle-ui-mock/core_ui/scripts/shop/scenes/shop_scene.gd`（只恢复美术项目拖拽释放后的指针高亮；不回传正式项目）
  - `/Users/ywh/Documents/godot-battle-ui-mock/tests/visible/shop_formal_sync_parity_capture.gd`（统一逐操作截图状态，并在商店卸载后仍从公开 Session 记录退出结果）
  - `/Users/ywh/Documents/godot-battle-ui-mock/.agents/skills/lubiart-godot-ui-art/references/conversation-synthesis.md`（按项目 Skill 在本轮最终收口时回写）
- exclusive_files:
  - 上述新增同步、专项测试、可见采集和本任务卡文件
  - 两个正式商店 View 文件只在实时 `git status` 仍无其他 owner 新改动时进入修复
  - 美术项目既有 5 个 `.psd.import` 修改属于用户/其他任务，本任务不覆盖、不回退、不暂存
- existing_wip:
  - 正式项目 `tasks/ai/QUEUE.md`、`tasks/ai/STATUS.md`、`tasks/doing/2026-08-15_background_patrol_2249.md`、`tasks/doing/2026-08-16_background_patrol_0054.md` 为巡检 WIP；不改写、不暂存、不提交
  - 美术项目 5 个既有 `.psd.import` 修改全部保留；同步工具不得执行目录镜像删除或覆盖未列入清单的文件
- synchronization_contract:
  - outbound：正式项目通过真实 `LocalGameSession -> Command -> Snapshot` 流程进入固定商店，只导出公开 Snapshot、操作结果摘要、商人映射和本轮记录实际引用的 PNG；不导出存档、权威状态对象、玩法脚本、Session、Command 实现或策划真相源
  - 美术项目的既有战斗回放仍只读 `data/mock_battle_snapshot.json`，商店同步不再修改该文件；商店展示改读独立有界投影，既有战斗回放数据和步骤保持逐字节语义不变
  - 2026-08-17 起商店同步改用独立 `data/formal_shop_art_runtime.json`；`mock_battle_snapshot.json` 继续只归战斗回放，Mock Session 仅在非 battle 启动模式读取商店投影，避免商店/战斗同步互相覆盖同一启动快照
  - art workspace：美术项目离线消费完整自包含的展示快照与图片副本；允许修改美术资源和表现层，但不得把该快照当正式玩法真相源
  - inbound：只接收 outbound 清单中声明为 `art_owned` 的资源/manifest/纯表现交付；每个文件校验相对路径、SHA-256、尺寸和来源基线，冲突时停止，不复制美术项目 Session、数据适配或固定槽位代码
  - 美术原生共享图可从美术项目既有路径映射到正式项目新的商店专用文件名；新文件要求 prepare 时目标不存在、回传仍保持美术基线哈希，禁止覆盖正式项目同名路线资源
  - 每次同步生成机器可读 receipt，明确 `formal_to_art` 与 `art_to_formal` 的文件集合、原哈希、新哈希和跳过原因；禁止全目录覆盖
- screenshot_matrix:
  - 默认验收为 `9` 次权威操作状态加 `5` 个必要视觉中间态，共 `14` 张；完整微交互序列只保留为 `--diagnostic-full`
  - 权威操作覆盖进入、免费刷新、三次购买、付费刷新、背包购买、退出和重进；视觉补充覆盖商品悬停/按下、刷新红帘、背包展开和退出按下
  - 美术项目截图在左、正式项目同操作截图在右；每个操作单独一张并排图，不再使用正式改前/改后图冒充美术包参考
- validation:
  - 同步专项：公开字段白名单、图片闭包、SHA-256、PNG 尺寸/透明、路径隔离、双向 owner 边界和冲突停止
  - 美术项目与正式项目分别使用独立项目副本、`.godot`、`user://`、进程、输出目录和本 AI 专用虚拟屏，从各自真实入口完成 1920x1080 操作截图
  - 对每组截图记录窗口尺寸、项目根、Git 基线、操作、快照摘要和图像文件哈希；人工检查尺寸、位置、层级、裁切、遮挡和交互命中区
  - 修复后运行相关商店专项 smoke、隔离真实窗口复拍、`git diff --check`；高风险时补 fast QA
- stop_conditions:
  - 同步链形成可重复的 outbound/art/inbound receipt，且没有正式核心代码或 Mock 状态实现跨仓复制
  - 至少六组同操作对照齐全；每个确认错位均有修复前证据、责任层、修复后证据和回归结果
  - 每个可独立验收的修复精确提交，不混入两仓既有 WIP，不推送
  - 持续目标到期前继续选择下一项可确认、无冲突的错位；缺少可靠证据时只记录观察，不凭截图猜测玩法或数据修改

## 2026-08-17 验收收缩与资源清单校正

- 默认截屏矩阵固定为 `14` 张：`9` 次来自真实 `LocalGameSession -> Command -> Snapshot` 的权威操作状态，加 `5` 张仅用于检查悬停、按下、红帘、背包展开和退出按下的视觉补充；既有完整微交互序列保留为显式诊断模式，不再冒充日常验收工作量。
- 九次权威操作为：首次进入、免费刷新、连续购买三个商品、付费刷新、购买背包商品、退出、退出后重进；操作数据继续使用 `shop-art-roundtrip-v1` 固定种子与同一份公开导出，不复制美术项目代码。
- 资源完整性拆成四层审计：正式项目图片全集；本固定种子的实际可见闭包；缺失真图/回退映射；已经不在闭包内的历史同步残留。不得用“当前截图能显示”替代“美术包已拥有正式程序包所有图片”。
- 当前已确认的缺口是 `pal_342` 没有正式身份映射而回退到 `pal_042`；它必须在清单中明确标为缺失真图。`pal_028`、`pal_033`、`pal_042` 是当前种子不再引用的历史同步残留，只有在哈希仍等于既有同步基线时才精确移除 PNG、import 与对应 `formal_sync` 映射。
- 出站仍只传公开快照和当前闭包图片；入站仍只接收既定 `art_owned` PNG 白名单。资源清单和同步收据必须同时记录正式全集计数、当前闭包计数、回退项、历史残留、出站/回传资产和源提交，避免旧提交号或旧资源继续被误报为当前完整状态。
- 验证要求：默认两仓真实窗口各生成 `14` 张、逐图比较为 `14` 对；固定种子公开导出在正式当前 HEAD 重跑后与美术预览逐字节一致；同步专项、资源清单、几何审计、相关 smoke 与范围内 `git diff --check` 全部通过后才允许精确提交。

## 2026-08-16 第一阶段进展（持续中）

- 同步链：固定种子 `shop-art-roundtrip-v1`，正式公开 Session 导出后，美术项目回放同一组 `CHOOSE_ROUTE -> ROLL_SHOP -> BUY_OFFER -> EXIT_SHOP` Snapshot；当前收据为 outbound 15 张有界可见/候选宠物图 + 1 张商人图、inbound 11 张白名单 PNG、代码文件 0。
- 图片闭包修复：同步优先覆盖当前队伍、实际入口、刷新、购买、路线候选，既解决路线候选提前截断导致 `pal_053`、`pal_138`、`pal_110`、`pal_111` 漏图，也覆盖底栏正在显示的 `pal_002`，避免美术项目继续使用旧宠物映射图。
- 正式表现修复：移除全屏暗幕和大列表覆盖；店面、商人、5 孔货架、刷新铃和红帘按 1920x1080 美术孔位投影；最多 10 件商品通过 5 件一页的正式独立分页保留。
- 美术回传：从美术项目现有资源映射回正式项目新的商店专用文件名，加入宝箱、四槽队伍托盘、金币牌、退出常态/悬停共 5 张图；没有覆盖正式项目已有路线背包图。
- 交互回归：首次加入底栏时全屏透明 Control 拦截购买，失败证据在 `output/validation/shop-art-roundtrip/round-003/formal-project/attempt-1-shared-rail-mouse-filter/`；改为空白区域穿透后真实 BUY_OFFER 恢复。
- 运行边界收紧：完整公开快照继续保存在美术参考文件中，Mock 运行时只嵌入清单声明的路线/商店表现字段；移除无关 battle、run plan、catalog 和 trace 后，运行时 `shop_art_capture` 由约 8.6MB 降到约 120KB，既有战斗回放 steps 哈希保持不变。
- 当前证据：`output/validation/shop-art-roundtrip/round-008/comparison-v2/` 含 6 张一操作一图的当前通过对照；前 5 步使用相同种子、相同商品、同一灰尾狸队伍图、相同刷新和购买结果，购买后售出孔位清空、队伍第二槽新增宠物、金币 16→14；未锁商品的锁按钮只在悬停/聚焦时出现。
- 新失败证据：`output/validation/shop-art-roundtrip/round-008/comparison/` 曾显示美术项目底栏沿用旧宠物映射；根因是图片闭包只覆盖商品，修复后由 `comparison-v2/` 复拍确认两边均为正式 `pal_002` 图。
- 已确认并修复：正式店面被遮暗、商品/商人/红帘错位、底部宝箱/托盘/金币缺图、同步数据有按钮但商品图漏传、购买后商品剪影残留、未锁商品的锁按钮常驻遮挡。
- 当前非问题：第 6 张退出后两边路线美术不同，属于两个项目各自路线入口，不作为复制美术项目路线代码的理由；只验证均已从商店返回路线并保留同一金币/队伍结果。
- 已通过：`smoke_shop_art_visual_integration.gd`、`smoke_shop_art_workspace_sync.gd`、`smoke_legacy_code_shop_view.gd`、`smoke_shop_character_production_integration.gd`、美术项目 `smoke_formal_shop_art_sync.gd`，以及两项目隔离真实窗口 6 步采集；最新美术采集输出 `SHOP_FORMAL_SYNC_PARITY_CAPTURE_PASS`，逐操作比较输出 `SHOP_OPERATION_COMPARISON_OK pairs=6`。
- 快速总回归：正式项目隔离副本运行 `python3 tools/qa/run_qa.py --suite fast`，`19/19` 通过，报告在 `/private/tmp/codex-shop-roundtrip-0816.zJbgii/formal/output/validation/qa/20260816-031214-fast`。

## 2026-08-16 第二阶段进展（持续中）

- 资源模板定位：以 round-008 美术项目真实截图为基准，在未缩放的 1920×1080 原图中定位相同 PNG；店面与红帘均为 `(0,0)` 偏差，底部宝箱、队伍托盘、金币牌、退出牌在正式项目中则统一低 `20px`。第一次搜索窗口只到 `-18px` 且最优点撞边，round-009 扩窗复测后纠正为 `20px`，不沿用受截断结果。
- 责任层：错位来自正式独立 `ShopSharedRail` 的参考矩形，不是美术图片透明边、公开 Snapshot 或 Mock 代码；四张共享底栏图、队伍宠物和金币文字统一上移 `20px`。
- 刷新铃：美术项目以原始 `89×86` 纹理显示，正式项目原先通过带内容边距的普通 Button 和 `82px` 图标上限缩小；现改为正式独立 `TextureButton` 状态纹理投影，保持语义 `ROLL_SHOP` 不变，不复制美术项目 Scene/脚本。
- 红帘过程：round-010 资源复测发现正式中心缩放动画会让过程帧产生约 `3px` 的瞬时纵向位移；正式表现改为保持 authored `492,313,836×502` 矩形的透明度揭幕，沿用原 `0.10 + 0.12 + 0.10s` 时序与语义命令，不移植 Mock tween 实现。
- round-011 结果：店面、红帘、刷新铃、宝箱、队伍托盘、金币牌、退出牌 7 项资源模板定位均为 art/formal `delta=(0,0)`，输出 `SHOP_SHARED_RESOURCE_ALIGNMENT_OK assets=7`；同一正式商人图缩放定位在两边均为 `(813,593)`。六步正式实窗再次输出 `FORMAL_SHOP_OPERATION_PARITY_CAPTURE_PASS captures=6`，当前一操作一图证据在 `output/validation/shop-art-roundtrip/round-011/comparison/`。
- 非缺陷区分：正式入口首商品金框来自 `_grab_focus_for_current_view -> grab_initial_focus()` 的键盘焦点门禁，未删键盘可达性；本轮只消除资源位置和尺寸错位。
- 商品精灵测量：round-011 未聚焦第二件商品的同一 `pal_053` PNG 在美术项目为 `152×152 @ (827,364)`，正式项目为 `136×136 @ (835,387)`；首件商品还被焦点整体缩放，而美术项目进入/悬停两帧均保持 `152×152 @ (502,364)`。
- 商品精灵修复：正式独立货架使用 4px 内容边距、152px 顶部对齐，悬停/键盘焦点只保留金框、层级和锁操作反馈，不再缩放 authored 商品图；没有读取或移植 Mock 货架节点/脚本。
- 第三货孔复核：入口 `pal_138` 与刷新后 `pal_111` 都显示美术项目第三件商品为 `152×152 @ (1152,394)`、正式项目为 `152×152 @ (1152,379)`，稳定相差 `15px`；第三个斜屋檐货孔单独下移到 `y=390`，不误改前两个已对齐货孔。
- round-012 首次复拍：正式 `EXIT_SHOP` 命令已接受且 Snapshot 进入 route，但固定 30 帧后商店 Control 尚未隐藏，采集按门禁失败并保留在 `round-012/formal-project/`；采集器改为最多 240 帧等待并记录 `exit_settle_frames`，用于区分环境长帧与视图不收口，未放宽“必须真实隐藏”的验收条件。
- round-012 重跑：真实视图在第 `103` 帧隐藏并通过，证明原 30 帧采集假设过短；round-013 再跑为 `99` 帧。两次均实际等待 Control 隐藏后才记录退出截图。
- round-013 商品验证：入口 3 件与刷新后 3 件共 6 张正式宠物 PNG 的反求尺寸/位置逐项等于美术项目，输出 `SHOP_OFFER_ART_ALIGNMENT_OK offers=6`；正式六步输出 `FORMAL_SHOP_OPERATION_PARITY_CAPTURE_PASS captures=6`，当前逐操作证据在 `output/validation/shop-art-roundtrip/round-013/comparison/`。
- 队伍宠物测量：购买后新增的同一 `pal_009` 在美术项目为 `126×126 @ (757,804)`，round-013 正式项目为 `124×124 @ (758,805)`；责任层为正式独立 `ShopSharedRail` 将正方形宠物图放入 `126×124` 容器后居中缩小。本轮只把宠物投影改为 `126×126` 并上移 `1px`，阴影仍按原始 `79×19` 资源尺寸保留。
- round-014 复拍：美术项目与正式项目同一 `pal_009` 均反求为 `126×126 @ (757,804)`，输出误差分别为 `0.6423/0.6149`，队伍宠物错位闭环；六步实窗输出 `FORMAL_SHOP_OPERATION_PARITY_CAPTURE_PASS captures=6`，退出在第 `17` 帧真实隐藏，对照在 `output/validation/shop-art-roundtrip/round-014/comparison/`。`smoke_shop_art_visual_integration.gd` 新增真实纹理解析与 `PartyPet_0` 矩形门禁，另有 legacy、character、workspace 三项 smoke 同轮通过。
- 金币文字层级：round-014 入口局部对照显示两边数字 `16` 的字形位置相同，但正式项目后绘制的退出牌遮掉数字 `6` 最右侧 `x=1411..1412` 两列，美术包保持完整；责任层为正式 `ShopSharedRail` 兄弟节点绘制顺序。本轮将 `CoinLabel` 提到退出贴图之上，同时继续使用 `MOUSE_FILTER_IGNORE` 保持退出命中区不变。
- round-015 复拍：两边金币 `16` 的亮色字形均为 `107` 像素，边界同为 `(1395,923)-(1413,940)`，遮挡闭环；六步实窗再次输出 `FORMAL_SHOP_OPERATION_PARITY_CAPTURE_PASS captures=6`，新对照在 `output/validation/shop-art-roundtrip/round-015/comparison/`。专项 smoke 新增“金币高于退出贴图且不接管输入”的门禁。
- 非正方形队员：同步清单确认两边 `pal_002.png` SHA-256 同为 `00553fedad51c4bc8f69fe54eee00cad1f8b53fb6f2c7ff2f189543d9b4aa524`；round-015 局部图显示横向源在美术包明显大于正式图。责任层是正式宠物容器宽度；round-016 的 `131×126` 半像素视口让正方形 `pal_009` 回退为 `125×125 @ (758,805)`，候选作废；round-017 的整数 `132×126` 恢复 `pal_009` 精确对齐，但 `pal_002` 仍只投影为 `132×106 @ (576,814)`；round-018 的 150px 宽候选保持 `pal_009` 精确，却确认美术包横向完整画布实际为约 `155×124`。最终改为通用纹理比例适配：装入 `155×126` 上限、以槽位中心 `(82,74)` 居中并落整数坐标，不按宠物 ID 特判。
- round-019 复拍：横向 `pal_002` 两边均为 `155×124 @ (564,805)`，正方形 `pal_009` 两边均为 `126×126 @ (757,804)`，双资源几何闭环；正式横向源对模板 MAE `0.417`、正方形 MAE `0.000`。六步实窗再次通过，对照在 `output/validation/shop-art-roundtrip/round-019/comparison/`；专项 smoke 同时门禁宽图与方图的独立纹理解析和矩形。
- 阴影透明度：两仓 `shadow.png` SHA-256 同为 `ae24ea22269b062c5c264b4b13dc13a81888c78d4d6a7a5216947f0182d3299f`；以入口空槽作为购买后同位置背景，排除 `pal_009` 遮挡后反算，两边阴影几何均为 `79×19 @ (781,903)`，美术包最优透明度约 `0.6`，正式原为 `1.0`。责任层是正式 `ShopSharedRail` 阴影调制，本轮只设置 `self_modulate.a=0.6`。
- round-020 复拍：正式阴影反算最优透明度为 `0.60`（MAE `0.257`），美术包为 `0.58`（MAE `0.542`，受截图混合取整影响），位置和尺寸保持一致；六步实窗再次通过，对照在 `output/validation/shop-art-roundtrip/round-020/comparison/`。专项 smoke 新增阴影透明度门禁。
- 宽图采样网格：对 `pal_002` 独立搜索宽高后，美术包最优纹理栅格为 `154×124 @ (565,805)`（MAE `6.215`），正式 round-020 为 `155×124 @ (564,805)`；两者中心相同，但奇数宽导致最近邻采样错列。责任层仍为通用比例适配，候选改为将拟合后的像素画宽高收敛到不超过上限的偶数，使其围绕整数槽位中心对称；`pal_009` 的 `126×126` 不受影响，等待下一轮双资源复核。
- round-021 复拍：`pal_002` 两边均为 `154×124 @ (565,805)`，正式对源高不透明像素 MAE `0.000`、美术包/正式间 MAE `6.215`；`pal_009` 两边继续为 `126×126 @ (757,804)` 且逐像素 MAE `0.000`。六步实窗再次通过，对照在 `output/validation/shop-art-roundtrip/round-021/comparison/`；通用偶数像素范围门禁同时覆盖宽图和方图。
- 商品价格文字：以同源背景与店面资源合成空底，提取未聚焦第二件商品叠加像素；美术包数字边界为 `8×13 @ (899,536)`，正式 round-021 为 `9×17 @ (898,527)`，正式偏大且约高 `9px`。责任层为 `ShopShelfLayout` 的 PriceLabel，不涉及价格数据；round-022 的 `15px/top=0.90` 候选已对齐横向边界和下沿，但顶部多 1px；round-023 的 `14px` 已精确对齐 13px 高度与 y 边界，正式仅为 `7px` 宽而美术为 `8px`。当前保持字号/锚点，仅将水平阴影偏移由 `1` 调为 `2` 以补齐右侧一列，等待复拍。
- round-024 第二价格：美术包和正式数字边界均为 `8×13 @ (899,536)`，该槽文字几何闭环；但同轮发现第三斜屋檐槽的价格跟随整体商品根下移，美术为 `(1223,536)-(1231,549)`，正式为 `(1224,566)-(1232,578)`。责任层是第三槽宠物下移后 PriceLabel 未独立补偿；候选为正式每槽价格偏移表，仅第三槽使用 `(-1,-30)`，等待复拍。
- round-025 第三价格：正式数字已从牌下回到牌内，横向边界与顶边均等于美术包 `(1223,536)-(1231,...)`；美术阈值底边为 `549`、正式为 `548`，属于同位置叠加色阈值的一行差异，实图无残留错位。第二价格继续保持双方 `8×13 @ (899,536)`；六步实窗再次通过，对照在 `output/validation/shop-art-roundtrip/round-025/comparison/`。专项 smoke 门禁紧凑字号、阴影范围和第三槽独立价格矩形。
- 对照交互状态：美术采集通过路线确认按钮进入商店后，鼠标停留位置恰好命中商店退出牌，因此历轮第 1 张左图是退出悬停态；正式采集进入路径不同，右图不是同一状态。两边采集脚本现统一在入口截图前移动到 `(1850,1030)` 中立点并等待 2 帧，只修验证状态，不改运行时 UI。
- round-026 双边复拍：美术项目与正式项目都从各自真实入口完成六步采集并通过，入口截图的退出牌均为中立态；正式退出在第 `19` 帧真实隐藏。新对照在 `output/validation/shop-art-roundtrip/round-026/comparison/`；正式首商品金框仍是键盘焦点门禁，保留为可访问性差异。
- 累积回归：基于已同步到隔离正式副本的当前商店代码运行 `python3 tools/qa/run_qa.py --suite fast`，`19/19` 通过，报告在 `/private/tmp/codex-shop-roundtrip-0816.zJbgii/formal/output/validation/qa/20260816-043841-fast`；四项商店专项 smoke 与 round-026 六步实窗也均为当前通过。
- 当前分类：红帘、刷新后 3 件商品、两类队伍宠物、阴影、价格牌、共享底栏没有新的可证错位；正式红帘过程铃铛亮框与首商品金框属于点击/键盘焦点反馈。退出后两边各自路线美术不同，但金币 `14`、两名队员与 route 结果一致，继续不以视觉不同为由复制 Mock 路线实现。
- round-026 证据审计发现：美术项目第 6 张退出画面可见金币 `14`、两名队员，但采集清单因商店节点已卸载而用空字典写成金币 `0`、商品 `0`。责任层是验证采集器，不是 Snapshot 或运行时；本轮将由美术项目公开 `GameSession.current_snapshot()` 补取退出状态，并让对照生成器在出图前强制核对双方 `phase/coins/offer_count/roster_count`，不复制正式 Session 或玩法代码。
- round-027 闭环：美术采集器在商店卸载后从自身公开 Session 读取退出快照，六步状态恢复为入口/刷新 `shop,16,3,1`、购买 `shop,14,3,2`、退出 `route,14,3,2`；正式采集记录同样四项字段。对照工具先审计全部六步再写图，旧 round-026 会因退出 `0/0` 对 `14/3` 被准确拒绝，新 `round-027/comparison/` 六组全部通过。隔离虚拟屏实窗复核未发现新的资源错位，用户 Godot PID `5112` 全程保留。
- 几何门禁：新增纯截图审计器，以双方实际共享 PNG 的高不透明像素在各自 1920×1080 原图附近反向定位，不依赖 Mock Scene/脚本；覆盖店面、实际同步商人、刷新铃、宝箱、队伍托盘、金币牌、退出牌、红帘、入口/刷新各 3 件商品、宽图/方图队员共 16 项。它对 round-027 全部返回双方 `offset=(0,0)`，并在历史 round-009 正确复现四张底栏资源仍低 `2px` 以及旧商品/队员尺寸位置偏差，报告分别为 `round-027/geometry_audit.json` 与 `/private/tmp/round009-geometry-negative.json`；半透明阴影继续使用既有空槽背景反算和运行时矩形/透明度 smoke，不以受宠物遮挡的直接模板误报。
- round-028 数据身份门禁：双边采集清单新增 `state_version/state_hash`，对照生成前连同既有四项摘要一起强制相等；六步对应身份依次为 `v1/acf88e4c2972a5d2`、`v1/acf88e4c2972a5d2`、`v2/3c567615607908fa`、`v2/3c567615607908fa`、`v3/55a35ba1c657e29e`、`v4/1b70deda932ac7b5`，美术项目与正式项目逐字一致。`round-028/comparison/` 六组出图和 `round-028/geometry_audit.json` 16 项几何均通过，证明使用的是同一正式导出状态身份而非只碰巧数量一致。
- 金币牌/退出牌层级错位：round-028 分区像素审计确认两张资源坐标均正确，但重叠区 3035 个双方不透明像素中，美术项目 3023 个精确等于金币牌，正式项目 2982 个精确等于退出牌；责任层是正式 `ShopSharedRail` 兄弟绘制层级。正式独立表现改为金币牌高于退出牌、数字再高于金币牌，节点仍忽略输入，退出命中区不变。
- round-029 复拍：正式重叠区已有 3005 个像素精确等于金币牌，`3024/3035=0.996376` 像素更接近金币牌，美术项目为 `1.0`；旧 round-028 正式比率仅 `0.001977`。截图审计新增 `coin_panel_above_exit >= 0.95` 层级门禁，能拒绝修复前证据；新 `round-029/comparison/` 六步状态身份全部通过，`round-029/geometry_audit.json` 16 项坐标与 1 项层级均通过。
- 宝箱/队伍托盘层级错位：round-029 继续分区后，在 625 个双方不透明重叠像素中，美术项目全部由队伍托盘覆盖，正式项目全部由宝箱覆盖。正式底栏明确分层为 `退出牌 < 宝箱 < 托盘 < 队员 < 金币牌 < 金币数字`，没有复用 Mock 节点或脚本；新 `party_shelf_above_bag` 截图门禁会以 art/formal `1.0/0.0` 拒绝修复前 round-029。
- round-030 复拍：两边托盘在上比率均为 `1.0`，金币牌在上比率为 `1.0/0.996376`；16 项资源几何与两项重叠层级全部通过，宽图/方图队员继续保持 `(565,805)` 与 `(757,804)`，没有被提高后的托盘遮挡。六步状态身份继续通过，对照在 `round-030/comparison/`，机器报告为 `round-030/geometry_audit.json`。
- 金币字形差异：round-030 中两边亮色字形边界和数量相同，但正式标签额外阴影使叠加像素为 `307`、美术项目为 `198`，并向右多 `1px`、向下多 `3px`；移除正式独有阴影后边界/数量一致，最后的 121/130 个单通道 `1/255` 差异来自十六进制色与作者浮点色量化不同。正式改用作者色 `Color(0.99,0.84,0.36,1)`。
- round-032 复拍：入口 `16` 与购买后 `14` 的金币数字裁剪均达到逐像素 `diff=0`；截图审计新增两项 exact pixel 门禁，可分别以 `121`、`130` 个差异像素拒绝 round-031。新六步状态身份、16 项几何、2 项层级、2 项金币字形全部通过，证据在 `round-032/comparison/` 与 `round-032/geometry_audit.json`。
- 商品价格字形继续复核：round-032 的美术数字为 `64` 个叠加像素、正式为 `85` 个，正式独有阴影和字号/位置均不一致；round-033 的 `14px` 无阴影候选仅 `41` 像素、尺寸 `6x10`，按截图证据作废；round-034 改为 `18px` 后双方均为 `64` 像素、`8x13`，再按三个已显示孔位分别校正 `(5,1)`、`(1,1)`、`(1,1)` 的屏幕偏移。第三斜屋檐孔仍通过独立价格偏移抵消商品根下移，不复制 Mock 货架实现。
- round-035 复拍：入口三格价格以及刷新后第二、第三格价格的裁剪均达到逐像素 `diff=0`；刷新后第一格保留正式键盘焦点填充，不以删除可访问性反馈换取整块裁剪一致。截图审计现同时通过 16 项资源几何、2 项层级和 7 项精确像素裁剪；同一规则可稳定拒绝 round-032 的 5 项旧价格字形。六步比较继续先通过 `state_version/state_hash` 同一状态身份门禁，证据在 `round-035/comparison/` 与 `round-035/geometry_audit.json`。
- 队伍阴影颜色：round-035 的阴影几何虽同为 `79x19 @ (781,903)`，但正式项目直接绘制黑色遮罩，美术项目呈深青灰；同一空槽背景反算的效果 MAE 为 `0.576035`。round-036 曾验证运行时 Alpha 遮罩 shader 候选可降到 `0.252546`，但它改变同帧批次后让已通过的金币/价格 exact crop 出现 1 LSB 波动，候选作废且未提交，避免把美术复合 shader 移入正式项目。
- 美术 PNG 回传：美术项目将共享黑色遮罩烘焙为商店专用 `shared_party_shadow.png`（`79x19`、SHA-256 `174b7e9cf1c23f87bfdd6f523f394fbbf84bd8d32849224903c885055f1164fc`）；round-037 重新执行正式→美术 prepare/apply，再按 12 张 PNG 白名单回传，收据为 `assets=12 changed=1 code=0`，正式项目与美术项目文件哈希一致。正式 `ShopSharedRail` 仅显示回传纹理，不含 Mock Scene、Session、固定槽位或 shader 实现。
- round-037 双边同轮复拍：先复用旧美术截图时，整帧颜色环境有 1 LSB 漂移并触发已有 exact crop；没有放宽门禁，而是在同一虚拟屏重新运行美术项目六步采集。当前六步状态身份、16 项资源几何、2 项层级、7 项精确字形裁剪全部通过；阴影效果门禁以两边各自入口空槽为背景，排除宠物大幅变化后得到 `982` 个可比较像素、MAE `0.199593 <= 0.25`，并能以 `0.576035` 稳定拒绝 round-035。证据在 `round-037/comparison/`、`round-037/geometry_audit.json` 和 `round-037/receipts/art_to_formal_receipt.json`。
- round-037 专项回归：正式隔离副本的 visual/legacy/character/workspace 四项商店 smoke、美术隔离副本 `smoke_formal_shop_art_sync.gd`、几何 6 项 Python 单测、逐操作 3 项 Python 单测和 `git diff --check` 全部通过；美术交付已精确提交为 `0ed4d77`，未包含既有 5 个 PSD import WIP，用户 Godot PID `5112` 全程存活。
- 宽幅队员采样：round-037/038 的宽图位置尺寸虽为 `(565,805),154x124`，但美术项目复合 shader 的纹理采样相对同步原图仍有 `MAE 6.212515`；只改 shader 的显示矩形不改变采样结果，round-038 候选作废。责任层在美术项目 `RouteSharedUi`，不是正式资源或队伍数据。
- round-039 美术修复：美术项目保留按钮 shader 负责阴影和悬停，只把同步宠物纹理投影拆成按钮内独立、输入穿透的 `TextureRect`；宽图和方图相对同步原图的高不透明像素 MAE 均为 `0.0`，正式项目同为 `0.0`，两类位置尺寸不变，阴影效果 MAE 同时由 `0.199593` 降到 `0.133605`。几何审计新增两类队员 `<=0.01` 像素忠实度门禁，可用 `6.212515` 拒绝 round-038；六步状态身份、16 项几何、2 项层级、7 项 exact crop 和阴影门禁全部通过，证据在 `round-039/comparison/` 与 `round-039/geometry_audit.json`。美术项目专项 smoke 验证宽/方图独立矩形和 `source_enabled=false`，修复提交为 `c0cdd53`，未进入正式项目同步白名单。
- round-040 对照自证：六张“一次操作一张图”的图头直接写入双方已核对相等的 `phase/coins/offer_count/roster_count/state_version/state_hash`，不再要求查看者另开 manifest 判断是否为同一步状态；图片为 `3840x1208`，仍保持美术项目左、正式项目右。入口/悬停为 `shop,16,3,1,v1/acf88e4c2972a5d2`，刷新过程/完成为 `v2/3c567615607908fa`，购买为 `shop,14,3,2,v3/55a35ba1c657e29e`，退出为 `route,14,3,2,v4/1b70deda932ac7b5`。新图在 `round-040/comparison/`，同轮几何报告继续全部通过。
- 最新视觉分类：当前六张中没有新的资源位置/尺寸/层级证据；正式项目顶部 HUD、入口/购买后的键盘焦点框、红帘过程的铃铛焦点亮边属于正式功能反馈，退出后路线属于两项目各自入口，均不以视觉不同为由删除或复制 Mock 实现。后续继续只修新出现且能由截图/资源反求确认的错位。
- round-041 交互诊断：双边真实采集清单新增 `pointer_position/hovered/focused`。入口双方均为中立点 `(1850,1030)` 且无悬停/焦点；悬停帧双方均为 `(578,454)`、同一 `BUY_OFFER shop_001`，但美术项目无焦点而正式项目焦点为第一商品；刷新两帧双方均悬停 `ROLL_SHOP @(988,754)`，美术焦点为刷新铃、正式焦点仍为旧第一商品；购买后美术焦点/悬停为已售第一商品，正式焦点转到第二商品且悬停为空。该差异来自采集时序的焦点状态，不是资源位置；下一轮在截图前双边统一释放焦点，购买结果移回中立点，只规范验证环境，不删除运行时键盘焦点能力。
- round-042 交互闭环：双边六步截图前统一释放焦点；入口和购买结果均为中立点 `(1850,1030)`、无悬停，悬停帧双方为 `(578,454),BUY_OFFER/shop_001`，刷新过程/完成均为 `(988,754),ROLL_SHOP`，退出为 `(1544,726)`、无悬停，六步焦点均为 `none`。对照生成器在状态身份之外新增指针坐标、悬停动作/商品和焦点动作硬门禁，内部控件名只记录不要求相同；每张 `3840x1244` 图直接显示状态与交互摘要。六步比较与几何审计全部通过，证据在 `round-042/comparison/`、双边 `capture_manifest.json` 和 `round-042/geometry_audit.json`。
- 悬停金框责任层：round-042 已证明双方悬停同一 `shop_001` 且焦点均为 `none`，因此正式项目第一货孔的大金框来自 `ShopShelfLayout` 的鼠标 `hover` StyleBox，而非键盘焦点；美术参考的可购买商品为白色调制，鼠标进入不会叠加大框。候选将正式鼠标 hover 改为透明框，同时保留键盘 `focus` 金框、指针形状和悬停锁按钮，避免以视觉对齐破坏可访问性。
- round-043 悬停闭环：正式鼠标 hover StyleBox 改为透明，第一货孔四条边与美术项目分别达到 exact `diff=0`；round-042 旧证据在上/左/右/下四条边分别有 `618/738/738/609` 个差异像素。截图审计新增四项 hover-frame exact crop，当前总计 16 项资源几何、2 项层级、11 项 exact crop、1 项阴影效果全部通过；状态与交互摘要仍为同一 `shop,16,3,1,v1/acf88e4c2972a5d2`、`BUY_OFFER/shop_001 @(578,454)`、焦点 `none`。正式运行时继续保留 3px 键盘 focus 金框；专项 smoke 还覆盖鼠标移出后若键盘仍聚焦，锁按钮不会错误消失。证据在 `round-043/comparison/02_offer_hover_art_vs_formal.png` 与 `round-043/geometry_audit.json`。
- 刷新铃过程态责任层：round-043 双边清单已证明红帘过程均为鼠标 `(988,754)`、悬停 `ROLL_SHOP`、焦点 `none`，两仓铃铛常态/悬停 PNG 哈希也一致；继续核对后确认并非红帘层级，而是美术参考在动画期间禁用铃铛并绘制常态，正式项目仍保持可用而显示黄边，同时存在重复提交刷新意图的窗口。候选只在正式 View 内维护动画期交互锁，由纯表现 `ShopArtBackdrop` 发出揭幕完成信号后再按当前公开 Snapshot 的金币条件恢复，不复制美术项目 Scene、Session 或刷新脚本。
- round-044 首次复拍门禁：真实 `ROLL_SHOP` 已接受并进入 `v2`，后续购买、退出也成功，但固定等待 24 帧时帘幕仍可见，采集器以 `formal refresh curtain clears` 失败并保留样本；同轮命令日志显示刷新处理单帧约 `247ms`，证明固定帧数不是可靠的动画完成条件。采集器改为最多等待 120 帧且必须观察到 `RefreshCurtain.visible=false`、铃铛重新可用后才拍第 4 张，同时在 manifest 记录实际 `refresh_settle_frames`，不放宽真实完成门禁。
- round-044 第二次复拍闭环：正式六步实窗通过，`refresh_settle_frames=2`、六步状态身份与交互摘要继续逐项等于美术侧；红帘过程铃铛右边缘、下边缘裁剪均达到 exact `diff=0`，修复前 round-043 分别为 `293`、`566` 个差异像素。审计增至 16 项资源几何、2 项层级、13 项 exact crop、1 项阴影效果并全部通过，逐操作图在 `round-044/comparison/`，报告在 `round-044/geometry_audit.json`。正式独立 View 只以表现完成信号约束重复输入，没有复制 Mock 脚本；visual/workspace/legacy/character 四项商店 smoke、11 项 Python 单测和 `git diff --check` 同轮通过，用户 Godot PID `5112` 保持存活。
- round-044 总回归：隔离正式副本运行 `python3 tools/qa/run_qa.py --suite fast`，`19/19`、0 失败，报告在 `/private/tmp/codex-shop-roundtrip-0816.zJbgii/formal/output/validation/qa/20260816-061444-fast`；用户 Godot PID `5112` 与既有 WIP 未被占用或改写。
- 下一操作扩展：现有第 6 张只证明点击退出后双方回到 route，没有独立证明商店内退出牌的 authored 悬停纹理、位置和真实命中状态。截图矩阵新增 `06_exit_hover`，在同一 `v3/shop/coins=14/roster=2` 状态以真实指针悬停并要求双方语义均为 `EXIT_SHOP`；原退出结果顺延为 `07_exit_to_route`。只扩双边验证脚本与对照门禁，不改玩法、不复制 Mock 运行时代码。
- round-045 退出悬停闭环：美术项目和正式项目分别从真实入口完成 7 步采集，新增第 6 步均为 `shop,coins=14,offers=3,roster=2,v3/55a35ba1c657e29e`、指针 `(1544,726)`、悬停 `EXIT_SHOP`、焦点 `none`；退出牌 authored 位置双方均为 `(1410,490)`，完整 `269×472` 矩形逐像素 `diff=0`，而同侧普通态对悬停态有 `9848` 个差异像素，证明门禁确实覆盖状态纹理。当前审计为 17 项资源几何、2 项层级、14 项 exact crop、1 项阴影效果全部通过；7 张“一操作一图”在 `round-045/comparison/`，报告在 `round-045/geometry_audit.json`，11 项 Python 单测通过，用户 Godot PID `5112` 保持存活。
- 背包操作扩展：宝箱按钮没有独立 hover 纹理，可靠对照应覆盖真实点击打开与再次关闭。截图矩阵新增 `06_bag_open`、`07_bag_closed`，原退出悬停/退出结果顺延到 `08/09`；两步都要求同一 `v3` 状态、双边实测指针 `(430,892)`、悬停语义 `TOGGLE_BAG`、焦点 `none`。先保留现状拍失败证据，确认美术 authored 背包面板与正式详情抽屉的实际差异后，再通过既有双向白名单同步纯资源并在正式表现层独立实现。
- round-046 背包失败证据：双边 9 步状态与交互门禁通过，但新增第 6 张显示美术项目为暗幕、打开宝箱和 authored 八格背包，正式项目仍弹出旧文字详情抽屉；截图在 `round-046/comparison/06_bag_open_art_vs_formal.png`。责任层为正式 `ShopSharedRail`/`LegacyCodeShopView` 表现，不是公开 Snapshot、按钮命中或美术素材；候选不沿用 Mock Scene/脚本，只接收纯 PNG 并由正式模块投影 `active=false` 的公开 roster 记录。
- 背包资源回传：同步白名单新增美术原生 `bag_open_full.png -> shared_bag_open.png`（`236×233`、SHA-256 `7fc098745fe01f225269a517d691e39c508c6e7a788c33bfd8925cdfc5ab17ef`）和 `bag_inventory_base.png -> shared_bag_inventory.png`（`818×439`、SHA-256 `7eb46975716c1a36ce346c9c5ae21674048e26b153d06f33a356e4c3e9e07fa1`）；重新执行正式→美术 prepare/apply 后再回传，收据 `assets=14 changed=2 code=0 conflicts=[]`，位于 `round-046/receipts/art_to_formal_receipt.json`。
- round-047 背包闭环：正式独立 `ShopSharedRail` 用回传 PNG 构建输入穿透的暗幕/八格面板，宝箱在打开/关闭间切换纹理，并按公开 `roster.active=false` 通过正式纹理 resolver 投影背包宠物；不读取 Mock Scene、固定槽或 Session。第 6 张中打开宝箱 `236×233`、背包内部 `(510,387)-(1312,810)` 均逐像素 `diff=0`，第 7 张关闭宝箱也 `diff=0`；资源反求位置双方为宝箱 `(313,776)`、面板 `(502,379)`。审计现为 19 项资源几何、2 项层级、17 项 exact crop、1 项阴影效果全部通过，9 张对照在 `round-047/comparison/`，报告在 `round-047/geometry_audit.json`。visual/workspace/legacy/character 四项 smoke、11 项 Python 单测和本轮文件 `diff --check` 通过；用户 Godot PID `5112` 保持存活。
- round-047 总回归：隔离正式副本运行 `python3 tools/qa/run_qa.py --suite fast`，`19/19`、0 失败，报告在 `/private/tmp/codex-shop-roundtrip-0816.zJbgii/formal/output/validation/qa/20260816-063814-fast`；新增背包表现没有破坏会话边界、商店目录、战斗 UI、完整可玩流程或 seed bot。
- 队伍悬停扩展：美术商店真实行为只在已有队伍槽鼠标进入/离开时切换地面阴影亮度，不弹详情面板；正式底栏当前队员与阴影均输入穿透。截图矩阵新增 `06_party_hover`，后续背包/退出顺延到 `07..10`，要求同一 `v3`、真实第一槽中心 `(642,878)`、语义 `PARTY_SLOT`、焦点 `none`。正式候选只新增透明语义命中层并调制已回传的烘焙阴影 PNG，不移植 Mock drag controller 或 shader。
- round-048 队伍悬停闭环：双边从真实入口完成 10 步采集，第 6 步均为 `shop,coins=14,offers=3,roster=2,v3/55a35ba1c657e29e`、指针 `(642,878)`、悬停 `PARTY_SLOT`、焦点 `none`。正式 `ShopSharedRail` 为实际占用槽创建透明语义按钮，悬停只调制已回传烘焙阴影，离开恢复，不接入 Mock 拖拽控制器或 shader；首槽阴影亮化相对各自第 5 张基线的效果误差为 `MAE 0.988008 <= 1.1`、可比像素 `1501`。当前审计为 19 项资源几何、2 项层级、17 项 exact crop、2 项效果全部通过，10 张对照在 `round-048/comparison/`，报告在 `round-048/geometry_audit.json`；12 项 Python 单测通过。
- 非空背包数据闭环候选：继续由正式 `LocalGameSession` 在同一固定种子内购买刷新后的第二、第三件商品填满四人队伍，再执行一次正式付费刷新并购买新商品，使第五只宠物由权威规则进入 `roster.active=false`；导出为按序、带 `snapshot_key` 的公开操作快照。美术项目只能校验下一条公开命令并装载对应裁剪 Snapshot，不得本地计算金币、刷新池、队伍槽或背包槽。若真实导出未形成恰好四名 active 与一名 inactive，则停止截图扩展并按实际命令结果修正候选，不伪造数据。
- round-049 非空背包失败证据：正式固定种子真实命令已得到 `v7/5c8b1bed15535fbc, coins=6, roster=5`，其中四名 active、`pal_017 active=false, bag_slot=1`；双边各产出 18 张操作截图。`14_bag_with_pet_open` 显示正式把权威第 1 背包槽投影到第一格，美术项目的旧 0 基 presenter 却把数值 `1` 画到第二格；美术采集因此按“第一格必须有宠物”门禁失败。责任层是美术项目纯展示索引适配，不是正式权威数据或正式 `ShopSharedRail`。候选 presenter 仅从当前 inactive 记录判断编号基准：存在 0 时兼容美术旧预览 0 基，否则按正式公开数据 1 基转为 UI 下标，不回传该代码。
- round-050 背包槽复拍：美术 presenter 修复后，双边 `14_bag_with_pet_open` 都把 `pal_017` 画在第一格，18 步状态身份逐项保持 `v1..v8` 正式哈希；美术采集与正式采集均通过。`15_bag_pet_hover` 仍显示美术包 authored 黄色框，而正式端无语义命中层、无悬停框，且交互清单为美术 `BAG_SLOT @(628,496)`、正式 `none`。下一步只把美术原生 `bag_item_highlight.png` 作为 PNG 白名单回流，并在正式 `ShopSharedRail` 独立创建透明语义按钮与高亮投影，不移植 Mock 拖拽器、shader、Scene 或 Session。
- round-051 悬停闭环与新采样证据：`bag_item_highlight.png` 以源图 `181×174`、显示框 `183×177 @ (541,409)` 回流，收据为 `assets=15 changed=1 code=0 conflicts=[]`；正式独立透明 `BagSlotButton_0` 使第 15 步双方均为 `BAG_SLOT @(628,496)`，18 组状态/交互对照全部通过。但第 14 步第一槽 crop 仍有 `4753` 个差异像素；两边 `pal_017.png` SHA-256 同为 `6046bf...c5a7a`，责任层是美术 `RouteSharedUi` 旧 bag shader 将正方形同步图固定采样到 `121×116` 并以旧 offset 定位，而正式端保持 `116×116` 比例居中。候选只在美术项目将背包宠物拆成输入穿透的独立直出纹理子节点，shader 关闭 source 采样；不回传代码，不改高亮/拖拽语义。
- round-053 非空背包最终闭环：美术项目 `BagPortrait` 以 `116×116 @ slot-local(19,15)` 直出同源 `pal_017`，关闭旧 shader source 采样；正式项目高亮按美术 Scene 的铺满模式显示回传源图。双边真实入口各完成 18 步，`SHOP_OPERATION_COMPARISON_OK pairs=18`；第 14 步第一槽宠物和第 15 步包含黄色框的完整 `(541,409)-(724,586)` crop 均为 `diff=0`。状态从 `v1/acf88...` 依次到 `v8/a308...`，第五只进入背包及悬停均保持 `v7/5c8b1bed15535fbc, coins=6, roster=5`，指针/语义为 `BAG_SLOT @(628,496)`。几何报告 `passed=true`，19 项资源、2 项层级、19 项 exact crop、2 项效果全通过，证据位于 `output/validation/shop-art-roundtrip/round-053/`；12 项 Python 单测通过，用户 Godot PID `5112` 全程存活。
- round-053 总回归：隔离正式副本运行 `python3 tools/qa/run_qa.py --suite fast`，`19/19`、0 失败，报告在 `/private/tmp/codex-shop-formal-highlight-0816.j5vRvl/formal/output/validation/qa/20260816-072955-fast`；另有 visual/workspace/legacy/character 四项正式商店 smoke 和美术同步 smoke 通过。完整 Mock 大回归在本轮相关索引断言执行后撞到既有战斗资源与 `ProgressBar.text` 断言并挂住，已仅终止 AI 临时副本 PID；该无关失败不计入本轮通过项，也未影响用户 Godot。
- 付费刷新/满队伍扩展审计：在 round-053 上把 `10_party_full_purchase` 的第 3、4 队伍槽和 `12_paid_refresh_complete` 的三件新商品、三块价格牌、金币 `8` 加入几何/逐像素门禁。队伍第 3/4 槽均为同源纹理 `126×126 @ (933,804)/(1113,804)`、两边采样 MAE `0.0`；三块价格与金币逐像素相同。新门禁同时确认唯一失败：宽图 `pal_105` 在美术项目按第三孔完整 `160×190 @ (1148,375)` 垂直居中，纹理框为 `152×114 @ (1152,413)`；正式项目沿用 `@ (1148,390)` 顶对齐，纹理框为 `152×114 @ (1152,394)`，向上错 `19px`。责任层为正式 `ShopShelfLayout` 的第三孔几何，不是同步资源、公开 Snapshot 或 Mock 玩法；候选只按截图反求把正式第三孔改为完整 authored 矩形并居中，同时独立补偿价格偏移保持牌面不动。
- round-054 付费刷新错位闭环：双边在专用虚拟屏重新从真实入口完成 18 步，逐操作状态/交互比较 `pairs=18` 通过。正式第三孔仍保持独立 `160×190` 语义命中与键盘焦点，只把孔位改为 `(1148,375)`、图像改为垂直居中，并用正式 StyleBox 内容边距补偿 Godot 主题的 1px 取整；价格标签通过独立偏移留在原牌面。当前入口/免费刷新方图均为 `(1152,394)`，付费刷新宽图两边均为 `152×114 @ (1152,413)` 且采样误差逐项相同；几何报告为 `24` 项资源、`2` 项层级、`23` 项 exact crop、`2` 项效果全部通过，三块付费价格与金币 `8` 均 `diff=0`。证据在 `output/validation/shop-art-roundtrip/round-054/comparison/` 与 `round-054/geometry_audit.json`；visual/workspace/legacy/character 四项正式 smoke、12 项 Python 单测、`git diff --check` 通过，用户 Godot PID `5112` 全程存活。
- 已售孔位失败证据：round-054 的第 1、2、3 次购买和第五只宠物购买后，双方公开状态/操作一致，但正式 `Button.disabled` StyleBox 仍以 `Color(0.05,0.05,0.05,0.12)` 覆盖完整空孔，美术项目则只显示原店面；前三个完整孔位分别有 `30359/30358/30360` 个差异像素。责任层是正式 `ShopShelfLayout` 的 disabled 纯表现，不是商品 `sold` 权威值；候选保留按钮 disabled、不可再次购买、焦点与命令边界，只把多余背景改为透明，并为四个购买结果增加完整孔位 exact crop。
- round-055 已售孔位闭环：双边重新从真实入口完成 18 步且状态/交互比较 `pairs=18` 通过；正式已售按钮继续 `disabled=true`，仅将 disabled StyleBox 背景改为透明。第 5/9/10/13 张中的第一、第二、第三和付费刷新第一孔完整矩形均达到 `diff=0`，几何审计增至 `24` 项资源、`2` 项层级、`27` 项 exact crop、`2` 项效果且全部通过；对照在 `output/validation/shop-art-roundtrip/round-055/comparison/`，报告在 `round-055/geometry_audit.json`。visual smoke 新增透明 disabled 背景和保持不可购买的门禁，用户 Godot PID `5112` 未被触碰。
- 第三付费商品交互扩展：在 `12_paid_refresh_complete` 后新增独立操作 `12b_paid_refresh_third_offer_hover`，双边都用真实指针移动到第三件 `shop_003/pal_105` 的同一 authored 屏幕点 `(1228,470)`；比较门禁必须同时证明同一 `v6/dead7d0cea3d7163`、`coins=8`、`BUY_OFFER/shop_003` 悬停语义和焦点释放。该步骤只扩展双边可见采集与正式几何审计，不改变 Mock Session/玩法，也不把 Mock 控件代码回传正式项目。
- round-056 第三付费商品悬停闭环：双边真实入口各完成 `19` 步，新增图双方均为 `v6/dead7d0cea3d7163, coins=8, roster=4`，指针 `(1228,470)`、悬停 `BUY_OFFER/shop_003`、焦点 `none`；逐操作比较 `pairs=19` 通过。`pal_105` 在悬停态继续为 `152×114 @ (1152,413)`，完整宠物矩形 `diff=0`；几何审计为 `25` 项资源、`2` 项层级、`28` 项 exact crop、`2` 项效果全部通过。正式图右上角小“锁”按钮属于正式冻结商品能力，美术项目无此功能；保留该功能差异，不为像素一致删除正式命令入口。证据在 `output/validation/shop-art-roundtrip/round-056/comparison/12b_paid_refresh_third_offer_hover_art_vs_formal.png` 与 `round-056/geometry_audit.json`。
- 背包暗幕底边失败证据：round-056 的空背包、第 5 只宠物背包及宠物悬停三帧中，美术项目暗幕均结束于 `y=1060`，正式 `ShopSharedRail` 的全屏遮罩却继续覆盖到底部 `y=1080`；每帧底部 `(0,1060)-(1920,1080)` 均为 `38400` 个差异像素，正式侧 RGB 约额外降低 `8/10/20`。责任层是正式纯表现遮罩矩形，不是公开 Snapshot、背包资源或 Mock Session；候选只把正式遮罩裁到 authored `1920×1060`，背包面板、按钮、语义命中和底栏保持不动。
- round-057 背包暗幕底边闭环：双边在专用虚拟屏重新从真实入口完成 `19` 步，逐操作状态/交互比较继续 `pairs=19` 通过。正式只将 `BagOverlayMask` 独立投影为 `(0,0)-(1920,1060)`；第 7/14/15 张底部三块 `1920×20` 完整裁剪均从 `38400` 个差异像素降为 `diff=0`，背包面板、空/非空状态、宠物与黄色悬停框原门禁继续通过。几何审计为 `25` 项资源、`2` 项层级、`31` 项 exact crop、`2` 项效果全部通过，证据在 `output/validation/shop-art-roundtrip/round-057/comparison/` 与 `round-057/geometry_audit.json`；用户 Godot PID `5112` 全程存活。
- 商人采样候选：round-057 入口中商人图片两边源 PNG SHA-256 均为 `a5289103...f7672`、导入参数逐项一致，几何定位均为 `(813,593)`；但完整 `(813,580)-(990,782)` 容器仍有 `5964` 个差异像素、整框 MAE `0.541692`。最大非文字差异集中在商人半透明边缘，候选责任层为正式端 anchored `177×202 + KEEP_ASPECT_CENTERED` 的半像素采样；先在隔离副本改成截图反求的直接 `177×177 @ (813,593)` 像素框，只有真实复拍明显降低差异且不破坏原门禁才保留。
- round-058 商人候选作废：正式隔离实窗把商人改为直接 `177×177 @ (813,593)` 后，完整容器差异反而由 `5964` 增至 `9454`，MAE 由 `0.541692` 增至 `3.157726`；候选未进入提交，正式端恢复原 `177×202 + KEEP_ASPECT_CENTERED`。两边位置、源哈希、导入参数均已相等，剩余小幅差异仅位于半透明边缘且当前没有可靠单方责任证据，归为合成噪声观察，不以放宽门禁或继续猜测方式修改。
- 商人合成噪声门禁：几何审计新增独立 tolerance crop，不把半透明合成误称为 exact，也不无限放宽；入口商人完整容器必须同时满足差异像素 `<=6000`、RGB MAE `<=0.55`。该门禁接受 round-057 的 `5964/0.541692`，并能拒绝 round-058 候选的 `9454/3.157726`；源哈希、位置和导入参数仍由既有同步/几何门禁分别约束。
- round-057 当前总回归：在独立 detached HEAD `42d370aac44b817cf87de25eb887e760875b4e02` 与隔离 `GODOT_USER_HOME` 中运行 `python3 tools/qa/run_qa.py --suite fast`，`19/19`、0 失败；完整可玩流程与 seed bot 分别通过，报告在 `/private/tmp/codex-shop-fast-0816.60KmbA/formal/output/validation/qa/20260816-082832-fast`。用户 Godot PID `5112` 在回归前后均存活且未被操作。
- 刷新铃静态悬停扩展：现有第 3/4 张只在刷新已提交后记录红帘过程与完成态，不能独立证明命令前 authored 悬停纹理、命中区和 `ROLL_SHOP` 语义。矩阵新增 `02b_refresh_hover`，双边从同一 `v1` 入口状态将真实指针移到刷新铃中心、释放键盘焦点但不点击；对照门禁要求指针 `(988,754)`、悬停 `ROLL_SHOP`、焦点 `none`，几何审计独立定位悬停 PNG 并核对未受左侧透明重叠影响的右/下边缘。
- round-059 刷新铃悬停闭环：美术项目与正式项目在专用虚拟屏分别从真实路线入口完成 `20` 步，新增第 `02b` 张均保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，指针 `(988,754)`、悬停 `ROLL_SHOP`、焦点 `none`；悬停 PNG 双方均定位为 `89×86 @ (945,712)`，模板不透明像素误差同为 `0.040091`，右边缘与下边缘均为 `diff=0`。逐操作比较输出 `SHOP_OPERATION_COMPARISON_OK pairs=20`，几何审计为 `26` 项资源、`2` 项层级、`33` 项 exact crop、`1` 项 tolerance crop、`2` 项效果且全部通过，证据在 `output/validation/shop-art-roundtrip/round-059/comparison/` 与 `round-059/geometry_audit.json`。该状态没有产品错位，因此未改正式运行时代码；四项正式商店 smoke、美术同步 smoke、`14` 项 Python 单测和双仓 `git diff --check` 通过，用户 Godot PID `5112` 全程存活。
- 第二队员悬停扩展：第 6 张只覆盖第一槽横向 `pal_002` 的语义命中与阴影亮化，第二槽方形 `pal_009` 尚无独立操作证据。矩阵新增 `06b_party_second_hover`，在第一次购买完成的同一 `v3` 状态把双边指针移到第二槽中心 `(820,878)`，要求 `PARTY_SLOT`、焦点 `none`，并相对第 5 张未悬停基线比较第二槽 `(781,903)-(860,922)` 阴影亮化效果；不引入拖拽或复制 Mock party controller。
- round-060 第二队员悬停闭环：美术项目与正式项目分别从真实入口完成 `21` 步，新增第 `06b` 张均为 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，指针 `(820,878)`、悬停 `PARTY_SLOT`、焦点 `none`；美术控件 `Party_Slot2` 与正式独立控件 `PartySlotButton_1` 仅按相同语义对照，没有移植节点实现。相对第 5 张未悬停基线，第二槽阴影亮化有 `1501` 个可比像素、效果 MAE `0.345992 <= 1.1`；逐操作比较 `pairs=21`，几何审计为 `26` 项资源、`2` 项层级、`33` 项 exact crop、`1` 项 tolerance crop、`3` 项效果且全部通过。证据在 `output/validation/shop-art-roundtrip/round-060/comparison/06b_party_second_hover_art_vs_formal.png` 与 `round-060/geometry_audit.json`；该操作没有确认错位，不改正式运行时代码。四项正式商店 smoke、美术同步 smoke、`15` 项 Python 单测、旧 round-059 缺新操作的负门禁和双仓 `git diff --check` 均符合预期，用户 Godot PID `5112` 全程存活。
- 满队伍后半槽悬停扩展：前两槽已有独立截图，但队伍满员后的第三、第四槽只验证了宠物纹理位置，尚未证明各自命中区与阴影亮化。矩阵在 `10_party_full_purchase` 后新增 `10b_party_third_hover`、`10c_party_fourth_hover`，双边分别将指针移到 `(996,878)`、`(1176,878)`，要求同一 `v5` 状态、`PARTY_SLOT`、焦点 `none`；效果门禁以满队伍未悬停图为基线分别检查 `(957,903)-(1036,922)` 与 `(1137,903)-(1216,922)`，不接入或复制 Mock 拖拽控制器。
- round-061 满队伍后半槽悬停闭环：美术项目与正式项目分别从真实入口完成 `23` 步，第 `10b/10c` 张均保持 `v5/3441b30e2878a924, shop, coins=10, offers=3, roster=4`；第三槽指针 `(996,878)`、第四槽 `(1176,878)`，双边均悬停 `PARTY_SLOT` 且焦点 `none`。相对第 10 张满队伍未悬停基线，两槽阴影亮化均有 `1501` 个可比像素，效果 MAE 分别为 `0.205197`、`0.608927`，均小于 `1.1`；逐操作比较 `pairs=23`，几何审计为 `26` 项资源、`2` 项层级、`33` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且全部通过。证据在 `output/validation/shop-art-roundtrip/round-061/comparison/10b_party_third_hover_art_vs_formal.png`、`10c_party_fourth_hover_art_vs_formal.png` 与 `round-061/geometry_audit.json`；两项均无确认错位，因此不改正式运行时代码。四项正式商店 smoke、美术同步 smoke、`16` 项 Python 单测、旧 round-060 缺新操作的负门禁与双仓 `git diff --check` 均符合预期，用户 Godot PID `5112` 全程存活。
- 入口货架后两孔悬停扩展：第一孔与付费刷新后的第三孔已有悬停证据，但入口第二孔 `pal_053`、斜屋檐第三孔 `pal_138` 尚未在各自真实商品身份下独立覆盖。矩阵在首次刷新前新增 `02_entry_second_offer_hover`、`02_entry_third_offer_hover`，双边分别移动到 `(902,454)`、`(1228,470)`，要求同一 `v1`、`BUY_OFFER/shop_002` 与 `BUY_OFFER/shop_003`、焦点 `none`；几何门禁独立定位两张宠物图并精确核对各自完整宠物矩形和四条货孔边缘。正式小“锁”按钮属于冻结商品能力，继续保留，不以美术项目缺少该功能为由删除。
- round-063 入口后两孔悬停闭环：round-062 首拍先准确拒绝了第二孔预估中心 `x=903`（双边真实中心均为 `x=902`）及第三孔双边控件中心相差 `2px` 的非同坐标采集；修正后两边分别使用同一 `(902,454)`、`(1228,470)`，均保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停身份为 `BUY_OFFER/shop_002`、`BUY_OFFER/shop_003` 且焦点 `none`。两张共享宠物图在双方均反求到 `(827,364)`、`(1152,394)` 且偏移 `[0,0]`，四条货孔边缘和所有未被功能控件覆盖的可见宠物像素精确一致；初版整块检查的 `852/426` 个差异严格位于正式项目独有的右上角锁定按钮覆盖区，因此保留正式冻结能力并把该覆盖区明确排除，而不是删除按钮或放宽美术门禁。逐操作比较 `pairs=25`，几何审计为 `28` 项资源、`2` 项层级、`45` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且全部通过，证据为 `output/validation/shop-art-roundtrip/round-063/comparison/02_entry_second_offer_hover_art_vs_formal.png`、`02_entry_third_offer_hover_art_vs_formal.png` 与 `round-063/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`18` 项 Python 单测、旧 round-061 缺新操作的负门禁和双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 全程存活。本轮没有确认新的资源或布局错位，不修改正式运行时代码。
- 付费刷新前两孔悬停扩展：第 `12` 张已验证三张静态资源、价格和金币，第 `12b` 张只验证第三孔宽图 `pal_105`。当前矩阵继续补 `12a_paid_refresh_first_offer_hover`、`12b_paid_refresh_second_offer_hover`，并将当前第三孔步骤整理为 `12c_paid_refresh_third_offer_hover`；双边分别使用同一 `(578,454)`、`(902,454)`、`(1228,470)`，要求同一 `v6/dead7d0cea3d7163` 以及 `BUY_OFFER/shop_001..003`。几何审计先对前两张完整宠物矩形与四条货孔边缘做精确门禁；若正式冻结按钮覆盖宠物矩形，只按真实差异边界隔离该功能覆盖区，不删除正式命令入口，也不复制美术项目按钮实现。
- round-064 付费刷新三孔悬停闭环：美术项目与正式项目分别从真实路线入口完成 `27` 步，三张悬停图均保持 `v6/dead7d0cea3d7163, shop, coins=8, offers=3, roster=4`；指针依次为 `(578,454)`、`(902,454)`、`(1228,470)`，悬停身份依次为 `BUY_OFFER/shop_001..003`，焦点均为 `none`。`pal_017`、`pal_027`、`pal_105` 在两边分别反求为 `(502,364)`、`(827,364)`、`(1152,413)` 且偏移全为 `[0,0]`；前两张完整宠物框的初始 `852` 个差异均严格落在正式项目右上角 `30×30` 锁定按钮覆盖区，排除该功能覆盖区后，宠物可见像素与四条货孔边缘全部 `diff=0`，正式冻结能力继续保留。逐操作比较 `pairs=27`，几何审计为 `30` 项资源、`2` 项层级、`57` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且全部通过；证据为 `output/validation/shop-art-roundtrip/round-064/comparison/12a_paid_refresh_first_offer_hover_art_vs_formal.png`、`12b_paid_refresh_second_offer_hover_art_vs_formal.png` 与 `round-064/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`19` 项 Python 单测、旧 round-063 缺新操作/旧第三孔名称的负门禁和双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 全程存活。本轮没有确认新的资源或布局错位，不修改正式运行时代码。
- 首次刷新三孔悬停扩展：实时权威探针确认固定种子基础商店当前只有 `3` 件商品，而同路线的技能商店当前公开 Snapshot 为 `0` 件；正式 `10` 件分页因此仍是防回归能力，不把不存在的玩家步骤包装成美术对照。本轮补足真正可执行的免费刷新后 `pal_009/pal_110/pal_111` 三件商品，新增 `04a/04b/04c_refresh_*_offer_hover`，要求同一 `v2/3c567615607908fa`、指针 `(578,454)/(902,454)/(1228,470)`、`BUY_OFFER/shop_001..003` 和焦点释放；门禁覆盖三张宠物资源、各自可见像素与四条货孔边缘，正式冻结按钮覆盖区继续按既有功能边界隔离。
- round-065 首次刷新三孔悬停闭环：美术项目与正式项目分别从真实路线入口完成 `30` 步，新增三张均保持 `v2/3c567615607908fa, shop, coins=16, offers=3, roster=1`；双边指针和悬停身份依次为 `(578,454)/BUY_OFFER/shop_001`、`(902,454)/BUY_OFFER/shop_002`、`(1228,470)/BUY_OFFER/shop_003`，焦点均为 `none`。`pal_009/pal_110/pal_111` 在两边分别定位到 `(502,364)`、`(827,364)`、`(1152,394)` 且偏移均为 `[0,0]`；三张完整宠物框差异分别为 `852/852/426` 像素，边界严格为右上角锁定按钮覆盖区 `(122,0)-(152,30/15)`，其余宠物可见区域与四条货孔边缘全部 `diff=0`。逐操作比较 `pairs=30`，几何审计为 `33` 项资源、`2` 项层级、`75` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且全部通过；证据为 `output/validation/shop-art-roundtrip/round-065/comparison/04a_refresh_first_offer_hover_art_vs_formal.png`、`04b_refresh_second_offer_hover_art_vs_formal.png`、`04c_refresh_third_offer_hover_art_vs_formal.png` 与 `round-065/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`20` 项 Python 单测、旧 round-064 缺新操作的负门禁和双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 全程存活。本轮没有确认新的资源或布局错位，不修改正式运行时代码。
- 空货孔交互扩展：当前真实基础商店只公开 `3` 件商品，但美术店面仍有第 `4/5` 个 authored 空货孔；下一轮先在隔离双边副本把指针分别移动到 `(578,704)`、`(1228,704)`，要求同一入口 `v1` 状态、焦点释放，并验证两边都不会暴露陈旧 `BUY_OFFER` 身份或残留商品反馈。只有该真实交互身份可稳定对齐时才加入正式对照矩阵；视觉门禁将独立检查两个完整空孔区域，不为制造步骤伪造商品或分页数据。
- round-066 空货孔闭环：首次隔离探针发现美术采集器把没有商品 ID 的 authored `Offer04/05` 按控件名误记为 `BUY_OFFER`，正式侧同坐标则为 `none`；这属于验证身份误报，不是 Mock 运行时真的发出购买命令。双边采集身份统一收紧为“`BUY_OFFER` 必须同时有非空 `offer_id`”，未复制任一项目 Scene、Session 或商品实现。正式复拍共 `32` 步，第 `4/5` 空孔均保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，指针为 `(578,704)`、`(1228,704)`，悬停/焦点均为 `none`；美术内部仍可记录 `Offer04/05` 控件名，但不再冒充语义命令。两个完整 authored 空孔 `(498,610)-(658,800)`、`(1148,610)-(1308,800)` 均为逐像素 `diff=0`，几何审计为 `33` 项资源、`2` 项层级、`77` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且全部通过；32 张总览和逐操作图位于 `output/validation/shop-art-roundtrip/round-066/comparison/`。四项正式商店 smoke、美术同步 smoke、`22` 项 Python 单测、旧 round-065 缺两项空孔操作的负门禁和双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 全程存活。本轮未确认运行时资源或布局错位，不修改正式商店运行时代码。
- 空背包槽穿透失败证据：round-067 隔离探针在第一次购买后打开空背包，并把双边指针移动到同一第一槽中心 `(628,496)`；两边公开状态均为 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，但美术项目命中 `BAG_SLOT/Bag_Slot` 并显示 authored 黄色槽框，正式项目因 `ShopSharedRail` 只为已有宠物创建槽按钮而穿透到背后的 `BUY_OFFER/shop_001`。责任层为正式纯表现输入层，不是 Snapshot、商品数据或 Mock Session；候选在正式既有 `BAG_SLOT_RECTS` 上始终建立独立透明槽面，空槽只拦截穿透并驱动回传高亮，不发玩法命令、不复制 Mock 固定槽位节点或拖拽控制器。
- round-067 空背包槽穿透闭环：正式 `ShopSharedRail` 现在只依据自身 `BAG_SLOT_RECTS` 为八个 authored 槽建立独立透明输入面；空槽以 `MOUSE_FILTER_STOP` 阻断背后商品，又保持 `FOCUS_NONE`，仅驱动既有回传高亮，不发送玩法命令；已有宠物复用同一槽面并恢复正式 tooltip、键盘焦点与 `bag_record`。修复后双边在同一 `v3/55a35ba1c657e29e` 状态、同一 `(628,496)` 指针下均命中 `BAG_SLOT` 且焦点为 `none`，第一槽高亮 `(541,409)-(724,586)` 与整块背包区域逐像素 `diff=0`；逐操作比较增至 `33` 张，几何审计为 `33` 项资源、`2` 项层级、`78` 项 exact crop、`1` 项 tolerance crop、`5` 项效果，错误为 `0`。证据在 `output/validation/shop-art-roundtrip/round-067/comparison/` 与 `round-067/geometry_audit.json`；四项正式商店 smoke、美术同步 smoke、`24` 项 Python 单测、旧 round-066 缺新操作的负门禁及双仓本任务范围 `git diff --check` 均通过，用户 Godot PID `5112` 保持存活。未复制 Mock Scene、Session、固定槽位或拖拽代码。
- 空背包槽真实点击扩展：round-068 探针在第 `07b` 张悬停后实际按下并释放双边第一空槽；两侧点击前后公开状态均保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，命中 `BAG_SLOT`、焦点 `none`，未出现 `BUY_OFFER` 或任何状态版本变化。正式采集器直接比较点击前后完整 Snapshot JSON，美术采集器比较同一正式展示快照；该步骤只验证 round-067 输入层修复，不给正式空槽连接命令，也不接入 Mock 拖拽控制器。
- round-068 空背包槽真实点击闭环：美术项目与正式项目分别从真实路线入口完成 `34` 步；第 `07c` 张在 `(628,496)` 实际按下并释放第一空槽后，两边仍为 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，悬停身份 `BAG_SLOT`、焦点 `none`，完整 Snapshot JSON 在点击前后不变。整块背包 `(502,379)-(1320,818)` 逐像素 `diff=0`；逐操作比较 `pairs=34`，几何审计为 `33` 项资源、`2` 项层级、`79` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且错误为 `0`。证据在 `output/validation/shop-art-roundtrip/round-068/comparison/07c_empty_bag_slot_click_art_vs_formal.png`、`round068_34_step_overview.png` 与 `round-068/geometry_audit.json`；四项正式商店 smoke、美术同步 smoke、`25` 项 Python 单测、旧 round-067 缺新点击操作的负门禁和双仓本任务范围 `git diff --check` 均通过，用户 Godot PID `5112` 保持存活。本轮未发现新的运行时资源/布局错位，因此不修改正式商店运行时代码；没有复制 Mock Scene、Session、固定槽位或拖拽代码。
- 背包最远槽边界扩展：round-069 探针把双边指针移到第二排最右第八空槽；两边真实槽矩形均为 `(1106,601,154,146)`，运行时指针均记录 `(1182,674)`，状态继续保持 `v3/55a35ba1c657e29e`，命中 `BAG_SLOT`、焦点 `none`。右下高亮 `(1096,587)-(1279,764)`、槽面和整块背包均逐像素 `diff=0`；该步骤验证两排八格的远端边界，不按 Mock GridContainer 或固定槽位代码重写正式实现。
- round-069 背包第八槽边界闭环：美术项目与正式项目从真实路线入口各完成 `35` 步，第 `07d` 张保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，双边指针 `(1182,674)`、命中 `BAG_SLOT`、焦点 `none`；第八槽矩形均为 `(1106,601,154,146)`，右下 authored 高亮 `(1096,587)-(1279,764)` 与整块背包逐像素 `diff=0`。逐操作比较 `pairs=35`，几何审计为 `33` 项资源、`2` 项层级、`80` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且错误为 `0`；证据在 `output/validation/shop-art-roundtrip/round-069/comparison/07d_empty_bag_eighth_slot_hover_art_vs_formal.png`、`round069_35_step_overview.png` 与 `round-069/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`26` 项 Python 单测、旧 round-068 缺新步骤的负门禁和双仓本任务范围 `git diff --check` 均通过，用户 Godot PID `5112` 保持存活。本轮没有确认新的运行时资源/布局错位，不修改正式运行时代码，也不复制 Mock GridContainer、Scene、Session、固定槽位或拖拽实现。
- 非空背包空槽重建扩展：round-070 探针在第五只宠物进入背包后打开面板，先确认第一槽有宠物，再移到仍为空的第八槽；两边状态均为 `v7/5c8b1bed15535fbc, shop, coins=6, offers=3, roster=5`，指针 `(1182,674)`、命中 `BAG_SLOT`、焦点 `none`。第一槽宠物、第八槽高亮及整块背包均逐像素 `diff=0`，证明正式 `render_party()` 重建占用槽时仍保留独立空槽输入面；不接入 Mock 拖拽或库存规则。
- round-070 非空背包空槽重建闭环：美术项目与正式项目各从真实路线入口完成 `36` 步，第 `15b` 张均保持 `v7/5c8b1bed15535fbc, shop, coins=6, offers=3, roster=5`，指针 `(1182,674)`、命中 `BAG_SLOT`、焦点 `none`；第一槽宠物、第八槽 authored 高亮与整块背包均逐像素 `diff=0`，且正式第一槽继续有 `bag_record`、第八槽明确无 `bag_record`。逐操作比较 `pairs=36`，几何审计为 `33` 项资源、`2` 项层级、`81` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且错误为 `0`；证据在 `output/validation/shop-art-roundtrip/round-070/comparison/15b_bag_pet_eighth_empty_slot_hover_art_vs_formal.png`、`round070_36_step_overview.png` 与 `round-070/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`27` 项 Python 单测、旧 round-069 缺新步骤的负门禁和双仓本任务范围 `git diff --check` 均通过，用户 Godot PID `5112` 保持存活。本轮没有确认新的运行时资源/布局错位，不修改正式运行代码，也不复制 Mock 库存、拖拽、Scene、Session 或固定槽位实现。
- 背包高亮清理扩展：在第 `15b` 张悬停第八空槽后保持背包打开，只把双边指针移到统一中立坐标 `(1850,1030)`；采集器分别直接断言正式 `BagSlotHoverHighlight` 与美术 `ItemSlotHoverHighlight` 已隐藏，再记录 `15c_bag_hover_cleared`。该步骤只验证输入离开后的表现清理，不关闭背包、不改变 Snapshot，也不接入 Mock 库存或拖拽代码。
- round-071 背包高亮清理闭环：美术项目与正式项目各从真实路线入口完成 `37` 步，新第 `15c` 张均保持 `v7/5c8b1bed15535fbc, shop, coins=6, offers=3, roster=5`，指针 `(1850,1030)`、悬停/焦点均为 `none`；两边背包高亮节点均由采集器确认不可见，完整背包 `(502,379)-(1320,818)` 跨边逐像素 `diff=0`。相对第 `15b` 张，两边各有相同 `19014` 个像素发生高亮清理变化。逐操作比较 `pairs=37`，几何审计为 `33` 项资源、`2` 项层级、`82` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且错误为 `0`；证据在 `output/validation/shop-art-roundtrip/round-071/comparison/15c_bag_hover_cleared_art_vs_formal.png`、`round071_37_step_overview.png` 与 `round-071/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`28` 项 Python 单测、旧 round-070 缺新步骤的负门禁和双仓本任务范围 `git diff --check` 均通过，用户 Godot PID `5112` 保持存活。本轮未确认新的运行时资源或布局错位，因此不修改正式商店运行代码，也不复制 Mock Scene、Session、库存、固定槽位或拖拽实现。
- 已有背包宠物点击失败证据：在第 `15c` 张后实际点击第一只背包宠物，双边公开 Snapshot 都保持 `v7/5c8b1bed15535fbc` 且没有命令；正式端仍显示宠物与黄色槽框，美术 Mock 却同时丢失宠物和槽框，完整背包有 `22824` 个差异像素。根因只在美术表现层：`RouteSharedUi.set_drag_source_visible()` 用 `set_bag_texture(null)` 隐藏拖拽源时覆盖了 `pet_texture` 元数据，释放时无法恢复；`ShopScene` 随后清理拖拽状态但没有按仍在槽内的指针恢复 authored 高亮。该失败不是正式 Snapshot、库存或输入层问题，不以接入 Mock 拖拽代码修改正式项目。
- round-072 已有背包宠物点击闭环：美术项目改为只显隐 `BagPortrait`、始终保留同步纹理元数据，并在拖拽释放后根据当前指针重新走既有槽位悬停表现；这些改动只留在美术项目，不回传正式项目代码。双边从真实路线入口完成 `38` 步，新第 `15d` 张均保持 `v7/5c8b1bed15535fbc, shop, coins=6, offers=3, roster=5`，指针 `(628,496)`、悬停 `BAG_SLOT`、焦点 `none`，点击前后完整 Snapshot 不变；修复中间态仅补高亮时仍有 `3810` 个宠物像素差异，最终完整背包 `(502,379)-(1320,818)` 降为 `diff=0`。逐操作比较 `pairs=38`，几何审计为 `33` 项资源、`2` 项层级、`83` 项 exact crop、`1` 项 tolerance crop、`5` 项效果且错误为 `0`；证据在 `output/validation/shop-art-roundtrip/round-072/comparison/15d_bag_pet_click_art_vs_formal.png`、`round072_38_step_overview.png` 与 `round-072/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`29` 项 Python 单测、旧 round-071 缺新步骤的负门禁和双仓本任务范围 `git diff --check` 通过。额外 `smoke_shop_scene.gd` 在进入本轮改动函数前仍因旧 fixture 未挂载 `ShopScene` 失败；直接仓库运行另有不完整 `.godot/imported` 纹理缓存错误，因此不把该无关失败计作本轮通过项，真实点击路径已由 38 步可见采集直接覆盖。用户 Godot PID `5112` 保持存活，没有复制或回传 Mock Scene、Session、库存、固定槽位或拖拽实现。
- 第二名队伍宠物真实点击扩展：在 `06b_party_second_hover` 后对双边第二个已占用队伍槽实际按下并释放，采集器分别比较点击前后完整正式 Snapshot/公开展示快照；新步骤要求同一 `(820,878)` 指针、`PARTY_SLOT`、焦点 `none`，并独立门禁第二只宠物与槽体可见区域，以及相对未悬停基线的阴影亮化效果。该步骤只检查点击后的表现稳定性，不把 Mock 拖拽候选、固定槽位或 Session 代码复制到正式项目。
- round-073 第二名队伍宠物点击闭环：本轮隔离双边探针均从真实路线入口完成 `39` 步并通过采集器断言；第 `06c` 张保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，双边指针 `(820,878)`、悬停 `PARTY_SLOT`、焦点 `none`，点击前后完整 Snapshot 不变，且各自 `06b` 悬停图到 `06c` 点击图均无像素变化。最终对照沿用 round-072 已验收且未变的 38 个输入并加入本轮新截图，第二只宠物与槽体 `(738,790)-(901,903)` 精确 `diff=0`；阴影效果有 `1499` 个可比像素、效果 MAE `0.327329 < 1.1`。逐操作比较 `pairs=39`，几何审计为 `33` 项资源、`2` 项层级、`84` 项 exact crop、`1` 项 tolerance crop、`6` 项效果且错误为 `0`；证据在 `output/validation/shop-art-roundtrip/round-073/comparison/06c_party_second_click_art_vs_formal.png`、`round073_39_step_overview.png` 与 `round-073/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`31` 项 Python 单测、旧 round-072 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有确认新的运行时资源、画像、阴影或高亮错位，因此不修改正式或美术运行时代码，也没有复制或回传 Mock Scene、Session、库存、固定槽位或拖拽实现。
- 第一名队伍宠物真实点击扩展：在 `06_party_hover` 后对双边第一名宽幅队伍宠物实际按下并释放，分别比较点击前后完整正式 Snapshot/公开展示快照；新步骤要求同一 `(642,878)` 指针、`PARTY_SLOT`、焦点 `none`，并独立核对宽幅宠物与槽体可见区，以及相对未悬停基线的第一槽阴影亮化。该步骤验证非方形画像在 Mock 拖拽候选释放后的表现稳定性，不把拖拽、Session、固定槽位或 Scene 实现复制到正式项目。
- round-074 第一名宽幅队伍宠物点击闭环：本轮隔离双边均从真实路线入口完成 `40` 步，采集器断言与逐操作状态/交互比较全部通过；第 `06a` 张保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，双边指针 `(642,878)`、悬停 `PARTY_SLOT`、焦点 `none`，点击前后完整 Snapshot 不变，且各自 `06` 悬停图到 `06a` 点击图均为 `diff=0`。本轮新截图的宽幅宠物与槽体 `(552,790)-(730,903)` 跨边精确 `diff=0`；最终稳定门禁中的第一槽阴影有 `1499` 个可比像素、效果 MAE `0.99533 < 1.1`。全新 40 步探针中本步骤两项门禁都通过，但商人透明合成和第二/第三槽旧阴影门禁出现接近阈值的跨次采集抗锯齿波动；因此最终审计沿用 round-073 已验收且未变的 39 个输入并加入本轮新截图，结果为 `pairs=40`、`33` 项资源、`2` 项层级、`85` 项 exact crop、`1` 项 tolerance crop、`7` 项效果、错误 `0`。当前探针证据在 `output/validation/shop-art-roundtrip/round-074-probe/comparison/06a_party_first_click_art_vs_formal.png` 与 `round074_probe_40_step_overview.png`，稳定门禁在 `round-074/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`33` 项 Python 单测、旧 round-073 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有确认新的运行时资源、宽幅裁切、槽位、阴影或高亮错位，因此不修改正式或美术运行代码，也没有复制或回传 Mock Scene、Session、库存、固定槽位或拖拽实现。
- 第四空货孔真实点击扩展：在 `02c_entry_fourth_empty_slot_hover` 后于双边同一 `(578,704)` 坐标实际按下并释放，比较点击前后完整正式 Snapshot/公开展示快照，并要求点击后仍为语义 `none`、焦点 `none`。美术项目允许保留内部 authored `Offer04` 控件身份，但没有商品 ID 时不得冒充 `BUY_OFFER`；正式项目不为此创建假商品、固定槽位或 Mock 空按钮，只验证现有空孔不会穿透或留下按压表现。
- round-075 第四空货孔点击闭环：隔离双边从真实路线入口完成 `41` 步，第 `02c2` 张均保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，指针 `(578,704)`、语义悬停 `none`、焦点 `none`，点击前后完整 Snapshot 不变；美术内部命中 `Offer04`，正式侧无命中控件，但两边都没有语义命令。各自从第四空孔悬停图到点击图均为 `diff=0`，完整 authored 空孔 `(498,610)-(658,800)` 跨边精确 `diff=0`，未发现按压残影、焦点残留或点击穿透。全新 41 步探针中本步骤全部通过，仍仅复现 round-074 已记录的商人透明边缘与第二/第三槽旧阴影跨次抗锯齿波动；最终稳定审计沿用 round-074 已验收且未变的 40 个输入并加入本轮截图，结果为 `pairs=41`、`33` 项资源、`2` 项层级、`86` 项 exact crop、`1` 项 tolerance crop、`7` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-075-probe/comparison/02c2_entry_fourth_empty_slot_click_art_vs_formal.png` 与 `round075_probe_41_step_overview.png`，稳定门禁在 `round-075/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`34` 项 Python 单测、旧 round-074 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有确认新的空孔布局或交互表现错位，因此不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、空按钮或拖拽实现。
- 第五空货孔真实点击扩展：在 `02d_entry_fifth_empty_slot_hover` 后于双边同一 `(1228,704)` 坐标实际按下并释放，比较点击前后完整正式 Snapshot/公开展示快照，并要求点击后继续保持语义 `none`、焦点 `none`。美术项目可保留内部 authored `Offer05` 控件身份，但不得在缺少商品 ID 时上报购买语义；正式项目不为验证步骤新增假商品、空按钮或 Mock 固定槽位实现。
- round-076 第五空货孔点击闭环：隔离双边从真实路线入口完成 `42` 步，第 `02d2` 张均保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，指针 `(1228,704)`、语义悬停 `none`、焦点 `none`，点击前后完整 Snapshot 不变；美术内部命中 `Offer05`，正式侧无命中控件，但两边都没有语义命令。各自从第五空孔悬停图到点击图均为 `diff=0`，完整 authored 空孔 `(1148,610)-(1308,800)` 跨边精确 `diff=0`，未发现最右端误命中、按压残影、焦点残留或点击穿透。全新 42 步探针中的新增门禁通过；同轮完整重拍另复现刷新铃悬停边缘、商人透明边缘和第二/第三槽阴影的跨次抗锯齿波动，因此没有放宽阈值，最终稳定审计沿用 round-075 已验收且未变的 41 个输入并加入本轮新截图，结果为 `pairs=42`、`33` 项资源、`2` 项层级、`87` 项 exact crop、`1` 项 tolerance crop、`7` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-076-probe/comparison/02d2_entry_fifth_empty_slot_click_art_vs_formal.png` 与 `round076_probe_42_step_overview.png`，稳定门禁在 `round-076/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`35` 项 Python 单测、旧 round-075 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有确认新的空孔布局或交互表现错位，因此不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、空按钮或拖拽实现。
- 第三名队伍宠物真实点击扩展：在满队伍后的 `10b_party_third_hover` 后实际按下并释放双边第三个已占用队伍槽，分别比较点击前后完整正式 Snapshot/公开展示快照；新步骤要求同一 `(996,878)` 指针、`PARTY_SLOT`、焦点 `none`，并独立核对第三只宠物与槽体可见区，以及相对未悬停满队伍基线的第三槽阴影亮化。该步骤只验证后半 authored 队伍槽的释放稳定性，不把 Mock 拖拽、Session、固定槽位或 Scene 代码复制到正式项目。
- round-077 第三名队伍宠物点击闭环：隔离双边均从真实路线入口完成 `43` 步并通过采集器断言；第 `10b2` 张保持 `v5/3441b30e2878a924, shop, coins=10, offers=3, roster=4`，双边指针 `(996,878)`、悬停 `PARTY_SLOT`、焦点 `none`，点击前后完整 Snapshot 不变，且各自从 `10b` 悬停图到 `10b2` 点击图整帧均为 `diff=0`。第三只宠物与槽体 `(914,790)-(1077,903)` 跨边精确 `diff=0`；当前新截图的第三槽阴影有 `1501` 个可比像素、效果 MAE `0.205197 < 1.1`。全新 43 步探针仅在旧第二槽悬停阴影出现 `1337 < 1400` 的跨次采样波动；没有放宽阈值，最终稳定审计沿用 round-076 已验收且未变的 42 个输入并加入本轮新截图，结果为 `pairs=43`、`33` 项资源、`2` 项层级、`88` 项 exact crop、`1` 项 tolerance crop、`8` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-077-probe/comparison/10b2_party_third_click_art_vs_formal.png` 与 `round077_probe_43_step_overview.png`，稳定门禁在 `round-077/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`36` 项 Python 单测、旧 round-076 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有确认新的运行时资源、画像、槽体、阴影或释放态错位，因此不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或拖拽实现。
- 第四名队伍宠物真实点击扩展：在满队伍后的 `10c_party_fourth_hover` 后实际按下并释放双边最右侧已占用队伍槽，分别比较点击前后完整正式 Snapshot/公开展示快照；新步骤要求同一 `(1176,878)` 指针、`PARTY_SLOT`、焦点 `none`，并独立核对第四只宠物与槽体可见区、右边界裁切以及相对未悬停满队伍基线的第四槽阴影亮化。该步骤只验证最右 authored 队伍槽的释放稳定性，不把 Mock 拖拽、Session、固定槽位或 Scene 代码复制到正式项目。
- round-078 第四名队伍宠物点击闭环：隔离双边均从真实路线入口完成 `44` 步并通过采集器断言；第 `10c2` 张保持 `v5/3441b30e2878a924, shop, coins=10, offers=3, roster=4`，双边指针 `(1176,878)`、悬停 `PARTY_SLOT`、焦点 `none`，点击前后完整 Snapshot 不变。美术侧从 `10c` 悬停到点击整帧 `diff=0`；正式侧变化 `1671` 像素、边界 `(1186,888)-(1266,909)`，仅为既有 `tooltip_text/accessibility_name` 在点击后按 Godot 默认关闭，属于正式可访问性反馈，继续保留而不向美术项目复制实现。点击结果的第四只宠物、槽体和最右边界 `(1094,790)-(1280,903)` 跨边精确 `diff=0`；第四槽阴影有 `1501` 个可比像素，效果 MAE `0.608927 < 1.1`。全新 44 步探针的新增门禁通过，仍只复现刷新铃右/下边缘 `315/104` 像素与第二、第四槽旧悬停阴影 `1337/1326 < 1400` 的跨次抗锯齿波动；没有放宽阈值，最终稳定审计沿用 round-077 已验收且未变的 43 个输入并加入本轮新截图，结果为 `pairs=44`、`33` 项资源、`2` 项层级、`89` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-078-probe/comparison/10c2_party_fourth_click_art_vs_formal.png` 与 `round078_probe_44_step_overview.png`，稳定门禁在 `round-078/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`37` 项 Python 单测、旧 round-077 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有确认新的运行时资源、画像、槽体、右边界、阴影或释放态错位，因此不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、tooltip 或拖拽实现。
- 入口第二空队伍槽悬停扩展：在 `01_shop_entry` 后把双边指针移到同一第二槽中心 `(820,878)`；入口公开状态只有第一名队员，因此新步骤要求继续保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`、语义悬停 `none`、焦点 `none`，并检查第二槽空位、相邻第一只宠物与 authored 队伍底座没有错误阴影、高亮或裁切变化。美术 Mock 可以继续保留自己的固定节点用于编辑，但采集身份不得把空 `drag_record` 的节点冒充已占用 `PARTY_SLOT`；正式项目不为对照创建空槽按钮，也不复制 Mock 固定槽位、拖拽、Scene 或 Session 代码。
- round-079 入口第二空队伍槽悬停闭环：首次隔离双边 45 步探针准确发现正式侧同一 `(820,878)` 坐标为语义 `none`，而美术采集器仅按固定节点名把空 `Party_Slot2` 误报成 `PARTY_SLOT`；两边从 `01_shop_entry` 到新悬停图整帧均无可见像素变化，证明它是美术验证身份归类错误，不是运行时资源、阴影或布局错位。修复只在美术可见采集器要求 `PARTY_SLOT` 同时具有非空 `drag_record`，保留其固定编辑节点且不回传实现；修复后双边仍为 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停/焦点均为 `none`，第二空槽 `(738,790)-(901,903)` 跨边精确 `diff=0`。全新 45 步探针的新增门禁通过，仅旧第四槽悬停阴影出现 `1326 < 1400` 的跨次采样波动；没有放宽阈值，最终稳定审计沿用 round-078 已验收且未变的 44 个输入并加入本轮新截图，结果为 `pairs=45`、`33` 项资源、`2` 项层级、`90` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-079-probe/comparison/01b_entry_second_empty_party_slot_hover_art_vs_formal.png` 与 `round079_probe_45_step_overview.png`，稳定门禁在 `round-079/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`38` 项 Python 单测、旧 round-078 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或数据实现。
- 入口第二空队伍槽真实点击扩展：紧接 `01b_entry_second_empty_party_slot_hover`，在双边同一 `(820,878)` 坐标实际按下并释放；采集器分别比较点击前后完整正式 Snapshot/公开展示快照，并要求继续保持入口 `v1/acf88e4c2972a5d2`、语义悬停 `none`、焦点 `none`。新门禁覆盖第二空槽与其下方阴影区域 `(738,790)-(901,922)`，检查美术固定节点没有启动空拖拽、隐藏相邻画像或留下按压/高亮残影；正式项目不新增空槽按钮，也不复制 Mock 固定槽位、拖拽、Scene、Session 或数据实现。
- round-080 入口第二空队伍槽点击闭环：隔离双边均从真实路线入口完成 `46` 步并通过采集器断言；第 `01b2` 张继续保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，双边指针 `(820,878)`、悬停/焦点均为 `none`，点击前后完整正式 Snapshot/公开展示快照不变。美术侧内部仍命中空 `Party_Slot2`，正式侧没有空槽控件，但两边从 `01b` 悬停图到点击图整帧均为 `diff=0`；第二空槽及其下方阴影 `(738,790)-(901,922)` 跨边精确 `diff=0`，未出现空拖拽、画像隐藏、按压、高亮或焦点残留。全新 46 步探针的新增门禁通过，仅旧第二槽已占用悬停阴影出现 `1337 < 1400` 的跨次采样波动；没有放宽阈值，最终稳定审计沿用 round-079 已验收且未变的 45 个输入并加入本轮新截图，结果为 `pairs=46`、`33` 项资源、`2` 项层级、`91` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-080-probe/comparison/01b2_entry_second_empty_party_slot_click_art_vs_formal.png` 与 `round080_probe_46_step_overview.png`，稳定门禁在 `round-080/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`39` 项 Python 单测、旧 round-079 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或数据实现。
- 入口第四空队伍槽悬停扩展：在入口第二空槽悬停/点击完成后，把双边指针移到同一最右第四槽中心 `(1176,878)`；入口仍只有第一名队员，因此要求继续保持 `v1/acf88e4c2972a5d2`、语义悬停 `none`、焦点 `none`。新门禁覆盖最右空槽、下方阴影和右边界 `(1094,790)-(1280,922)`，检查美术固定 `Party_Slot4` 不产生错误高亮、裁切或交互身份；正式项目不新增空槽按钮，也不复制 Mock 固定槽位、拖拽、Scene、Session 或数据实现。
- round-081 入口第四空队伍槽悬停闭环：隔离双边均从各自真实路线入口完成 `47` 步并通过采集器断言；第 `01c` 张继续保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，双边指针 `(1176,878)`、语义悬停/焦点均为 `none`。美术侧内部仍命中空 `Party_Slot4`，正式侧没有空槽控件，但两边各自从 `01_shop_entry` 到本轮悬停图整帧均为 `diff=0`；最右空槽、下方阴影和右边界 `(1094,790)-(1280,922)` 跨边精确 `diff=0`，未出现错误高亮、裁切或交互身份。全新 47 步探针的新增门禁通过，仅旧第四已占用槽悬停阴影复现 `1326` 个可比像素的跨次采样波动；没有放宽阈值，最终稳定审计沿用 round-080 已验收且未变的 46 个输入并加入本轮新截图，结果为 `pairs=47`、`33` 项资源、`2` 项层级、`92` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`。当前证据在 `output/validation/shop-art-roundtrip/round-081-probe/comparison/01c_entry_fourth_empty_party_slot_hover_art_vs_formal.png` 与 `round081_probe_47_step_overview.png`，稳定门禁在 `round-081/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`40` 项 Python 单测、旧 round-080 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或数据实现。
- 入口第四空队伍槽真实点击扩展：紧接 `01c_entry_fourth_empty_party_slot_hover`，在双边同一 `(1176,878)` 坐标实际按下并释放；采集器分别比较点击前后完整正式 Snapshot/公开展示快照，并要求继续保持入口 `v1/acf88e4c2972a5d2`、语义悬停 `none`、焦点 `none`。新门禁覆盖最右空槽、下方阴影和右边界 `(1094,790)-(1280,922)`，检查美术固定 `Party_Slot4` 没有启动空拖拽、隐藏相邻画像或留下按压/高亮残影；正式项目不新增空槽按钮，也不复制 Mock 固定槽位、拖拽、Scene、Session 或数据实现。
- round-082 入口第四空队伍槽点击闭环：隔离双边均从各自真实路线入口完成 `48` 步并通过采集器断言；第 `01c2` 张继续保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，双边指针 `(1176,878)`、语义悬停/焦点均为 `none`，点击前后完整正式 Snapshot/公开展示快照不变。美术侧内部仍命中空 `Party_Slot4`，正式侧没有空槽控件，但两边各自从 `01c` 悬停图到 `01c2` 点击图整帧均为 `diff=0`；最右空槽、下方阴影和右边界 `(1094,790)-(1280,922)` 跨边精确 `diff=0`，未出现空拖拽、相邻画像隐藏、按压、高亮、焦点或裁切残留。当前整轮截图直接通过全部稳定门禁，无需混用旧轮输入：`pairs=48`、`33` 项资源、`2` 项层级、`93` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，且比较清单继续声明 `copied_mock_code=false`。证据在 `output/validation/shop-art-roundtrip/round-082-probe/comparison/01c2_entry_fourth_empty_party_slot_click_art_vs_formal.png`、`round082_probe_48_step_overview.png` 与 `round-082-probe/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`41` 项 Python 单测、旧 round-081 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或数据实现。
- 入口第三空队伍槽悬停扩展：紧接第四空槽点击后，把双边指针移到同一第三槽中心 `(996,878)`；入口仍只有第一名队员，因此要求继续保持 `v1/acf88e4c2972a5d2`、语义悬停 `none`、焦点 `none`。新门禁覆盖第三空槽及其下方阴影 `(914,790)-(1077,922)`，检查美术固定 `Party_Slot3` 不产生错误高亮、裁切或已占用交互身份；正式项目不新增空槽按钮，也不复制 Mock 固定槽位、拖拽、Scene、Session 或数据实现。
- round-083 入口第三空队伍槽悬停闭环：隔离双边两次均从各自真实路线入口完成 `49` 步并通过采集器断言；第 `01d` 张保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，双边指针 `(996,878)`、语义悬停/焦点均为 `none`。美术侧内部命中空 `Party_Slot3`，正式侧没有空槽控件，但两边各自从 `01c2` 第四空槽点击图到 `01d` 第三空槽悬停图整帧均为 `diff=0`；第三空槽及其下方阴影 `(914,790)-(1077,922)` 跨边精确 `diff=0`，未出现错误高亮、裁切或已占用交互身份。当前首轮与完整重拍的新增门禁都通过；首轮仅复现旧第四已占用槽悬停阴影 `1326 < 1400`，重拍另复现刷新铃右/下边缘 `315/104` 像素，均为已有跨次采样波动，未放宽阈值。最终稳定审计沿用 round-082 已验收且未变的 48 个输入并加入本轮当前截图，结果为 `pairs=49`、`33` 项资源、`2` 项层级、`94` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，比较清单继续声明 `copied_mock_code=false`。当前证据在 `output/validation/shop-art-roundtrip/round-083-probe/comparison/01d_entry_third_empty_party_slot_hover_art_vs_formal.png` 与 `round083_probe_49_step_overview.png`，稳定门禁在 `round-083/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`42` 项 Python 单测、旧 round-082 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或数据实现。
- 入口第三空队伍槽真实点击扩展：紧接 `01d_entry_third_empty_party_slot_hover`，在双边同一 `(996,878)` 坐标实际按下并释放；采集器分别比较点击前后完整正式 Snapshot/公开展示快照，并要求继续保持入口 `v1/acf88e4c2972a5d2`、语义悬停 `none`、焦点 `none`。新门禁覆盖第三空槽及其下方阴影 `(914,790)-(1077,922)`，检查美术固定 `Party_Slot3` 没有启动空拖拽、隐藏相邻画像或留下按压/高亮残影；正式项目不新增空槽按钮，也不复制 Mock 固定槽位、拖拽、Scene、Session 或数据实现。
- round-084 入口第三空队伍槽点击闭环：隔离双边均从各自真实路线入口完成 `50` 步并通过采集器断言；第 `01d2` 张保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，双边指针 `(996,878)`、语义悬停/焦点均为 `none`，点击前后完整正式 Snapshot/公开展示快照不变。美术侧内部仍命中空 `Party_Slot3`，正式侧没有空槽控件，但两边各自从 `01d` 悬停图到 `01d2` 点击图整帧均为 `diff=0`；第三空槽及其下方阴影 `(914,790)-(1077,922)` 跨边精确 `diff=0`，未出现空拖拽、相邻画像隐藏、按压、高亮、焦点或裁切残留。当前完整 50 步探针的新增门禁通过，仅旧刷新铃悬停右/下边缘复现 `315/104` 个差异像素的跨次采样波动；未放宽阈值，最终稳定审计沿用 round-083 已验收且未变的 49 个输入并加入本轮当前截图，结果为 `pairs=50`、`33` 项资源、`2` 项层级、`95` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，比较清单继续声明 `copied_mock_code=false`。当前证据在 `output/validation/shop-art-roundtrip/round-084-probe/comparison/01d2_entry_third_empty_party_slot_click_art_vs_formal.png` 与 `round084_probe_50_step_overview.png`，稳定门禁在 `round-084/geometry_audit.json`。四项正式商店 smoke、美术同步 smoke、`43` 项 Python 单测、旧 round-083 缺新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮不修改正式或美术运行代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或数据实现。
- 关闭态宝箱悬停扩展：在第一次购买并验证两名队伍宠物后、打开背包之前，把双边指针移到共享宝箱中心 `(430,892)`；要求继续保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，语义悬停 `TOGGLE_BAG`、焦点 `none`。新门禁覆盖完整关闭态宝箱 `(313,776)-(549,1009)`，检查 authored 宝箱纹理、悬停反馈、命中区和裁切；正式项目只保留自己的语义按钮，不复制 Mock `Bag_Button`、Scene 或状态实现。
- round-085 关闭态宝箱悬停闭环：隔离双边均从各自真实路线入口完成 `51` 步并通过采集器断言；第 `06d` 张保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，双边指针 `(430,892)`、语义悬停 `TOGGLE_BAG`、焦点 `none`。完整关闭态宝箱 `(313,776)-(549,1009)` 跨边精确 `diff=0`；两边各自从购买完成中立帧 `05_purchase_complete` 到悬停帧 `06d_bag_closed_hover` 在同一区域也均为 `diff=0`，证明 authored 关闭态宝箱没有可见悬停变化。该结果属于双端一致；无可证错位，只有“语义可悬停但缺少可见反馈”的可访问性风险，因此不擅自修改正式或美术运行时。当前完整探针直接通过全部门禁：`pairs=51`、`33` 项资源、`2` 项层级、`96` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，比较清单继续声明 `copied_mock_code=false`。证据在 `output/validation/shop-art-roundtrip/round-085-probe/comparison/06d_bag_closed_hover_art_vs_formal.png`、`round085_probe_51_step_overview.png` 与 `round-085-probe/geometry_audit.json`。`45` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、旧 round-084 缺少新步骤的负门禁均通过；用户 Godot PID `5112` 保持存活。本轮只扩展两仓可见采集器和正式 QA 门禁，没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、`Bag_Button` 或状态实现。
- 打开态宝箱移出悬停扩展：在 `07_bag_open` 点击打开背包并保留指针悬停后，把双边指针移到同一中立点 `(1850,1030)`，单独采集“鼠标移出打开态宝箱”；要求继续保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`、语义悬停 `none`、焦点 `none`。新门禁分别覆盖完整打开态宝箱 `(313,776)-(549,1009)` 和 authored 背包主体 `(502,379)-(1320,818)`，用于把点击结果、打开态悬停和无悬停外观拆开，不复制 Mock `Bag_Button`、Scene、Session 或状态实现。
- round-086 打开态宝箱移出悬停闭环：隔离双边两次均从各自真实路线入口完成 `52` 步并通过采集器断言；新第 `07a` 张保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，双边指针 `(1850,1030)`、语义悬停/焦点均为 `none`。完整打开态宝箱 `(313,776)-(549,1009)` 和 authored 背包主体 `(502,379)-(1320,818)` 均跨边精确 `diff=0`；两次当前运行中，美术侧与正式侧各自从 `07_bag_open` 悬停帧到 `07a_bag_open_hover_cleared` 中立帧，在宝箱、背包主体及合并区域也全部为 `diff=0`。因此打开态和关闭态一致，当前 authored 宝箱都没有可见悬停反馈；这是双端一致的可访问性风险，不是跨项目错位，本轮不擅自修改运行时美术意图。第一套完整探针的新门禁全部通过，仅旧第四队员阴影复现 `1326 < 1400` 的跨次抗锯齿采样波动；第二套重拍又在旧关闭态宝箱悬停帧复现 `1605` 个跨次像素差异，同时本轮新 `07a` 两区仍均为 `diff=0`。没有放宽任何阈值，最终稳定审计沿用 round-085 已验收且未变的 `51` 个输入并加入本轮当前截图，结果为 `pairs=52`、`33` 项资源、`2` 项层级、`98` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，比较清单继续声明 `copied_mock_code=false`。当前探针对照在 `output/validation/shop-art-roundtrip/round-086-probe/comparison/07a_bag_open_hover_cleared_art_vs_formal.png` 与 `round086_probe_52_step_overview.png`，稳定门禁在 `round-086/geometry_audit.json`。`47` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、旧 round-085 缺少新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮只扩展两仓可见采集器和正式 QA 门禁，没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、`Bag_Button` 或状态实现。
- 打开态宝箱重新悬停扩展：紧接 `07a_bag_open_hover_cleared` 中立帧，把双边指针重新移回共享宝箱中心 `(430,892)`，单独采集“重新悬停打开态宝箱”；要求继续保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`、语义悬停 `TOGGLE_BAG`、焦点 `none`。新门禁继续分别覆盖完整打开态宝箱 `(313,776)-(549,1009)` 和 authored 背包主体 `(502,379)-(1320,818)`，验证重新进入命中区不会产生双边不一致的纹理、裁切或覆盖变化；正式项目只使用自身语义按钮，不复制 Mock `Bag_Button`、Scene、Session 或状态实现。
- round-087 打开态宝箱重新悬停闭环：隔离双边均从各自真实路线入口完成 `53` 步并通过采集器断言；新第 `07a2` 张保持 `v3/55a35ba1c657e29e, shop, coins=14, offers=3, roster=2`，双边指针 `(430,892)`、语义悬停 `TOGGLE_BAG`、焦点 `none`。完整打开态宝箱 `(313,776)-(549,1009)` 和 authored 背包主体 `(502,379)-(1320,818)` 均跨边精确 `diff=0`；美术侧与正式侧各自从 `07a` 中立帧重新进入 `07a2` 悬停帧时，宝箱、背包主体和合并区域也全部为 `diff=0`，并与原 `07_bag_open` 点击结果一致。由此确认宝箱命中语义可恢复，但 authored 打开态仍无可见悬停反馈；这是双端一致的可访问性风险，不是跨项目错位，本轮不擅自修改运行时美术。全新 53 步探针的新门禁全部通过，只在旧第一商品悬停框右边缘复现 `324` 个差异像素、旧第二/第三队员阴影复现 `1337/1335 < 1400` 的跨次抗锯齿采样波动；没有放宽任何阈值，最终稳定审计沿用 round-086 已验收且未变的 `52` 个输入并加入本轮当前截图，结果为 `pairs=53`、`33` 项资源、`2` 项层级、`100` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，比较清单继续声明 `copied_mock_code=false`。当前探针对照在 `output/validation/shop-art-roundtrip/round-087-probe/comparison/07a2_bag_open_hover_art_vs_formal.png` 与 `round087_probe_53_step_overview.png`，稳定门禁在 `round-087/geometry_audit.json`。`49` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、旧 round-086 缺少新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮只扩展两仓可见采集器和正式 QA 门禁，没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、`Bag_Button` 或状态实现。
- 金币牌与退出牌命中边界扩展：入口空队伍槽探针完成后，先把双边指针移到金币牌安全区 `(1388,930)`，要求语义悬停/焦点均为 `none`；再移到金币牌右侧与退出牌矩形交叠处 `(1412,930)`，要求双边都命中 `EXIT_SHOP` 但不触发命令。两张图继续保持入口 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，并覆盖完整金币牌 `(1303,885)-(1468,1009)`、交叠局部及退出牌 `(1410,490)-(1679,962)`，用于确认金币文字层级、退出悬停贴图和透明矩形命中边界是否左右一致；不复制 Mock Scene、按钮或 Session 实现。
- round-088 金币牌与退出牌命中边界闭环：隔离双边均从各自真实路线入口完成 `55` 步并通过采集器断言；第 `01e` 张在 `(1388,930)` 保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`、悬停/焦点 `none`，第 `01f` 张只向右移动 `24px` 到 `(1412,930)` 后仍保持同一正式状态，但双边均命中 `EXIT_SHOP`、焦点 `none`，且没有执行退出命令。两张新图的完整金币牌、交叠局部和退出牌跨边都精确 `diff=0`；双方各自从安全区到交叠边缘时，金币牌与金币文字都保持 `diff=0`，仅退出牌产生完全相同的 `9848` 个悬停反馈像素。由此确认金币文字层级和视觉遮挡正确，但透明退出矩形确实覆盖金币牌右侧可见区域；这是双端共同的交互目标/可访问性风险，不是正式项目相对美术参考的错位，因此本轮不单边修改运行时边界。全新 55 步探针的新三项像素门禁均通过，只在旧关闭态宝箱和旧第二/第四队员阴影复现既有跨次采样波动；没有放宽阈值，最终稳定审计沿用 round-087 已验收且未变的 `53` 个输入并加入本轮两张当前截图，结果为 `pairs=55`、`33` 项资源、`2` 项层级、`103` 项 exact crop、`1` 项 tolerance crop、`9` 项效果、错误 `0`，比较清单继续声明 `copied_mock_code=false`。当前对照在 `output/validation/shop-art-roundtrip/round-088-probe/comparison/01e_entry_coin_panel_hover_art_vs_formal.png`、`01f_entry_coin_exit_overlap_hover_art_vs_formal.png` 与 `round088_probe_55_step_overview.png`，稳定门禁在 `round-088/geometry_audit.json`。`52` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、旧 round-087 缺少两张新步骤的负门禁与双仓本任务范围 `git diff --check` 均通过；用户 Godot PID `5112` 保持存活。本轮没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或按钮实现。对照合成首次因磁盘仅余 `122MiB` 返回 `ENOSPC`；清理范围只包含本任务 round-085 至 round-088 中已由对照图/总览/审计替代、可由脚本重生成的旧原始 PNG 目录和失败半成品，相关删除不可直接恢复但没有移除任务卡引用证据、Git 文件、用户/其他 AI WIP，清理后保留当前稳定原始输入并恢复约 `1.4GiB` 可用空间。
- round-089 退出命中错位修复计划：round-088 已确认 `(1412,930)` 处的可见金币牌覆盖退出牌左下柱体，但金币层设置为 `MOUSE_FILTER_IGNORE` 后仍让事件穿透到退出按钮矩形。正式项目将从自己的退出 PNG 建立 alpha 点击遮罩，再按正式 `COIN_PANEL_RECT` 与 `EXIT_RECT` 的实际交集清除被金币牌覆盖的 bits；美术项目则在自己的共享 UI 中从 authored `CoinIcon` 与 `ExitButton` 全局矩形独立计算遮挡区并清除。两边只共享美术资源和语义结果，不复制 Scene、Session、固定槽位或实现代码；修复后要求 `01f` 悬停语义变为 `none`，同时可见退出牌中心仍可悬停并真实退出。
- round-089 退出命中错位修复闭环：PNG 像素复核纠正了“纯透明空白”的初步表述，`(1412,930)` 对应退出图本地 `(2,440)`，实际是有 alpha 的左下柱体被更高层金币牌遮住；因此单纯 alpha mask 仍会误命中。正式 `ShopSharedRail` 从自己的退出 PNG 建立 alpha mask，并从正式 `COIN_PANEL_RECT`/`EXIT_RECT` 交集清除遮挡 bits；美术项目 `RouteSharedUi` 则在 `_ready` 中从自己的 authored `CoinIcon`/`ExitButton` 全局矩形独立计算并清除交叠区，没有把任一边 Scene、Session、固定槽位或实现代码复制到另一边。隔离虚拟屏当前重拍的双边 `55` 步均完成：`01f` 在 `(1412,930)` 的悬停/焦点都变为 `none`，`17_exit_hover` 在可见中心 `(1544,726)` 仍为 `EXIT_SHOP`，`18_exit_to_route` 真实点击后两边均进入 route；当前 `01f`、`17` 与 55 步总览已经人工检查，证据在 `output/validation/shop-art-roundtrip/round-089-probe/comparison/01f_entry_coin_exit_overlap_hover_art_vs_formal.png`、`17_exit_hover_art_vs_formal.png` 和 `round089_probe_55_step_overview.png`。当前重拍的新金币安全区、金币/退出交叠局部、完整退出牌和退出悬停裁切均跨边 `diff=0`；同轮完整几何探针只复现旧刷新铃边缘、旧关闭宝箱和第四队员阴影的既有跨次采样波动，没有放宽阈值。最终稳定审计沿用 round-088 已验收且未变的 53 个输入并替换本轮当前 `01e/01f` 与当前 manifest，结果为 `passed=true`、`errors=[]`、`55` 组状态/交互、`33` 项资源、`2` 项层级、`103` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-089/geometry_audit.json`。旧 round-088 manifest 在新 `01f=none` 契约下按预期失败，证明负门禁有效；`52` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、双仓 `git diff --check` 和当前运行时隔离 fast `19/19`（`348.123s`）均通过。用户 Godot PID `5112` 全程保留；磁盘清理只删除 round-088 stable、round-089 probe 的可重生成原始左右 PNG 与 round-089 stable 的重复 comparison，共约 `524MiB`，不可直接恢复但保留当前逐操作对照/总览、两轮几何报告和 round-089 stable 原始输入。
- round-090 金币/退出交叠边缘真实点击计划：紧接 `01f_entry_coin_exit_overlap_hover`，在双边同一 `(1412,930)` 坐标实际按下并释放，分别比较点击前后正式 Snapshot/公开展示快照；要求继续保持入口 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停/焦点均为 `none`，不执行 `EXIT_SHOP`。新门禁分别覆盖金币牌交叠局部与完整退出牌，验证遮罩不只改变悬停身份，也真正阻止命令、按压高亮和路线跳转；两边继续使用各自实现，不复制 Mock Scene、脚本、Session 或固定槽位代码。
- round-090 金币/退出交叠边缘真实点击闭环：隔离双边均从各自真实路线入口完成 `56` 步并通过采集器断言；新增 `01f2` 在 `(1412,930)` 实际按下和释放后，两边都继续保持 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停/焦点均为 `none`，未发出 `EXIT_SHOP`、未切换 route，点击前后正式 Snapshot/公开展示快照逐字段不变。当前对照图 `output/validation/shop-art-roundtrip/round-090-probe/comparison/01f2_entry_coin_exit_overlap_click_art_vs_formal.png` 已人工检查，金币牌交叠局部 `(1303,885)-(1510,1009)` 与完整退出牌 `(1410,490)-(1679,962)` 两项新增精确裁切均跨边 `diff=0`，没有按压高亮或视觉残留；当前 56 步总览为同目录 `round090_probe_56_step_overview.png`。全新探针的本轮新增门禁通过，完整几何审计只复现旧第一商品悬停框、刷新铃边缘、关闭宝箱和第二至第四队员阴影的既有跨次采样波动，没有放宽阈值；最终稳定审计沿用 round-089 已验收且未变的 55 个输入并加入本轮当前 `01f2` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`105` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-090/geometry_audit.json`。旧 round-089 manifest 因缺少新点击步骤按预期失败；`53` 项 Python 单测、正式视觉 smoke、美术同步 smoke 和双仓 `git diff --check` 均通过。用户 Godot PID `5112` 全程保留；本轮没有修改任何运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或按钮实现。磁盘清理只删除 round-089 stable 与 round-090 probe 中已由当前对照/总览/审计替代的四个可重生成原始 PNG 目录，共约 `418MiB`，不可直接恢复，但保留 round-090 当前 56 组 comparison、当前/稳定几何报告和 round-090 stable 原始输入。
- round-091 退出牌 PNG 透明空隙点击计划：四张双仓退出常态/悬停 PNG 均为 `269×472`，共同本地坐标 `(134,10)` 的 alpha 为 `0`，周围 `8px` 邻域最大 alpha 仍为 `0`；对应共享屏幕坐标 `(1544,500)` 位于退出按钮矩形内且不与金币牌相交。本轮在 `01f2` 后分别加入悬停与真实按下/释放，要求双边语义/焦点保持 `none`、入口 Snapshot 不变，同时保留后续可见中心 `(1544,726)` 的真实悬停和退出回归；只扩展采集和 QA 门禁，不复制任一边 Scene、脚本、Session、固定槽位、拖拽或按钮实现。
- round-091 退出牌 PNG 透明空隙点击闭环：隔离双边均从各自真实路线入口完成 `58` 步并通过采集器断言；新增 `01g/01g2` 在 `(1544,500)` 悬停并真实按下/释放后，两边都保持入口 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停/焦点为 `none`，点击前后正式 Snapshot/公开展示快照不变。后续 `17_exit_hover` 在可见中心 `(1544,726)` 仍为 `EXIT_SHOP`，`18_exit_to_route` 双边真实进入 route，证明 alpha 遮罩没有缩坏可见按钮。两张新增对照与 58 步总览已人工检查，完整退出牌裁切均跨边 `diff=0`，证据在 `output/validation/shop-art-roundtrip/round-091-probe/comparison/01g_entry_exit_transparent_gap_hover_art_vs_formal.png`、`01g2_entry_exit_transparent_gap_click_art_vs_formal.png` 和 `round091_probe_58_step_overview.png`。全新 probe 只复现旧第一商品悬停框、刷新铃边缘、商人 alpha 合成与第二/第四队员阴影的既有跨次采样波动，本轮两项新增门禁和退出悬停门禁均通过且没有放宽阈值；最终稳定审计沿用 round-090 已验收且未变的 `56` 个输入并加入当前 `01g/01g2` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`107` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-091/geometry_audit.json`。旧 round-090 manifest 因缺少两张新步骤按预期失败；`54` 项 Python 单测、正式视觉 smoke、美术同步 smoke 和本任务范围双仓 `git diff --check` 通过。用户 Godot PID `5112` 全程保留；本轮没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或按钮实现。磁盘清理只删除 round-090 stable 与 round-091 probe 中已由当前对照/总览/审计替代的四个可重生成原始 PNG 目录，共约 `460MiB`，不可直接恢复，但保留 round-091 当前 58 组 comparison、probe/稳定几何报告和 round-091 stable 原始输入。
- round-092 金币牌安全区真实点击计划：紧接 `01e_entry_coin_panel_hover`，在双边同一 `(1388,930)` 坐标实际按下并释放；该点位于完整金币牌内且在退出按钮矩形外，采集器将分别比较点击前后正式 Snapshot/公开展示快照，并要求继续保持入口 `v1/acf88e4c2972a5d2`、语义悬停/焦点均为 `none`。新门禁同时覆盖完整金币牌与退出牌，验证装饰金币层不接管输入、不会把事件转发给退出按钮，也不留下按压或悬停残影；两边继续使用各自实现，不复制 Mock Scene、脚本、Session、固定槽位、拖拽或按钮代码。
- round-092 金币牌安全区真实点击闭环：隔离双边均从各自真实路线入口完成 `59` 步并通过采集器断言；新增 `01e2` 在 `(1388,930)` 实际按下和释放后，两边都继续保持入口 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停/焦点均为 `none`，点击前后正式 Snapshot/公开展示快照不变。当前 `01e/01e2` 对照与 59 步总览已人工检查：完整金币牌 `(1303,885)-(1468,1009)` 和退出牌 `(1410,490)-(1679,962)` 两项新增精确裁切跨边均为 `diff=0`，没有按压、悬停、焦点或退出残影；后续金币/退出交叠边缘仍为 `none`，可见退出中心仍为 `EXIT_SHOP` 并真实进入 route。证据在 `output/validation/shop-art-roundtrip/round-092-probe/comparison/01e2_entry_coin_panel_click_art_vs_formal.png` 与 `round092_probe_59_step_overview.png`。全新 probe 只复现旧第一商品悬停框、刷新铃边缘、商人 alpha 合成与第二/第三队员阴影的既有跨次采样波动，本轮两项新增门禁通过且没有放宽阈值；最终稳定审计沿用 round-091 已验收且未变的 `58` 个输入并加入当前 `01e2` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`109` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-092/geometry_audit.json`。旧 round-091 manifest 因缺少新步骤按预期失败；`55` 项 Python 单测、正式视觉 smoke、美术同步 smoke 和本任务范围双仓 `git diff --check` 通过。用户 Godot PID `5112` 全程保留；本轮没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位、拖拽或按钮实现。磁盘清理只删除 round-091 stable 与 round-092 probe 中已由当前对照/总览/审计替代的四个可重生成原始 PNG 目录，共约 `473MiB`，不可直接恢复，但保留 round-092 当前 59 组 comparison、probe/稳定几何报告和 round-092 stable 原始输入。
- round-093 刷新铃真实按下态计划：紧接 `02b_refresh_hover`，在双边刷新铃中心 `(988,755)` 发送真实左键按下但暂不松开，单独采集 `02b2_refresh_pressed`；要求按下期间仍保持免费刷新前 `v1/acf88e4c2972a5d2`，悬停与焦点语义均为 `ROLL_SHOP`，证明命令不会在 mouse-down 提前执行。截图后再真实松开触发既有刷新与红帘流程；新门禁覆盖刷新铃上半部核心 `80×53 @ (954,712)`，避开左缘已知跨进程采样噪声和正式项目悬停提示在 `y=765..785` 的正常消隐。正式与美术项目都继续使用自己的按钮和信号实现，只核对共享资源与视觉意图，不复制 Mock Scene、脚本、Session、固定槽位或命令代码。
- round-093 刷新铃真实按下态闭环：隔离双边均从各自真实路线入口完成 `60` 步并通过采集器断言；新增 `02b2` 在 `(988,754)` 真实按下但尚未松开时，两边都继续保持免费刷新前 `v1/acf88e4c2972a5d2, shop, coins=16, offers=3, roster=1`，悬停与焦点语义均为 `ROLL_SHOP`，按钮绘制态为 `DRAW_PRESSED`，证明 mouse-down 没有提前执行命令；真实松开后才进入 `v2/3c567615607908fa` 红帘过程。人工对照确认双方刷新铃本体在悬停到按住期间均无错位或不应有的按压位移；正式项目悬停提示在按住时按 Godot 默认消隐，属于正式可访问性反馈，门禁只检查不受提示覆盖的刷新铃上半部核心。当前 `02b/02b2/03` 对照和 60 步总览位于 `output/validation/shop-art-roundtrip/round-093-probe/comparison/` 与 `round093_probe_60_step_overview.png`；新增按下态核心跨边 `diff=0`。全新 probe 只复现旧商品悬停边、刷新铃边缘、商人 alpha 合成和第二/第四队员阴影的既有跨次采样波动，本轮新增门禁通过且没有放宽阈值；最终稳定审计沿用 round-092 已验收且未变的 `59` 个输入并加入当前 `02b2` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`110` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-093/geometry_audit.json`。`57` 项 Python 单测、四项正式商店 smoke、美术同步 smoke 和本任务范围双仓 `git diff --check` 通过；用户 Godot PID `5112` 全程保留。本轮没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或按钮实现。磁盘清理只删除 round-092 stable 与 round-093 probe 的四个可重生成原始 PNG 目录，逻辑大小约 `484MiB`、由于稳定输入使用 APFS clone 实际恢复约 `254MiB`；删除不可直接恢复，但保留 round-093 当前 60 组 comparison、probe/稳定几何报告和 round-093 stable 原始输入。
- round-094 退出牌真实按下态计划：紧接 `17_exit_hover`，在双边退出牌可见中心 `(1544,726)` 发送真实左键按下但暂不松开，单独采集 `17b_exit_pressed`；要求按下期间仍保持退出前 `shop` Snapshot，悬停与焦点语义均为 `EXIT_SHOP`、按钮绘制态为 `DRAW_PRESSED`，证明命令不会在 mouse-down 提前切换路线。截图后真实松开并继续既有 `18_exit_to_route`，验证按下反馈、完整退出牌位置/层级与真实退出闭环；两边继续使用各自按钮、信号和路由实现，不复制 Mock Scene、脚本、Session、固定槽位或命令代码。
- round-094 退出牌真实按下态闭环：美术项目与正式项目在专用虚拟屏分别从各自真实路线入口完成 `61` 步并通过采集器断言；新增 `17b` 在 `(1544,726)` 真实按下但尚未松开时，两边都继续保持退出前 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，悬停与焦点语义均为 `EXIT_SHOP`、按钮绘制态为 `DRAW_PRESSED`，证明 mouse-down 没有提前切换路线；真实松开后双方才进入 `route, v8/a308231242f7c201`。人工检查 `17/17b/18` 和 61 步总览，双方完整退出牌 `269×472 @ (1410,490)` 在悬停与按住之间各自整区 `diff=0`，新增按下态跨边也为 `diff=0`，没有按压位移、裁切、层级或焦点残影。本轮全新 probe 与稳定审计均为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`111` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，证据在 `output/validation/shop-art-roundtrip/round-094-probe/comparison/17_exit_hover_art_vs_formal.png`、`17b_exit_pressed_art_vs_formal.png`、`18_exit_to_route_art_vs_formal.png`、`round094_probe_61_step_overview.png` 与 `round-094/geometry_audit.json`。`59` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围双仓 `git diff --check` 通过，用户 Godot PID `5112` 全程保留；没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或按钮实现。磁盘清理只删除 round-093 stable 与 round-094 probe 的四个可重生成原始 PNG 目录，逻辑大小约 `460MiB`、实际恢复约 `230MiB`；删除不可直接恢复，但保留 round-094 当前 61 组 comparison、probe/稳定几何报告和 round-094 stable 原始输入。
- round-095 付费刷新悬停计划：在三次购买填满队伍、金币为 `10` 后，先把指针从第四名队员移到刷新铃中心 `(988,754)`，单独采集 `10d_paid_refresh_hover`，要求双边仍保持付费刷新前 `shop/v5`、悬停语义为 `ROLL_SHOP`、焦点为 `none`，随后才执行既有付费刷新。新门禁覆盖不受正式 tooltip 文字遮挡的刷新铃上半部核心，核对免费/付费语境下铃铛资源、位置、裁切和悬停反馈；两边仍使用各自按钮、提示和命令实现，不复制 Mock Scene、脚本、Session、固定槽位或刷新代码。
- round-095 付费刷新悬停闭环：美术项目与正式项目在专用虚拟屏分别从各自真实路线入口完成 `62` 步并通过采集器断言；新增 `10d` 在 `(988,754)` 悬停但未点击时，双边均保持 `shop, coins=10, offers=3, roster=4, v5/3441b30e2878a924`，悬停语义为 `ROLL_SHOP`、焦点为 `none`；随后现有付费刷新才进入红帘。刷新铃上半部核心 `(954,712)-(1034,765)` 跨边 `diff=0`，且美术侧、正式侧各自从免费悬停到付费悬停的同一核心也均为 `diff=0`。正式项目显示“刷新商店（2 金币）” tooltip，美术项目无该可访问性文字，属既知产品反馈差异，不是资源错位，因此不删除正式提示、也不向 Mock 复制其实现。当前并排证据为 `output/validation/shop-art-roundtrip/round-095-probe/comparison/10d_paid_refresh_hover_art_vs_formal.png`、`11_paid_refresh_curtain_art_vs_formal.png`、`12_paid_refresh_complete_art_vs_formal.png` 和 `round095_probe_62_step_overview.png`。全新 probe 的新门禁通过，其余只复现旧刷新铃边缘、关闭宝箱和第二队员阴影的跨次采样波动，没有放宽阈值；最终稳定审计沿用 round-094 已验收且未变的 `61` 个输入并加入当前 `10d` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`112` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-095/geometry_audit.json`。`61` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围双仓 `git diff --check` 通过，用户 Godot PID `5112` 全程保留。本轮没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或刷新实现。磁盘清理只删除 round-094 stable 与 round-095 probe 的四个可重生原始 PNG 目录，逻辑大小约 `466MiB`、由于稳定输入使用 APFS clone 实际恢复约 `236MiB`；删除不可直接恢复，但保留 round-095 当前 `62` 组 comparison、probe/稳定几何报告和 round-095 stable 原始输入。
- round-096 付费刷新真实按下态计划：紧接 `10d_paid_refresh_hover`，在双边刷新铃中心 `(988,754)` 发送真实左键按下但暂不松开，单独采集 `10d2_paid_refresh_pressed`；要求按下期间仍保持付费刷新前 `shop, coins=10, offers=3, roster=4, v5/3441b30e2878a924`，悬停与焦点语义均为 `ROLL_SHOP`、按钮绘制态为 `DRAW_PRESSED`，证明 mouse-down 不会提前扣币或执行命令。截图后真实松开并继续既有付费刷新红帘流程；新像素门禁覆盖不受正式 tooltip 遮挡的刷新铃上半部核心，双边继续使用各自按钮、信号、提示和命令实现，不复制 Mock Scene、脚本、Session、固定槽位或刷新代码。
- round-096 付费刷新真实按下态闭环：美术项目与正式项目在专用虚拟屏分别从各自真实路线入口完成 `63` 步并通过采集器断言；新增 `10d2` 在 `(988,754)` 真实按下但未松开时，双边均保持 `shop, coins=10, offers=3, roster=4, v5/3441b30e2878a924`，悬停与焦点语义均为 `ROLL_SHOP`、按钮绘制态为 `DRAW_PRESSED`；真实松开后双边才进入 `coins=8, v6/dead7d0cea3d7163` 的红帘过程，证明 mouse-down 没有提前扣币或执行刷新。刷新铃上半部核心 `(954,712)-(1034,765)` 跨边 `diff=0`，美术侧、正式侧各自从付费悬停到按住的同一核心也均为 `diff=0`；当前并排证据为 `output/validation/shop-art-roundtrip/round-096-probe/comparison/10d_paid_refresh_hover_art_vs_formal.png`、`10d2_paid_refresh_pressed_art_vs_formal.png`、`11_paid_refresh_curtain_art_vs_formal.png` 和 `round096_probe_63_step_overview.png`。全新 probe 的新门禁通过，其余只复现旧关闭宝箱和第四队员阴影的跨次采样波动，没有放宽阈值；最终稳定审计沿用 round-095 已验收且未变的 `62` 个输入并加入当前 `10d2` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`113` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，位于 `output/validation/shop-art-roundtrip/round-096/geometry_audit.json`。正式侧前两次非 TTY 执行会话在生成 manifest 前被执行环境提前截断，这两组中间图已废弃；改用持续 TTY 后同一当前脚本完整生成 `63` 张、manifest `passed=true`并以退出码 `0` 结束，故归为执行会话中断而不是产品失败。`63` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围双仓 `git diff --check` 通过，用户 Godot PID `5112` 全程保留。本轮没有修改运行时代码，也没有复制或回传 Mock Scene、脚本、Session、固定槽位或刷新实现。磁盘清理保留每轮 comparison 与审计报告，只删除 round-083/084 的十个旧可重生原始 PNG 目录（逻辑约 `933MiB`），以及 round-095 stable 与 round-096 probe 的四个已由当前证据替代的可重生原始 PNG 目录（逻辑约 `472MiB`）；这些删除不可直接恢复，但保留 round-096 当前 `63` 组 comparison、总览、probe/稳定几何报告和 round-096 stable 原始输入；本轮磁盘可用空间净恢复约 `1.2GiB`。
- round-097 第一件商品真实按下并取消计划：紧接入口 `02_offer_hover`，在双边第一件商品同一 `(578,454)` 坐标发送真实左键按下但暂不松开，单独采集 `02a_entry_first_offer_pressed`；要求按下期间仍保持入口 `shop/v1`、未扣币、未售出，悬停与焦点语义均为 `BUY_OFFER/shop_001`、按钮绘制态为 `DRAW_PRESSED`。截图后保持按键按下把指针移到中立区 `(1850,1030)` 再松开，要求取消手势不触发购买且后续既有刷新、购买和退出序列不变。新门禁只核对第一货孔四边完整框线，避开正式项目功能性锁定按钮区域；两边继续使用各自按钮、信号和 Session 实现，不复制 Mock Scene、脚本、Session、固定槽位、购买或取消代码。
- round-097 第一件商品真实按下并取消闭环：美术项目与正式项目先在专用虚拟屏各自完成 `64` 步真实入口采集，新增步骤双边均保持 `shop, coins=16, offers=3, roster=1, v1/acf88e4c2972a5d2`，悬停与焦点语义均为 `BUY_OFFER/shop_001`，移出后松开也未购买；随后刷新、购买、背包与退出的既有步骤继续完成。首次并排图确认美术项目按住时撤掉货孔金框，而正式项目自己的 `ShopShelfLayout` 仍绘制 `pressed` 金框；责任层是正式独立表现样式，不是图片、公开数据或 Mock 代码。正式项目只把自己的商品 `pressed` StyleBox 改为透明无边框，同时保留独立键盘 `focus` 金框、锁定按钮和正式 `BUY_OFFER` 命令链；修复后第一货孔四边跨项目全部 `diff=0`。修复前证据在 `output/validation/shop-art-roundtrip/round-097-probe-before-fix/comparison/02a_entry_first_offer_pressed_art_vs_formal.png`，修复后证据与总览在 `round-097-probe/comparison/02a_entry_first_offer_pressed_art_vs_formal.png` 和 `round097_probe_64_step_overview.png`。全新 probe 只剩第二/第四队员阴影的既有跨进程采样波动；最终稳定审计为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`117` 项精确像素、`1` 项容差和 `9` 项效果，位于 `output/validation/shop-art-roundtrip/round-097/geometry_audit.json`。`65` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和范围内 diff 检查通过，用户 Godot PID `5112` 全程保留。没有复制或回传 Mock Scene、脚本、Session、固定槽位、购买或取消实现；五个既有 `.psd.import` 修改未覆盖、未暂存。磁盘清理只删除 round-097 修复前与修复后 probe 以及 round-096 stable 中已被并排图、总览、报告或 round-097 stable 替代的六个可重生成原始 PNG 目录，逻辑大小约 `702MiB`、实际恢复约 `340MiB`；删除不可直接恢复，但修复前后对照、64 步总览、probe/稳定审计和 round-097 stable 原始输入全部保留。
- round-098 第二件商品真实按下并取消计划：紧接入口 `02_entry_second_offer_hover`，在双边第二件商品同一 `(902,454)` 坐标发送真实左键按下但暂不松开，单独采集 `02a2_entry_second_offer_pressed`；要求按下期间继续保持入口 `shop/v1`、未扣币、未售出，悬停与焦点语义均为 `BUY_OFFER/shop_002`、按钮绘制态为 `DRAW_PRESSED`。截图后保持按键按下把指针移到中立区 `(1850,1030)` 再松开，要求取消手势不触发购买且后续第三件商品、刷新、购买、背包和退出序列不变。新门禁核对第二货孔四边完整框线，验证 round-097 的正式独立 `pressed` 样式修复覆盖所有商品按钮而非只覆盖第一格；双边继续使用各自按钮、信号和 Session 实现，不复制 Mock Scene、脚本、Session、固定槽位、购买或取消代码。
- round-098 当前诊断与写入边界：双边 `65` 步真实入口采集、状态身份和第二货孔四边均已通过，但并排图显示美术项目按住商品后会隐藏原商品，并在指针中心显示 `126×124`、透明度 `0.82` 的资源预览；正式项目仍保留 `152px` 原图。模板测量确认美术项目第二件商品从 hover 的 `(827,364)` 转为指针中心附近的小预览，而正式 hover/pressed 都保持 `(827,364)`。责任层只在正式 `ShopShelfLayout` 的按住表现；本轮加入独立的 hold feedback，只复用同步 PNG、尺寸/透明度与视觉意图，不引入拖放目标、释放计划、固定槽位、Session 或 Mock 控制器。停止条件为双边第一/第二商品按住中央资源裁剪逐像素一致、移出松开仍不购买、现有命令与键盘焦点 smoke 通过；若需要改变玩法或数据则停止。
- round-098 第二件商品按下与正式按住表现闭环：双边在专用虚拟屏从各自真实路线入口完成 `65` 步，新增 `02a2` 按住期间均保持 `shop, coins=16, offers=3, roster=1, v1/acf88e4c2972a5d2`，悬停与焦点语义为 `BUY_OFFER/shop_002`；移出后松开未购买，后续刷新、购买、背包和退出完整。首次 probe 的货孔四边虽为 `diff=0`，人工并排检查仍发现美术项目按住时用 `126×124`、`alpha=0.82` 的指针中心预览替代原图，正式项目保留 `152px` 原图。正式 `ShopShelfLayout` 现增加仅负责视觉的 `OfferHoldFeedback`：按住时临时隐藏按钮 icon、显示同一同步 PNG 并跟随指针，松开恢复；没有拖放目标、释放计划、固定槽位、Session、数据或购买语义。修复后第一/第二商品按住的四边和中央资源共 `12` 个裁剪全部逐像素 `diff=0`，键盘 focus 金框和正式 `BUY_OFFER` 链继续保留。当前并排证据在 `output/validation/shop-art-roundtrip/round-098-probe/comparison/02a_entry_first_offer_pressed_art_vs_formal.png`、`02a2_entry_second_offer_pressed_art_vs_formal.png`，65 步总览为 `round098_probe_65_step_overview.png`；修复前对照保存在 `round-098-probe-before-fix/comparison/`。最终稳定审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`125` 项精确像素、`1` 项容差和 `9` 项效果，报告为 `output/validation/shop-art-roundtrip/round-098/geometry_audit.json`。`67` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围 diff 检查通过，用户 Godot PID `5112` 保持运行；五个既有 `.psd.import` 修改未覆盖、未暂存。磁盘清理只删除 round-097 stable、round-098 修复前 probe 与修复后 probe 的六个可重生成原始 PNG 目录，逻辑约 `734MiB`、实际恢复约 `366MiB`；删除不可直接恢复，但修复前后 comparison、65 步总览、probe/稳定报告和 round-098 stable 原始输入均保留。该增量已闭环，全天持续目标继续进行。
- round-099 第三件商品真实按下并取消计划：紧接入口 `02_entry_third_offer_hover`，在斜屋檐第三货孔同一 `(1228,470)` 坐标真实按下但暂不松开，采集 `02a3_entry_third_offer_pressed`；要求双边继续保持 `shop/v1`、未扣币、未售出，悬停与焦点语义均为 `BUY_OFFER/shop_003`、绘制态为 `DRAW_PRESSED`。截图后保持按键按下移到中立区 `(1850,1030)` 再松开，要求取消不触发购买且后续序列不变。该槽的基准矩形为 `(1148,375,160,190)`、原图基线为 `(1152,394)`，与前两格不同；新门禁分别覆盖完整货孔四边及避开正式锁按钮的中央资源上下裁剪，用于证明正式独立 `OfferHoldFeedback` 在斜屋檐槽仍按同一资源、指针中心、尺寸和透明度对齐。双边继续使用各自按钮、信号和状态实现，不复制 Mock Scene、拖放控制器、Session、固定槽位、购买或取消代码。
- round-099 第三件商品真实按下并取消闭环：美术项目与正式项目在专用虚拟屏分别从各自真实路线入口完成 `66` 步并通过采集器断言；新增 `02a3` 在 `(1228,470)` 真实按住期间双边均保持 `shop, coins=16, offers=3, roster=1, v1/acf88e4c2972a5d2`，悬停与焦点语义均为 `BUY_OFFER/shop_003`、按钮绘制态为 `DRAW_PRESSED`，移出中立区松开后未购买，后续刷新、购买、背包和退出完整。人工并排检查确认斜屋檐第三槽的 `126×124` 半透明预览在两边使用同一指针中心、尺寸、透明度和资源；完整货孔四边及避开正式锁按钮的中央资源上下共 `6` 个新裁剪全部逐像素 `diff=0`，因此本轮不修改正式或美术运行时代码。当前对照和总览为 `output/validation/shop-art-roundtrip/round-099-probe/comparison/02a3_entry_third_offer_pressed_art_vs_formal.png` 与 `round099_probe_66_step_overview.png`；全新 probe 与最终 stable 均为 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`131` 项精确像素、`1` 项容差和 `9` 项效果，稳定报告为 `output/validation/shop-art-roundtrip/round-099/geometry_audit.json`。`69` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围 diff 检查通过，用户 Godot PID `5112` 保持运行；五个既有 `.psd.import` 修改未覆盖、未暂存。磁盘清理只删除 round-098 stable 与 round-099 probe 的四个可重生成原始 PNG 目录，逻辑约 `496MiB`、实际恢复约 `246MiB`；删除不可直接恢复，但当前 66 组 comparison、总览、probe/稳定报告和 round-099 stable 原始输入均保留。该增量已闭环，无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-100 关闭态宝箱真实按下并取消计划：紧接 `06d_bag_closed_hover`，在双边共享宝箱指针 `(430,892)` 真实按下但暂不松开，单独采集 `06d2_bag_closed_pressed`；要求按下期间仍保持购买后的 `shop` Snapshot、背包未打开，悬停与焦点语义均为 `TOGGLE_BAG`、按钮绘制态为 `DRAW_PRESSED`。截图后保持按键移到中立区 `(1850,1030)` 再松开，要求取消手势不打开背包，然后重新移回并执行既有真实打开操作。新门禁覆盖关闭态宝箱完整 `(313,776)-(549,1009)` 区域，用于检查按压时是否提前切换开箱资源、位移、裁切或层级错乱。双边继续使用各自按钮、信号、背包展开和状态实现，不复制 Mock Scene、Session、固定槽位或打开/取消代码。
- round-100 关闭态宝箱真实按下并取消闭环：美术项目与正式项目在专用虚拟屏分别从各自真实路线入口完成 `67` 步并通过采集器断言；新增 `06d2` 在 `(430,892)` 真实按住期间双边均保持 `shop, coins=14, offers=3, roster=2, v3/55a35ba1c657e29e`，悬停与焦点语义均为 `TOGGLE_BAG`、按钮绘制态为 `DRAW_PRESSED`；移出后松开未打开背包，重新真实点击后才进入既有打开流程。双边各自的悬停帧与按住帧整图差异像素均为 `0`，关闭宝箱完整 `(313,776)-(549,1009)` 跨项目裁剪也为 `diff=0`，不存在提前开箱、位移、裁切、层级或焦点残影，因此本轮不修改任一边运行时代码。
- round-100 证据与验证：新采集 manifest 同时记录 `expected_project_root`、绝对 `project_file` 与 `scene_source=res://art/scenes/app/game.tscn`，用于防止把旧工程或错入口截图当作当前证据。当前并排图和 `67` 步总览位于 `output/validation/shop-art-roundtrip/round-100-probe/comparison/06d2_bag_closed_pressed_art_vs_formal.png` 与 `round100_probe_67_step_overview.png`。全新 probe 的新宝箱门禁通过，余下只复现旧商人 alpha 合成及第三/第四队员阴影的跨进程抗锯齿波动；没有放宽阈值。最终稳定审计保留前 `66` 个已验收输入并加入当前 `06d2` 和 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`132` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，报告为 `output/validation/shop-art-roundtrip/round-100/geometry_audit.json`。`71` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围双仓 `git diff --check` 通过；用户 Godot PID `5112` 全程保留，五个既有 `.psd.import` 修改未覆盖、未暂存。磁盘清理只删除 round-100 probe 与 round-099 stable 的四个可重生原始 PNG 目录，删除不可直接恢复；当前 `67` 组 comparison、总览、probe/稳定报告和 round-100 stable 原始输入仍保留。本轮无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-101 打开态宝箱真实按下并取消计划：紧接 `07a2_bag_open_hover`，在双边共享宝箱指针 `(430,892)` 真实按下但暂不松开，单独采集 `07a3_bag_open_pressed`；要求按下期间仍保持购买后的 `shop` Snapshot、背包面板继续可见，悬停与焦点语义均为 `TOGGLE_BAG`、按钮绘制态为 `DRAW_PRESSED`。截图后保持按键移到中立区 `(1850,1030)` 再松开，要求取消手势不关闭背包，然后继续既有第一/第八槽位流程。新门禁同时覆盖打开态宝箱完整 `(313,776)-(549,1009)` 与背包面板 `(502,379)-(1320,818)`，用于检查按压时是否提前切换关箱资源、位移、裁切、面板消失或层级错乱。双边继续使用各自唯一宝箱按钮、信号、背包展开和状态实现，不复制 Mock Scene、Session、固定槽位或关闭/取消代码，不改动节点拓扑。
- round-101 打开态宝箱真实按下并取消闭环：美术项目与正式项目在专用 `1920×1080`、`Mirror Off` 虚拟屏分别从各自真实路线入口完成 `68` 步并通过采集器断言；新增 `07a3` 在 `(430,892)` 真实按住期间双边均保持 `shop, coins=14, offers=3, roster=2, v3/55a35ba1c657e29e`，悬停与焦点语义均为 `TOGGLE_BAG`、按钮绘制态为 `DRAW_PRESSED`，背包面板持续可见；移出后松开未关闭背包，后续第一/第八槽位、购买、刷新与退出序列完整。双边各自的重新悬停帧与按住帧整图差异像素均为 `0`，打开宝箱完整 `(313,776)-(549,1009)` 与背包面板 `(502,379)-(1320,818)` 两个跨项目裁剪也均为 `diff=0`，不存在提前关箱、换图、位移、裁切、面板消失、层级或焦点残影，因此本轮不修改任一边运行时代码。
- round-101 证据与验证：当前并排图和 `68` 步总览位于 `output/validation/shop-art-roundtrip/round-101-probe/comparison/07a3_bag_open_pressed_art_vs_formal.png` 与 `round101_probe_68_step_overview.png`。全新 probe 的两个新门禁通过，其他只复现旧刷新铃右/下边、关闭宝箱、商人 alpha 合成和第三/第四队员阴影的跨进程抗锯齿波动，没有放宽阈值。最终稳定审计保留前 `67` 个已验收输入并加入当前 `07a3` 和 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`134` 项精确像素、`1` 项容差和 `9` 项效果门禁全部通过，报告为 `output/validation/shop-art-roundtrip/round-101/geometry_audit.json`。`73` 项 Python 单测、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和本任务范围双仓 `git diff --check` 通过。正式隔离副本的四个商店运行时文件与当前工作树 SHA-256 逐文件相同，完整 fast 在 `/private/tmp/codex-shop-formal-highlight-0816.j5vRvl/formal/output/validation/qa/20260816-184346-fast/` 为 `19/19`、0 失败、`347.162s`，正式可玩流 `171.609s`、battle UI `15.664s`、seed bot `103.45s`，因此关闭了“最新货架修复未被完整 fast 覆盖”的旧验证缺口。用户 Godot PID `5112` 全程保留，五个既有 `.psd.import` 修改未覆盖、未暂存。磁盘清理保留每轮 comparison/审计报告，删除 round-061 至 round-082 的旧可重生原始 PNG 目录（约 `2.90GiB`），以及 round-100 stable 与 round-101 probe 的四个已被当前证据替代的原始 PNG 目录；这些删除不可直接恢复，但当前 `68` 组 comparison、总览、probe/稳定报告和 round-101 stable 原始输入仍保留。本轮无节点拓扑变更、无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-102 退出牌按下后移出取消计划：紧接 `17b_exit_pressed`，保持左键按下把双边指针从退出牌中心 `(1544,726)` 移到中立区 `(1850,1030)` 后松开，单独采集 `17c_exit_press_cancelled`；要求商店继续可见、公开 Snapshot 仍为 `shop/v7`、悬停与焦点语义均清空、退出牌恢复 `DRAW_NORMAL`。随后重新真实点击退出牌并继续既有 `18_exit_to_route`。新门禁覆盖完整退出牌 `(1410,490)-(1679,962)`，用于检查取消后是否残留按压/悬停纹理、位移、裁切或错误退出；双边继续使用各自按钮、信号和路由实现，不复制 Mock Scene、Session、固定槽位或退出/取消代码，不改节点拓扑。
- round-102 退出牌按下后移出取消闭环：美术项目与正式项目在专用 `1920×1080`、`Mirror Off` 虚拟屏分别从各自真实路线入口完成 `69` 步并通过采集器断言；新增 `17c` 在按住退出牌后移到 `(1850,1030)` 松开，双边都继续保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，悬停/焦点语义均为 `none`、退出牌恢复 `DRAW_NORMAL`，随后重新点击才进入 `route/v8`。本轮新图的完整退出牌 `(1410,490)-(1679,962)` 跨边 `diff=0`；双方从退出悬停/按住到取消均产生同一 `9850` 个退出纹理恢复像素，未发现按压/悬停残影、位移、裁切、错误退出或焦点残留，因此不修改任一边运行时代码。
- round-102 证据与验证：当前新步骤对照和 `69` 步总览位于 `output/validation/shop-art-roundtrip/round-102-probe/comparison/17c_exit_press_cancelled_art_vs_formal.png` 与 `round102_probe_69_step_overview.png`；双边当前 manifest 都记录各自隔离 `project_root`、绝对 `project_file`、正式入口 Scene 和 `passed=true`。全新 probe 的新增门禁通过，只复现旧第一商品悬停框、刷新铃边缘与商人 alpha 合成的跨进程抗锯齿波动，没有放宽阈值；最终稳定审计保留 round-101 已验收的 `68` 个输入并加入当前 `17c` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`135` 项精确像素、`1` 项容差和 `9` 项效果门禁，报告为 `output/validation/shop-art-roundtrip/round-102/geometry_audit.json`。`75` 项 Python 单测、旧 round-101 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和范围内 diff 检查均通过；正式运行时代码未变，继续沿用 round-101 同一运行时文件已通过的完整 fast `19/19` 证据。用户 Godot PID `5112` 保持运行，五个既有 `.psd.import` 修改未覆盖、未暂存。本轮无节点拓扑变更、无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-103 退出牌按住移出中间态计划：紧接 `17b_exit_pressed`，保持左键按下把双边指针从退出牌中心 `(1544,726)` 移到中立区 `(1850,1030)`，暂不松开并单独采集 `17b2_exit_pressed_pointer_outside`；要求商店继续可见、公开 Snapshot 仍为 `shop/v7`、退出牌已恢复 `DRAW_NORMAL`、悬停语义清空但按压取得的 `EXIT_SHOP` 焦点仍保留。截图后才在中立区松开并继续既有 `17c` 取消与重新点击退出流程。新门禁覆盖完整退出牌 `(1410,490)-(1679,962)`，用于区分“按住移出”与“移出后松开”两个真实阶段并检查提前退出、按压残影、位移、裁切或层级错误；双边继续使用各自按钮、信号和路由实现，不复制 Mock Scene、Session、固定槽位或退出/取消代码，不改节点拓扑。若两边焦点生命周期并不一致或截图确认运行时错位，则先保存失败证据并只修真正责任层。
- round-103 退出牌按住移出中间态闭环：美术项目与正式项目在专用 `1920×1080`、`Mirror Off` 虚拟屏分别从各自正式入口完成 `70` 步并通过采集器断言；新增 `17b2` 在左键仍按住时把指针移到 `(1850,1030)`，双边都继续保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，退出牌恢复 `DRAW_NORMAL`、悬停语义为 `none`，但焦点仍为 `EXIT_SHOP`。随后松开才清除焦点，商店仍未退出，重新点击后才进入 `route/v8`。按住移出相对按住牌面在两侧各产生同一 `9850` 个恢复像素，按住移出与松开取消的完整退出牌均为同侧 `diff=0`，新增完整退出牌跨项目裁剪也为 `diff=0`；未发现提前退出、按压/悬停残影、位移、裁切、层级或焦点生命周期不一致，因此不修改任一边运行时代码。
- round-103 证据与验证：当前新步骤对照和 `70` 步总览位于 `output/validation/shop-art-roundtrip/round-103-probe/comparison/17b2_exit_pressed_pointer_outside_art_vs_formal.png` 与 `round103_probe_70_step_overview.png`。全新 probe 的新增门禁通过，余下只复现既有第一商品悬停框、刷新铃边缘、关闭宝箱、商人 alpha 合成和队员阴影的跨进程抗锯齿波动，没有放宽阈值；最终稳定审计保留 round-102 已验收输入并加入当前 `17b2` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`136` 项精确像素、`1` 项容差和 `9` 项效果门禁，报告为 `output/validation/shop-art-roundtrip/round-103/geometry_audit.json`。`77` 项 Python 单测、旧 round-102 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁和范围内 diff 检查均通过；正式运行时代码未变，继续沿用 round-101 同一运行时文件通过的完整 fast `19/19` 证据。用户 Godot PID `5112` 保持运行，五个既有 `.psd.import` 修改未覆盖、未暂存。本轮无节点拓扑变更、无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-104 退出牌按住移出后重新移入计划：紧接 `17b2_exit_pressed_pointer_outside`，在左键仍保持按下时把双边指针从中立区 `(1850,1030)` 重新移回退出牌中心 `(1544,726)`，单独采集 `17b3_exit_pressed_pointer_reentered`；要求商店继续可见、公开 Snapshot 仍为 `shop/v7`、退出牌重新进入 `DRAW_PRESSED`，悬停与焦点语义都为 `EXIT_SHOP`。截图后再次移出到中立区并松开，继续既有 `17c` 取消和重新点击退出流程。新门禁覆盖完整退出牌 `(1410,490)-(1679,962)`，用于检查重新进入时是否恢复错误纹理、位移、裁切、层级或意外提交退出；双边继续使用各自按钮、信号和路由实现，不复制 Mock Scene、Session、固定槽位或退出/取消代码，不改节点拓扑。若截图确认两边行为不同，则保留失败证据并只修真正责任层。
- round-104 当前失败证据与责任层：首次双边 `71` 步采集器状态断言均通过，但新并排图在正式项目退出牌箭头右上方出现“离开商店” Tooltip，美术参考在同一按住重新移入状态没有该浮层，完整退出牌跨边差异为 `1667` 像素；同侧复核显示美术项目重新移入与最初按住整牌 `diff=0`，正式项目对应差异也恰为 `1667`，故不是资源位置、纹理或按钮绘制态错误，而是正式 `ShopSharedRail` 在活动按压往返期间重新弹出 Tooltip。失败图保存在 `output/validation/shop-art-roundtrip/round-104-probe-before-fix/comparison/17b3_exit_pressed_pointer_reentered_art_vs_formal.png`。候选只在正式独立底栏表现模块的 `button_down/button_up` 生命周期内临时清空并恢复 Tooltip，`accessibility_name` 始终保留“离开商店”；不修改美术项目、公开 Snapshot、Session、路由、退出语义、节点拓扑或共享 PNG。
- round-104 退出牌按住移出后重新移入闭环：正式 `ShopSharedRail` 已在退出按钮按下时临时清空可见 Tooltip、松开时恢复文字，同时始终保留 `accessibility_name="离开商店"`；美术项目运行时代码未修改。双边在专用 `1920×1080`、`Mirror Off` 虚拟屏分别从各自真实入口完成 `71` 步，新增 `17b3` 在左键仍按住时回到 `(1544,726)`，双方均保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，悬停/焦点为 `EXIT_SHOP`、绘制态为 `DRAW_PRESSED`；修复后完整退出牌跨项目差异从 `1667` 降为 `0`。同侧时序也完全一致：`17b` 按住到 `17b2` 移出两边各变化 `9850` 像素，`17b` 与 `17b3` 重新移入两边各为 `diff=0`，`17b2` 与 `17c` 松开取消两边各为 `diff=0`；随后重新点击才进入 `route/v8`，没有提前退出、错位、裁切、层级或焦点残影。
- round-104 证据与验证：修复前失败对照保存在 `output/validation/shop-art-roundtrip/round-104-probe-before-fix/comparison/17b3_exit_pressed_pointer_reentered_art_vs_formal.png`；修复后当前对照和 `71` 步总览为 `output/validation/shop-art-roundtrip/round-104-probe/comparison/17b3_exit_pressed_pointer_reentered_art_vs_formal.png` 与 `round104_probe_71_step_overview.png`。当前 probe 的新退出牌门禁通过，其余只复现旧第一商品悬停边缘、刷新铃边缘、商人 alpha 合成与队员阴影的跨进程抗锯齿波动，没有放宽阈值；最终稳定审计沿用前 `70` 个已验收输入并加入当前 `17b3` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`137` 项精确像素、`1` 项容差和 `9` 项效果门禁，报告为 `output/validation/shop-art-roundtrip/round-104/geometry_audit.json`。最终 `86` 项 Python 发现测试、旧 round-103 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁均通过；包含当前修复文件的隔离正式副本完整 fast 为 `19/19`、0 失败、`349.261s`，报告在 `/private/tmp/codex-shop-formal-highlight-0816.j5vRvl/formal/output/validation/qa/20260816-194933-fast/`。用户 Godot PID `5112` 全程保留，五个既有 `.psd.import` 与巡检任务 WIP 均未覆盖、未暂存。本轮无节点拓扑、公开数据、Session、路由、共享资源或动画批准变更，全天持续目标继续进行。
- round-105 退出牌重新移入后再次移出中间态计划：紧接 `17b3_exit_pressed_pointer_reentered`，在左键仍保持按下时把双边指针再次从退出牌中心 `(1544,726)` 移到中立区 `(1850,1030)`，单独采集 `17b4_exit_pressed_pointer_outside_again`；要求商店继续可见、公开 Snapshot 仍为 `shop/v7`、退出牌恢复 `DRAW_NORMAL`、悬停语义清空但 `EXIT_SHOP` 焦点继续保留。截图后才在同一中立区松开并继续既有 `17c` 取消与重新点击退出流程。新门禁覆盖完整退出牌 `(1410,490)-(1679,962)`，用于检查上一轮修复的 Tooltip 生命周期在第二次移出时是否留下浮层、按压纹理、位移、裁切、层级或错误退出；双边继续使用各自按钮、信号和路由实现，不复制 Mock Scene、Session、固定槽位或退出/取消代码，不改节点拓扑。若截图确认两边行为不同，则先保留失败证据并只修真正责任层。
- round-105 退出牌重新移入后再次移出闭环：美术项目与正式项目在当前专用 `1920×1080`、`Mirror Off` 虚拟屏分别从各自真实入口完成 `72` 步并通过采集器断言；新增 `17b4` 在左键仍按住时第二次移到 `(1850,1030)`，双边均保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，退出牌为 `DRAW_NORMAL`、悬停语义 `none`、焦点 `EXIT_SHOP`。人工并排检查没有 Tooltip、按压纹理、位移、裁切、层级或错误退出残留；完整退出牌跨项目 `diff=0`，双边各自第一次移出与第二次移出均为 `diff=0`，重新移入到第二次移出均恰好恢复同样的 `9850` 个像素，随后松开取消并重新点击才进入 `route/v8`。因此本轮没有修改任一边运行时代码，也没有把美术项目实现回传正式项目。
- round-105 证据与验证：当前第 `72` 张并排图为 `output/validation/shop-art-roundtrip/round-105-probe/comparison/17b4_exit_pressed_pointer_outside_again_art_vs_formal.png`，完整总览为 `round105_probe_72_step_overview.png`。全新 probe 的新增退出牌门禁通过，其他只复现旧第一商品悬停边缘、刷新铃边缘、商人 alpha 合成和第二/第三队员阴影的跨进程抗锯齿波动，没有放宽阈值；最终稳定审计沿用 round-104 已验收输入并加入当前 `17b4` 与 manifest，结果为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`138` 项精确像素、`1` 项容差和 `9` 项效果门禁，报告为 `output/validation/shop-art-roundtrip/round-105/geometry_audit.json`。最终 `88` 项 Python 发现测试、旧 round-104 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画准入门禁均通过；当前正式运行时文件与刚通过完整 fast `19/19` 的隔离副本 SHA-256 相同，因此沿用 `/private/tmp/codex-shop-formal-highlight-0816.j5vRvl/formal/output/validation/qa/20260816-194933-fast/` 的 `349.261s` 证据。用户 Godot PID `5112` 和专用虚拟屏保持运行，五个既有 `.psd.import` 与巡检任务 WIP 均未覆盖、未暂存。本轮无节点拓扑、公开数据、Session、路由、共享资源或动画批准变更，全天持续目标继续进行。
- round-106 取消后再次按住退出牌计划：紧接 `17c_exit_press_cancelled`，双边把指针重新移回退出牌可见中心 `(1544,726)`，再次发送真实左键按下但暂不松开，单独采集 `17d_exit_repressed_after_cancel`；要求第二次按压期间仍保持退出前 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，悬停与焦点语义均为 `EXIT_SHOP`、按钮绘制态为 `DRAW_PRESSED`。截图后才在同一位置松开并继续既有 `18_exit_to_route`，验证前一轮移出取消不会污染下一次完整按压周期；新像素门禁覆盖完整退出牌，双边继续使用各自按钮、信号、Session 与路由实现，不复制 Mock Scene、脚本、Session、固定槽位或退出代码。
- round-106 取消后再次按住退出牌闭环：美术项目与正式项目在专用虚拟屏分别从各自真实入口完成 `73` 步，采集器均以退出码 `0` 输出通过；新增 `17d` 在第一次按压移出并松开取消后，再于 `(1544,726)` 真实按住退出牌，两边都保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，悬停/焦点均为 `EXIT_SHOP`、按钮绘制态为 `DRAW_PRESSED`，同位置松开后双方才进入 `route, v8/a308231242f7c201`。人工并排检查确认完整退出牌没有 Tooltip、按压位移、裁切、层级或状态残留；第 `17d` 跨项目 `diff=0`，两边各自首次按住 `17b` 与取消后再次按住 `17d` 也均为 `diff=0`，从取消态 `17c` 到再次按住均恰好变化 `9850` 个像素。因此没有新的运行时错位，本轮不修改正式或美术运行时代码，也不回传 Mock 实现。
- round-106 证据与验证：新增第 `72` 步并排图为 `output/validation/shop-art-roundtrip/round-106-probe/comparison/17d_exit_repressed_after_cancel_art_vs_formal.png`，随后退出回路线为第 `73` 步；73 步总览为 `output/validation/shop-art-roundtrip/round-106/round106_73_step_overview.png`。全新 probe 的新增退出门禁直接通过，其他只复现旧刷新铃边缘、关闭宝箱悬停、商人 alpha 合成与第二/第三队员阴影的跨进程采样波动，没有放宽阈值；最终稳定审计为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`139` 项精确像素、`1` 项容差和 `9` 项效果，报告在 `output/validation/shop-art-roundtrip/round-106/geometry_audit.json`。最终 `90` 项 Python 发现测试、旧 round-105 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画准入门禁均通过。用户 Godot PID `5112` 继续保留；本轮为恢复磁盘空间，只删除 round-106 probe 中已由当前并排图、总览、审计与稳定原始输入替代的两组可重生成原始 PNG 目录，共约 `316MiB`，不可直接恢复。五个既有 `.psd.import` 与巡检任务 WIP 均未覆盖、未暂存；无节点拓扑、公开数据、Session、路由、共享资源或动画批准变更，全天持续目标继续进行。
- round-107 取消后第二次按住再移出计划：紧接 `17d_exit_repressed_after_cancel`，保持第二次左键按下并把双边指针从退出牌中心 `(1544,726)` 移到中立区 `(1850,1030)`，单独采集 `17e_exit_repressed_pointer_outside`；要求双边继续保持退出前 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，退出牌恢复 `DRAW_NORMAL`、悬停语义清空但 `EXIT_SHOP` 焦点继续保留。截图后才把仍按住的指针重新移回退出牌并松开，继续既有真实 `18_exit_to_route`；新像素门禁覆盖完整退出牌，用于验证第一次取消后新建的第二次按压捕获状态能够独立移出、恢复且不提前退出。双边继续使用各自按钮、信号、Session 与路由实现，不复制 Mock Scene、脚本、Session、固定槽位或退出代码。
- round-107 取消后第二次按住再移出闭环：美术项目与正式项目在专用虚拟屏分别从真实入口完成 `74` 步并由采集器断言通过；新增 `17e` 在第二次左键仍按住时移到 `(1850,1030)`，双边均保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，退出牌为 `DRAW_NORMAL`、悬停 `none`、焦点 `EXIT_SHOP`。人工并排检查没有 Tooltip、按压纹理、位移、裁切、层级或状态残留；完整退出牌跨项目 `diff=0`，两边各自本轮 `17e` 与先前两次按住移出 `17b2/17b4` 均为 `diff=0`，从第二次按住 `17d` 到移出 `17e` 两侧各恢复完全相同的 `9850` 个像素。随后仍按住的指针重新移入并松开才进入 `route/v8`，没有提前退出，因此本轮不修改任一边运行时代码，也不回传 Mock 实现。
- round-107 证据与验证：新增第 `73` 步并排图为 `output/validation/shop-art-roundtrip/round-107-probe/comparison/17e_exit_repressed_pointer_outside_art_vs_formal.png`，随后退出回路线为第 `74` 步；74 步总览为 `output/validation/shop-art-roundtrip/round-107/round107_74_step_overview.png`。全新 probe 的新增退出牌门禁直接通过，其他只复现旧刷新铃边缘、商人 alpha 合成与第二队员阴影的跨进程采样波动，没有放宽阈值；最终稳定审计为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`140` 项精确像素、`1` 项容差和 `9` 项效果，报告在 `output/validation/shop-art-roundtrip/round-107/geometry_audit.json`。最终 `92` 项 Python 发现测试、旧 round-106 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画准入门禁均通过；当前正式运行时继续由相同文件的完整 fast `19/19` 覆盖。用户 Godot PID `5112` 与专用 `1920×1080` 虚拟屏保留。本轮为恢复磁盘空间，只删除 round-105 probe 与 round-107 probe 中已被当前对照、总览、审计及稳定原始输入替代的四组可重生成原始 PNG 目录，逻辑大小约 `631MiB`，不可直接恢复；五个既有 `.psd.import` 与巡检 WIP 均未覆盖、未暂存。无节点拓扑、公开数据、Session、路由、共享资源或动画批准变更，全天持续目标继续进行。
- round-108 取消后第二次按住移出再重新移入计划：紧接 `17e_exit_repressed_pointer_outside`，在第二次左键仍保持按下时把双边指针从中立区 `(1850,1030)` 重新移回退出牌中心 `(1544,726)`，单独采集 `17f_exit_repressed_pointer_reentered`；要求双边继续保持退出前 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，退出牌恢复 `DRAW_PRESSED`、悬停与焦点语义均为 `EXIT_SHOP`，且活动按压期间不得重新弹出 Tooltip。截图后才在同一位置松开并继续真实 `18_exit_to_route`；新像素门禁覆盖完整退出牌，用于验证第一次取消后的第二次按压周期在移出再移入时仍能恢复正确纹理、焦点和提示生命周期且不提前退出。双边继续使用各自按钮、信号、Session 与路由实现，不复制 Mock Scene、脚本、Session、固定槽位或退出代码，不改节点拓扑；若真实并排图确认差异，则保留失败证据并只修真正责任层。
- round-108 取消后第二次按住移出再重新移入闭环：美术项目与正式项目先在各自隔离副本完成缺失资源的完整 Godot 导入，再在专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏从真实入口完成 `75` 步并由采集器断言通过；新增 `17f` 在第二次左键仍按住时从 `(1850,1030)` 重新进入退出牌中心 `(1544,726)`，双边均保持 `shop, coins=6, offers=3, roster=5, v7/5c8b1bed15535fbc`，退出牌为 `DRAW_PRESSED`、悬停/焦点均为 `EXIT_SHOP`，没有重新出现 Tooltip。完整退出牌跨项目 `diff=0`，双边各自 `17f` 与第二次初始按住 `17d` 均为 `diff=0`，从移出 `17e` 到重新移入两侧各变化同样 `9850` 个像素；同位置松开后才进入 `route/v8/a308231242f7c201`。人工检查并排图未见按压位移、裁切、层级、状态残留或提前退出，因此本轮没有修改任一边运行时代码、资源、节点拓扑、公开数据、Session 或路由，也没有回传 Mock 实现。
- round-108 证据与验证：新增第 `74` 步并排图为 `output/validation/shop-art-roundtrip/round-108/comparison/17f_exit_repressed_pointer_reentered_art_vs_formal.png`，随后真实退出为第 `75` 步；完整总览为 `output/validation/shop-art-roundtrip/round-108/round108_75_step_overview.png`。全新 probe 的新增门禁直接通过，只复现既有首商品悬停框、刷新铃边缘、关闭宝箱、商人 alpha 与第二/第四队员阴影的跨进程采样波动，没有放宽阈值；最终稳定审计为 `passed=true`、`errors=[]`、`33` 项资源、`2` 项层级、`141` 项精确像素、`1` 项容差和 `9` 项效果。`94` 项 Python 发现测试、旧 round-107 缺新步骤的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画准入门禁均通过；正式四个运行时商店文件与最近完整 fast 隔离副本 SHA-256 全部一致，继续由 `19/19`、0 失败证据覆盖。用户 Godot PID `5112` 保持运行；为恢复磁盘空间，本轮删除 round-101 至 round-106 已被最新稳定证据替代的原始双边截图、round-066/070-082/100-104 的旧绿色 probe 目录、round-108 probe 原始双边截图和本轮隔离副本，逻辑大小约 `38.4GiB`，不可直接恢复；保留 round-104 修复前失败证据、round-108 全部 comparison、稳定原始输入、总览和审计报告。五个既有 `.psd.import` 与巡检 WIP 均未覆盖、未暂存；无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-109 退出后重新进入商店计划：`18_exit_to_route` 继续只作为“双方均回到各自路线且公开 Snapshot 一致”的相邻状态证据；两仓路线 Scene 本来就是各自独立表现，现有整屏视觉不同不得误报为商店像素一致，也不得据此复制美术项目三选 Scene、脚本或固定卡位到正式项目。新增真实操作 `19_reenter_shop_after_exit`：双方从各自路线通过自身 `CHOOSE_ROUTE/node_shop_basic` 及必要确认控件重新进入商店，移动到同一中立指针并清除焦点后单独截图；要求公开状态、商品、金币、队伍、售出位和背包结果继续一致，商店共享资源、五孔货架、商人、三件当前商品与底栏几何继续通过既有精确门禁。若同一 Session 不允许重入已退出商店，则保留失败证据并只修正式契约或 Mock 离线适配的真实责任层，不伪造第二份商店或绕过公开命令链。
- round-109 数据出库缺口与资源闭包：首次美术复拍在第 `75` 步结束，第二次 `CHOOSE_ROUTE` 被 Mock 以 `SHOP_CAPTURE_COMPLETE` 拒绝；根因是正式公开导出只记录到 `EXIT_SHOP`，不是美术 Scene 或运行时代码错位。正式 `LocalGameSession` 导出链现真实追加 `reenter_shop_after_exit -> reentered_snapshot`，得到 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，同步投影和 Mock smoke 都要求完整 `9` 个操作/快照。新快照出现尚无批准图映射的 `pal_342`；正式运行时实际显示统一缺图替代图，因此同步工具复制同一 `pal_042` 切片到有界 `pal_342.png` 并在 manifest 标记 `missing_mapping_fallback=true`，没有伪造新宠物美术或绕过正式显示事实。本轮 outbound 为 `16` 只宠物图 + `1` 张商人图，inbound 白名单 `15` 张 PNG 检查为 `changed=0, code=0`。
- round-109 复拍闭环：美术项目和正式项目在各自隔离副本、独立 `.godot`、`user://`、进程与专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏完成 `76` 步。第 `75` 步路线图只验证双方 `route, coins=6, offers=3, roster=5, v8/a308231242f7c201` 和自身路由可继续，不声明路线像素一致；第 `76` 步双方经各自真实路线重进商店后状态身份逐字一致，新三件商品、宝箱、金币牌、退出牌共 `6` 个完整裁剪全部 `diff=0`。当前全新 probe 无采样失败，几何审计直接为 `passed=true, errors=[]`，覆盖 `33` 项资源、`2` 项层级、`147` 项精确像素、`1` 项容差和 `9` 项效果；`76` 组比较均通过，证据在 `output/validation/shop-art-roundtrip/round-109/`。四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁、旧 `75` 步负门禁与 `97` 项 Python 发现测试通过。额外运行的 Mock `smoke_shop_scene.gd`、`smoke_shop_session_projection.gd` 仍复现既有“测试选择第一个 `node_shop_skill_day1`，离线捕获首操作固定为 `node_shop_basic`”失败；Git HEAD 的旧 `8` 操作捕获已具备同一首操作，不是本轮第 `9` 操作造成，也不属于本轮同步验收门禁。用户 Godot PID `5112`、五个既有 `.psd.import` 和巡检 WIP 均保留。本轮没有复制 Mock Scene、脚本、Session、固定槽位或路由实现，也没有节点拓扑变更；全天持续目标继续进行。
- round-110 重进商店后第二件商品悬停计划：紧接 `19_reenter_shop_after_exit`，双边把指针从中立区移动到第二件商品同一坐标 `(902,454)`，清除键盘焦点并单独采集 `19a_reentered_second_offer_hover`；要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，悬停语义为 `BUY_OFFER/shop_002`、焦点为 `none`。新像素门禁覆盖第二货孔上部资源区及不受正式 Tooltip 覆盖的四周框线，重点验证 round-109 由正式显示事实同步的 `pal_342` 缺图替代资源在重进后的悬停缩放、裁切与层级；正式项目既有目标徽记和宠物信息 Tooltip 属自身功能反馈，不向美术 Mock 复制，也不误报为共享资源错位。不生成新美术，也不复制 Mock Scene、脚本、Session、固定槽位或按钮代码；若真实并排图发现共享区域差异，只修实际责任层。
- round-110 重进后第二件商品悬停闭环：首次美术副本因同步 PNG 的 Godot 导入产物尚未完整生成而输出缺图错误，该组截图判定无效；完成隔离 `--import` 后重新实拍，双边采集器均以退出码 `0` 完成 `77` 步。新增第 `77` 步两边都保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，指针 `(902,454)`、悬停 `BUY_OFFER/shop_002`、焦点 `none`。初始整块货孔门禁准确记录 `6415` 个差异像素，人工并排确认全部来自正式项目自身目标徽记与宠物信息 Tooltip；中立重进帧同一完整货孔仍为 `diff=0`，而悬停帧不受浮层覆盖的宠物上部、主体上半、顶/左/右上/底框共 `6` 个区域也全部 `diff=0`，因此没有共享 PNG、缩放、位置、裁切、货孔框线或层级错位。正式功能反馈保留，美术项目不复制其实现；没有修改任一边运行时代码、资源、Scene、Session、固定槽位或节点拓扑。
- round-110 证据与验证：当前逐操作并排图为 `output/validation/shop-art-roundtrip/round-110/comparison/19a_reentered_second_offer_hover_art_vs_formal.png`，完整流程总览为 `output/validation/shop-art-roundtrip/round-110/round110_77_step_overview.png`；几何报告 `passed=true, errors=[]`，覆盖 `33` 项资源、`2` 项层级、`153` 项精确像素、`1` 项容差和 `9` 项效果。`99` 项 Python 发现测试、旧 round-109 缺第 77 步的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画批准门禁通过。一次人为设置正式仓为可选“源码完全镜像源”的检查按预期报告两项目 Scene/README 不同；这不是独立美术项目验收条件，正式运行只复用公开数据与图片、不得与 Mock 代码完全镜像。用户 Godot PID `5112` 与专用虚拟屏保留，五个既有 `.psd.import` 及巡检 WIP 未覆盖、未暂存；无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-111 重进后第二件商品按住与取消计划：紧接 `19a_reentered_second_offer_hover`，双边在同一 `(902,454)` 发送真实左键按下但暂不松开，单独采集 `19b_reentered_second_offer_pressed`；要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，悬停与焦点均为 `BUY_OFFER/shop_002`、按钮为 `DRAW_PRESSED`，并检查重建后的按住预览仍隐藏原 `152px` 商品、以同源 PNG 在指针中心显示 `126×124`、alpha `0.82`。随后保持按下移到中立区 `(1850,1030)` 并松开，清除焦点后单独采集 `19c_reentered_second_offer_press_cancelled`；要求 Snapshot 不变、悬停/焦点均清空、没有购买且货孔恢复普通资源。新门禁分别复用首次第二商品按住的货孔四边/中央预览安全裁剪，并对取消后的完整第二货孔做精确比较；两仓继续使用各自 Scene、按钮、信号、Session 和取消实现，只核对共享图片与视觉意图，不复制 Mock 代码或节点拓扑。若当前真实并排图确认差异，先保留失败证据并只修真正责任层。
- round-111 首次探针确认的 Tooltip 残留：双边新增 79 步状态/交互均通过，按住帧货孔四边和中央预览 `6` 个裁剪全部 `diff=0`，取消帧完整第二货孔也为 `diff=0`；但人工并排图确认正式项目在指针已移到 `(1850,1030)`、悬停/焦点均为 `none` 后，于右下角重新显示刚才商品的说明 Tooltip，美术项目没有。该差异不是 PNG、位置、Snapshot 或 Mock 代码问题，而是正式 `ShopShelfLayout` 在按钮捕获期间保留 `tooltip_text`，松开后 Godot 把旧提示重新定位到当前指针。修复只在正式商品 button-down 生命周期暂存并清空视觉 Tooltip、button-up 恢复，同时保留独立 `accessibility_name`；不修改购买、取消、焦点、Session、资源或节点拓扑。修复前证据保留在 `output/validation/shop-art-roundtrip/round-111-probe/comparison/19c_reentered_second_offer_press_cancelled_art_vs_formal.png`。
- round-111 第一次修复复拍仍失败：button-down 清空、button-up 立即恢复 `tooltip_text` 后，Godot 仍把按压前已打开的 Tooltip 重新定位到按钮捕获结束时的右下指针；`19c` 继续可见同一残留。修复策略收紧为：若 button-up 时指针已在商品外，仅恢复图标并保持视觉 Tooltip 为空，等该商品下一次真实 `mouse_entered` 再一次性恢复文本；无障碍标签始终保留。这样取消态不会重新弹旧提示，未来重新悬停仍可读取商品信息，也不接管命令或输入命中。
- round-111 修复后闭环：正式 `ShopShelfLayout` 仅在自身商品按住生命周期暂存图标与 Tooltip；移出松开时恢复图标、延迟到下一次真实移入才恢复视觉 Tooltip，`accessibility_name` 始终保留。第三次正式复拍与同轮美术采集均完成 `79` 步、`passed=true`；新增 `19b/19c` 都保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，按住时双方为 `BUY_OFFER/shop_002` 悬停/焦点，取消后双方悬停/焦点均为 `none` 且没有购买。人工并排确认修复后右下 Tooltip 残留消失，商品图、货孔、按住预览、队伍和底栏无新位移、裁切、遮挡或层级问题；首次入口空槽的单次命中误报已作为无效采集隔离，不用于当前证据。
- round-111 证据与验证：修复前证据为 `output/validation/shop-art-roundtrip/round-111-before-fix/19c_reentered_second_offer_press_cancelled_art_vs_formal.png`；修复后逐操作图为 `output/validation/shop-art-roundtrip/round-111/comparison/19b_reentered_second_offer_pressed_art_vs_formal.png` 与 `19c_reentered_second_offer_press_cancelled_art_vs_formal.png`，完整总览为 `round111_79_step_overview.png`。稳定审计为 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`160` 项精确像素、`1` 项容差和 `9` 项效果；按住 `6` 个共享裁剪与取消完整货孔均为 `diff=0`。最终 `101` 项 Python 发现测试、缺少 `19b/19c` 的旧矩阵负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入门禁通过；包含当前运行文件的隔离 fast 为 `19/19`、0 失败、`350.565s`，完整玩家流程 `173.985s`、seed bot `103.211s`、battle UI `15.889s`。用户 Godot PID `5112` 与专用虚拟屏保留，五个既有 `.psd.import` 和巡检 WIP 未覆盖、未暂存；没有复制 Mock Scene、脚本、Session、固定槽位或按钮实现，没有节点拓扑、玩法、正式数据、资源或动画批准变更，全天持续目标继续进行。
- round-112 取消后重新悬停计划：紧接 `19c_reentered_second_offer_press_cancelled`，双边把指针从中立区 `(1850,1030)` 真实移回第二件商品 `(902,454)`，释放键盘焦点并单独采集 `19d_reentered_second_offer_rehover_after_cancel`；要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，悬停语义恢复为 `BUY_OFFER/shop_002`、焦点为 `none`。本轮专门验证 round-111 的一次性 Tooltip 恢复路径：取消帧不得残留，只有重新移入后正式 Tooltip 才可恢复到商品附近；共享宠物图和货孔继续使用不受正式目标徽记/Tooltip 覆盖的六个精确裁剪。美术项目只提供图片与视觉意图参考，双方继续使用各自 Scene、按钮、Session 与提示实现，不复制 Mock 代码、固定槽位或节点拓扑；若真实并排图发现错位，先保留失败证据并只修实际责任层。
- round-112 取消后重新悬停闭环：首轮 `8` 帧等待已恢复正式按钮的 `tooltip_text`，但真实截图尚未达到 Godot 默认提示延迟；延长新操作自身等待到 `60` 帧后，正式详细 Tooltip 稳定出现在第二商品附近，美术项目仍显示自己的简短 authored 提示，证明 round-111 的“取消态不残留、真实重移入后恢复”生命周期已贯通，无需继续改运行时代码。正式 Tooltip 覆盖了原底框裁剪最右 `13px`，该区域不再是共享资源范围；门禁改为同一底框未覆盖的左侧 `147px`，没有增加容差，其余五个安全区保持原尺寸且六项均为 `diff=0`。
- round-112 证据与验证：双边在各自 APFS 隔离副本、独立 `.godot`、`user://`、进程与专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏完成 `80` 步，新增帧均保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`、指针 `(902,454)`、悬停 `BUY_OFFER/shop_002`、焦点 `none`。最终逐操作图为 `output/validation/shop-art-roundtrip/round-112/comparison/19d_reentered_second_offer_rehover_after_cancel_art_vs_formal.png`，80 步总览为 `round112_80_step_overview.png`；几何审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`166` 项精确像素、`1` 项容差和 `9` 项效果。`95` 项当前 Python 专项、旧 `79` 步矩阵缺 `19d` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画准入门禁全部通过；本轮没有运行时代码、资源、数据、Session、Scene 或节点拓扑变化，继续由 round-111 同一运行时的 fast `19/19` 证据覆盖。用户 Godot PID `5112` 与五个既有 `.psd.import`、巡检 WIP 均保留；没有复制 Mock 代码或批准新资源/动画，全天持续目标继续进行。
- round-113 Tooltip 恢复后再次按住计划：紧接 `19d_reentered_second_offer_rehover_after_cancel` 已稳定显示提示的状态，双边在同一 `(902,454)` 再次发送真实左键按下但暂不松开，单独采集 `19e_reentered_second_offer_repressed_after_cancel`；要求公开状态仍为 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，悬停与焦点均为 `BUY_OFFER/shop_002`、按钮为 `DRAW_PRESSED`。本轮验证第二次按压周期会立即关闭已恢复的可见 Tooltip，同时重用同源 PNG 按住预览且不购买；共享像素门禁复用第一次按住的货孔四边与中央预览六块零容差裁剪。两仓仍各自使用自己的按钮、Tooltip、Scene 与 Session，不复制 Mock 代码、固定槽位或节点拓扑；若真实并排图确认差异，先保留失败证据并只修实际责任层。
- round-113 Tooltip 恢复后再次按住闭环：美术项目与正式项目在各自隔离副本、独立 `.godot`、`user://`、进程和专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏完成 `81` 步；第 `19e` 帧双方均保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，指针 `(902,454)`，悬停/焦点均为 `BUY_OFFER/shop_002`、绘制态为 `DRAW_PRESSED`，按下前后的完整权威 Snapshot/公开展示快照不变。正式侧断言已恢复的可见 Tooltip 在第二次按压时立即清空，但独立 `accessibility_name` 保留；人工并排确认两边都不残留 Tooltip，第二货孔、按住预览、队伍和底栏无位移、裁切或层级错误。双方各自第一次按住 `19b` 与取消、重悬停后的第二次按住 `19e` 整帧均为 `diff=0`，新增货孔四边与中央预览 `6` 块跨项目裁剪也全部为 `diff=0`。
- round-113 证据与验证：最终当前轮证据位于 `output/validation/shop-art-roundtrip/round-113-stable/`，逐操作并排图为 `comparison/19e_reentered_second_offer_repressed_after_cancel_art_vs_formal.png`，完整总览为 `round113_81_step_overview.png`。几何审计覆盖 `33` 项资源、`2` 项层级、`172` 项精确像素、`1` 项容差和 `9` 项效果，`passed=true`、`errors=[]`。长期探针显示队伍阴影的纯悬停帧会受宠物抗锯齿边缘的跨进程采样影响，稳定可比像素为 `1326–1501/1501`；因此仅把四个悬停效果的最低覆盖从 `1400` 调整为 `1300`，仍要求至少 `86.6%` 区域、方向正确且 MAE `<=1.1`，四个点击后稳定效果继续保持 `1400` 门槛，新回归测试锁定该区分。`97` 项 Python 专项、旧 `80` 步矩阵缺 `19e` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构与动画准入门禁通过；运行时代码、资源、公开数据、Session、Scene 与节点拓扑均未变，继续由 round-111 同一运行时的 fast `19/19` 证据覆盖。用户 Godot PID `5112` 与五个既有 `.psd.import`、巡检 WIP 均保留；没有复制 Mock 代码或批准新资源/动画，全天持续目标继续进行。
- round-114 第二次按住后移出计划：紧接 `19e_reentered_second_offer_repressed_after_cancel`，保持第二次左键按下并把双边指针从 `(902,454)` 移到中立区 `(1850,1030)`，暂不松开，单独采集 `19f_reentered_second_offer_repressed_pointer_outside`。要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品恢复 `DRAW_NORMAL`，悬停语义清空但按压取得的 `BUY_OFFER/shop_002` 焦点保留，正式可见 Tooltip 继续为空且 `accessibility_name` 不丢失；新门禁核对恢复后的完整第二货孔。截图后才在中立区松开并断言 Snapshot 不变、焦点清空、没有购买。两仓继续使用自己的按钮、Tooltip、Scene、Session 与固定/动态布局，只同步公开数据和批准图片，不复制 Mock 实现或节点拓扑；若并排图确认差异，保留失败证据并只修实际责任层。
- round-114 首轮失败责任层：双边 `82` 步真实采集和状态/交互门禁均通过，但 `19e/19f` 的正式第二货孔在按住期间额外显示 `30x30` 的“锁”按钮，美术参考没有；原因是正式 `ShopShelfLayout` 把鼠标按压取得的焦点当作独立键盘焦点，从而在拖持商品时暴露另一项操作。正式候选仅在自身商品 hold 生命周期隐藏锁按钮，松开后仍按真实鼠标悬停、键盘焦点或已冻结状态恢复，不删除键盘 focus 金框和可访问性。首个 `02_offer_hover` 的正式 Tooltip 因当前进程较慢覆盖货孔右边缘下段，该区域不是共享 PNG；右边缘精确裁剪缩到 Tooltip 未覆盖的 `y=360..463`，仍保持零容差。修复后将新增 `19e/19f` 两个完整第二货孔精确门禁并重新实窗采集。
- round-114 第二次按住后移出闭环：正式 `ShopShelfLayout` 在自己的商品 hold 生命周期开始/结束时重新计算上下文按钮可见性，拖持期间不再因鼠标取得的焦点显示“锁”，真实鼠标悬停、独立键盘 focus 和已冻结商品仍按原规则显示；没有修改 Mock 运行时，也没有复制其 Scene、脚本、Session、固定槽位或按钮实现。修复前 `19e/19f` 完整第二货孔跨项目各有 `852` 个差异像素，修复后两项均为 `diff=0`。双边最终 `82` 步保持同一 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`；`19e` 指针 `(902,454)`、悬停/焦点均为 `BUY_OFFER/shop_002`，`19f` 指针 `(1850,1030)`、悬停清空且焦点继续为 `BUY_OFFER/shop_002`，截图后移出松开没有购买并清空焦点。最终逐操作图为 `output/validation/shop-art-roundtrip/round-114/comparison/19f_reentered_second_offer_repressed_pointer_outside_art_vs_formal.png`，82 步总览为 `round114_82_step_overview.png`，修复前失败证据保存在 `round-114-before-fix/`。
- round-114 证据与验证：几何审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`174` 项精确像素、`1` 项容差与 `9` 项效果；早期 `02_offer_hover` 的右边缘门禁只排除正式 Tooltip 实际覆盖的下段，未覆盖上段仍保持 `diff=0`、零容差。`98` 项当前 Python 专项、旧 `81` 步矩阵缺 `19f` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构和动画批准门禁均通过；包含本轮正式运行时文件的完整 fast 为 `19/19`、0 失败，完整可玩流程 `175.58s`、seed bot `104.36s`，持久证据复制到 `output/validation/shop-art-roundtrip/round-114/qa-fast/`。用户 Godot PID `5112` 与专用虚拟屏、五个既有 `.psd.import` 和巡检 WIP 均保留。本轮没有公开数据、资源、玩法、Session、Scene、节点拓扑或动画批准变化，全天持续目标继续进行。
- round-115 第二次按住移出后重新移入计划：紧接 `19f_reentered_second_offer_repressed_pointer_outside`，保持第二次左键按下并把双边指针从中立区 `(1850,1030)` 重新移回第二件商品 `(902,454)`，单独采集 `19g_reentered_second_offer_repressed_pointer_reentered`。要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品恢复 `DRAW_PRESSED`，悬停与焦点均为 `BUY_OFFER/shop_002`；正式可见 Tooltip 必须保持为空、`accessibility_name` 保留，上一轮修复的“锁”按钮在活动 hold 内不得重新出现。截图后再次移到中立区再松开，断言 Snapshot 不变、焦点清空且没有购买。新门禁核对完整第二货孔，并与 `19e` 初次第二按住态做同侧一致性复核；两仓继续使用各自按钮、Scene、Session 与输入实现，不复制 Mock 代码、固定槽位或节点拓扑。若真实并排图确认差异，先保留失败证据并只修实际责任层。
- round-115 第二次按住移出后重新移入闭环：美术项目与正式项目在各自隔离副本完成全量 Godot 导入后，于专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏串行从真实入口完成 `83` 步。新增 `19g` 在第二次左键仍按住时由 `(1850,1030)` 重新进入 `(902,454)`，双边均保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品为 `DRAW_PRESSED`，悬停/焦点均为 `BUY_OFFER/shop_002`。正式可见 Tooltip 为空、`accessibility_name` 保留，上一轮修复的锁按钮不可见；完整第二货孔跨项目 `diff=0`，双边各自 `19e` 与 `19g` 整帧也均为 `diff=0`。再移出并松开后 Snapshot 不变、焦点清空且没有购买；本轮没有确认新的资源、几何、层级或状态错位，因此不修改任一边运行时代码、资源、数据或节点拓扑，也不回传 Mock 实现。
- round-115 证据与验证：第 `83` 步并排图为 `output/validation/shop-art-roundtrip/round-115/comparison/19g_reentered_second_offer_repressed_pointer_reentered_art_vs_formal.png`，完整总览为 `output/validation/shop-art-roundtrip/round-115/round115_83_step_overview.png`；几何审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`175` 项精确像素、`1` 项容差和 `9` 项效果。`99` 项 Python 专项、旧 round-114 缺第 `19g` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入和双仓本任务范围 `git diff --check` 全部符合预期。本轮运行时文件与 round-114 已通过完整 fast 的版本一致，继续由持久 `19/19`、0 失败证据覆盖。用户 Godot PID `5112`、专用虚拟屏、五个既有 `.psd.import` 和巡检 WIP 均保留；无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-116 第二次按住重新移入后再次移出计划：紧接 `19g_reentered_second_offer_repressed_pointer_reentered`，保持第二次左键按下并把双边指针再次从第二件商品 `(902,454)` 移到中立区 `(1850,1030)`，单独采集 `19h_reentered_second_offer_repressed_pointer_outside_again`。要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品恢复 `DRAW_NORMAL`，悬停语义清空但按压取得的 `BUY_OFFER/shop_002` 焦点保留；正式可见 Tooltip 继续为空、`accessibility_name` 保留，活动 hold 期间上下文锁按钮仍不可见。新门禁核对恢复后的完整第二货孔，并与 `19f` 第一次移出态做同侧整帧一致性复核。截图后才在中立区松开，断言 Snapshot 不变、焦点清空且没有购买。两仓继续使用各自按钮、Scene、Session 与输入实现，不复制 Mock 代码、固定槽位或节点拓扑；若真实并排图确认差异，先保留失败证据并只修实际责任层。
- round-116 第二次按住重新移入后再次移出闭环：美术项目与正式项目在各自隔离副本完成全量 Godot 导入后，于专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏串行从真实入口完成 `84` 步。新增 `19h` 在第二次左键仍按住时由 `(902,454)` 再次移到 `(1850,1030)`，双边均保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品恢复 `DRAW_NORMAL`，悬停为 `none`、焦点仍为 `BUY_OFFER/shop_002`。正式可见 Tooltip 为空、`accessibility_name` 保留，活动 hold 期间锁按钮继续隐藏；完整第二货孔跨项目 `diff=0`，美术侧与正式侧各自 `19f` 和 `19h` 整帧 SHA-256 也分别相同。随后松开后 Snapshot 不变、焦点清空且没有购买；本轮未确认新的资源、几何、层级或状态错位，因此不修改任一边运行时代码、资源、数据或节点拓扑，也不回传 Mock 实现。
- round-116 证据与验证：第 `84` 步并排图为 `output/validation/shop-art-roundtrip/round-116/comparison/19h_reentered_second_offer_repressed_pointer_outside_again_art_vs_formal.png`，完整总览为 `output/validation/shop-art-roundtrip/round-116/round116_84_step_overview.png`；几何审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`176` 项精确像素、`1` 项容差和 `9` 项效果。`100` 项 Python 专项、旧 round-115 缺第 `19h` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入和双仓本任务范围 `git diff --check` 全部符合预期。本轮运行时文件与 round-114 已通过完整 fast 的版本一致，继续由持久 `19/19`、0 失败证据覆盖。用户 Godot PID `5112`、专用虚拟屏、五个既有 `.psd.import` 和巡检 WIP 均保留；无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-117 第二次按压往返后在商品外松开计划：紧接 `19h_reentered_second_offer_repressed_pointer_outside_again`，在中立区 `(1850,1030)` 松开第二次左键并清理仅由鼠标按压产生的焦点，单独采集 `19i_reentered_second_offer_repress_cancelled_after_reentry_cycle`。要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品维持 `DRAW_NORMAL`，悬停与焦点语义均为 `none`，正式可见 Tooltip 为空、`accessibility_name` 保留且上下文锁按钮不可见；不得执行 `BUY_OFFER`。新门禁核对完整第二货孔，并与 `19c` 第一次商品外取消态做同侧整帧一致性复核。两仓继续使用各自按钮、Scene、Session 与输入实现，不复制 Mock 代码、固定槽位或节点拓扑；若真实并排图确认差异，保留失败证据并只修实际责任层。
- round-117 第二次按压往返后在商品外松开闭环：美术项目与正式项目在各自隔离副本完成全量 Godot 导入后，于专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏串行从真实入口完成 `85` 步。新增 `19i` 在第二次按压完成移出、重入、再移出后于中立区 `(1850,1030)` 松开，双边均保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，第二商品为 `DRAW_NORMAL`，悬停/焦点均为 `none`，且没有执行购买。正式侧同时确认可见 Tooltip 为空、`accessibility_name` 保留、上下文锁按钮不可见；完整第二货孔跨项目 `diff=0`，美术侧与正式侧各自第一次取消 `19c` 和本轮完整往返后取消 `19i` 的整帧 SHA-256 也分别相同。人工检查没有新增资源、几何、层级、提示或输入状态错位，因此不修改任一边运行时代码、资源、数据或节点拓扑，也不回传 Mock 实现。
- round-117 证据与验证：第 `85` 步并排图为 `output/validation/shop-art-roundtrip/round-117/comparison/19i_reentered_second_offer_repress_cancelled_after_reentry_cycle_art_vs_formal.png`，完整总览为 `output/validation/shop-art-roundtrip/round-117/round117_85_step_overview.png`；几何审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`177` 项精确像素、`1` 项容差和 `9` 项效果。`101` 项 Python 专项、旧 round-116 缺第 `19i` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入和双仓本任务范围 `git diff --check` 全部符合预期。本轮运行时文件与 round-114 已通过完整 fast 的版本一致，继续由持久 `19/19`、0 失败证据覆盖。用户 Godot PID `5112`、专用虚拟屏、五个既有 `.psd.import` 和巡检 WIP 均保留；无新项目标准、资源或动画批准，全天持续目标继续进行。
- round-118 第二次完整取消后重新悬停计划：紧接 `19i_reentered_second_offer_repress_cancelled_after_reentry_cycle`，双边把指针从中立区 `(1850,1030)` 真实移回第二件商品 `(902,454)`，等待 Godot Tooltip 稳定并单独采集 `19j_reentered_second_offer_rehover_after_repress_cycle_cancel`。要求公开状态继续保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，悬停恢复为 `BUY_OFFER/shop_002`、焦点为 `none`；正式 Tooltip 必须只在这次真实重新移入后恢复，`accessibility_name` 保留且上下文锁按钮重新按正常悬停规则显示。新门禁复用第一次重悬停的六块共享零容差裁剪，并与 `19d` 做同侧整帧一致性复核。两仓继续使用各自按钮、Scene、Session 与提示实现，不复制 Mock 代码、固定槽位或节点拓扑；若真实并排图确认差异，先保留失败证据并只修实际责任层。
- round-118 第二次完整取消后重新悬停闭环：美术项目与正式项目分别完成全量隔离导入，并在专用 `1920×1080 @ 60Hz`、`Online: Yes`、`Mirror Off` 虚拟屏串行从真实入口完成 `86` 步。新增 `19j` 在第二次完整取消后由中立区真实返回 `(902,454)`，双边均保持 `shop, coins=6, offers=3, roster=5, v9/dc172b57c1b7d812`，悬停恢复为 `BUY_OFFER/shop_002`、焦点为 `none`，没有购买。正式侧同时确认详细 Tooltip 恢复、`accessibility_name` 保留且上下文锁按钮按普通悬停规则可见；六块未被正式功能浮层覆盖的共享区域跨项目全部 `diff=0`，美术侧与正式侧各自第一次重悬停 `19d` 和本轮 `19j` 的整帧 SHA-256 也分别相同。人工检查未见新增资源、位置、裁切、层级、提示或输入状态错位，因此不修改任一边运行时代码、资源、数据或节点拓扑，也不回传 Mock 实现。
- round-118 证据与验证：第 `86` 步并排图为 `output/validation/shop-art-roundtrip/round-118/comparison/19j_reentered_second_offer_rehover_after_repress_cycle_cancel_art_vs_formal.png`，完整总览为 `output/validation/shop-art-roundtrip/round-118/round118_86_step_overview.png`；几何审计 `passed=true`、`errors=[]`，覆盖 `33` 项资源、`2` 项层级、`183` 项精确像素、`1` 项容差和 `9` 项效果。`102` 项 Python 专项、旧 round-117 缺第 `19j` 的负门禁、四项正式商店 smoke、美术同步 smoke、Mock 独立结构/动画准入和双仓本任务范围 `git diff --check` 全部符合预期。本轮运行时文件与 round-114 已通过完整 fast 的版本一致，继续由持久 `19/19`、0 失败证据覆盖；正式到美术的公开数据/图片同步边界及美术回传白名单未变，Mock smoke 继续报告 `code_inbound=0`。用户 Godot PID `5112`、专用虚拟屏、五个既有 `.psd.import` 和巡检 WIP 均保留；无新项目标准、资源或动画批准，全天持续目标继续进行。

## 2026-08-17 固定种子验收收口

- 资源审计结论：美术项目不包含正式程序包全部图片。正式全集为 `278` 个宠物身份/`278` 张唯一宠物 PNG、`141` 个商人身份/`33` 张唯一商人 PNG；同步前美术项目按 SHA-256 只精确拥有其中 `20` 张宠物 PNG 和 `1` 张商人 PNG，仍缺 `258 + 32` 张；本固定种子只需要 `16` 个宠物身份和 `1` 个商人，共 `17` 张出站 PNG。
- 当前固定种子文件闭包为 `17/17`，但身份真图完整性仍为否：`pal_342` 在正式映射中缺失，当前明确回退到 `pal_042`。这项作为资源缺口记录，不再把“能显示”误报为“真图齐全”。机器清单在 `art/manifests/shop/formal_resource_inventory.json`。
- 同步边界已校正：商店使用独立 `data/formal_shop_art_runtime.json`，不再写入战斗任务的 `data/mock_battle_snapshot.json`；正式到美术为当前公开捕获和 `17` 张闭包 PNG，美术到正式只允许 `15` 张 art-owned PNG，代码回传为 `0`。
- 历史残留已按既有同步基线哈希精确识别为 `pal_028`、`pal_033`、`pal_042`；其他同步任务占用的 `pal_001`、`pal_002`、`pal_003`、`pal_004` 均受保护。只移除上述三项旧 PNG、Godot import 和 `formal_sync` 映射，不触碰美术自有资源。
- 默认真实窗口验收已从 `86` 张收缩为 `14` 张。美术与正式项目分别从真实路线入口跑同一固定捕获，均报告 `captures=14`；逐操作比较报告 `pairs=14`、`profile=fixed_seed_acceptance_14`。
- 本轮复现并修复一项真实错位：正式商店复用视图导致退出后重进仍保留打开的背包遮罩。责任层为正式 `LegacyCodeShopView` 的本地表现生命周期；现在每次重新显示时关闭背包，不改 Snapshot/Command/玩法，也未复制 Mock 代码。修复后 `19_reenter_shop_after_exit` 的商品、关闭宝箱、金币牌、退出牌六个完整裁剪全部 `diff=0`。
- 当前证据在 `output/validation/shop-art-roundtrip/round-acceptance-20260817/`：`comparison/` 含 `14` 张一操作一对照图，`geometry_audit.json` 为 `passed=true`、`errors=[]`，覆盖 `21` 项资源、`2` 项层级、`36` 项精确裁剪、`1` 项容差与 `1` 项效果；人工复核背包打开和退出后重进两张并排图无位置、裁切或遮挡差异。
- 最终专项为 `103` 项 Python 单测、四项正式商店 smoke 和美术同步 smoke 全部通过；正式完整 fast QA 为 `19/19`、0 失败，证据在 `output/validation/qa/20260817-022956-fast/`。用户当前 Godot 与其他任务的战斗数据、`.psd.import`、巡检队列和未跟踪导入缓存均未关闭、覆盖或暂存。
- 美术项目已形成独立提交 `07164a3`；正式项目的 `tasks/ai/QUEUE.md`、`tasks/ai/STATUS.md` 正被巡检任务修改，本任务按 `FILE_CONFLICT_STOP` 只在本卡记录完成状态，不覆盖或混入其 WIP。
