# 战斗逐操作动画与显示对齐审计（2026-07-19）

## 口径

- 只读美术基准：`/Users/ywh/Downloads/棋盘-战斗界面/`，重点核对 `Scenes/Main_Scene.tscn`、`Scenes/BoardCell.tscn`、`Scripts/BoardGrid.gd` 和 `Prefabs/**`。
- 当前运行时基准：`/Users/ywh/Documents/godot-latest` 的真实 `YSBZS Godot Singleplayer (DEBUG)` 主入口、真实读档 / 路线战斗、`battleTrace`、snapshot 和玩家操作 JSONL。
- 合并原则：只增不退。美术包规定动画与显示下限；当前 8x8、核心规则、完整自动战斗及所有已确认优化均保留。

## 本轮真实检查

- 真实窗口从第 1 天节点 3 路线进入正式战斗，看到 8x8、四只我方宠物、双方主将、敌方单位、行动面板及美术“自动布置 / 开始行动”按钮。
- 完整战斗实际从回合 1 自动推进到回合 9 胜利结算，金币 8 → 12；最终返回路线 / 结算信息面。
- `user://ysbzs_player_operations.jsonl` 证实自动循环每轮真实执行 `AUTO_POSITION_HEROES → RUN_COMBAT_ROUND`，不是静态 fixture。
- 审计期间 `battle-trace-snapshot-staging` 已由提交 `da7109f` 完成：trace 从行动前棋盘起播，结束后才落最终 HP、死亡、敌方位置和下一轮预览；该项转入非回归基线。

## 必须保留的现有优化

- 8x8 / 64 格、真实核心 board 与攻击形状，不回退 demo 8x7 和本地常量规则。
- 四宠身份、数量和显式玩家宠物图片；自动布置的击杀优先、生存规避、唯一合法落点和主将占位保护。
- 一次“开始行动”逐轮自动布置、等待动画并走到真实结算。
- 拖拽跟手、攻击范围投影、合法落位平滑吸附、非法落点回弹和残留清理。
- 玩家元素弹道、空格元素冲击、护盾 / 生命分拆伤害数字、受击抖动、敌方移动、咬击、死亡标记、波次召唤淡入缩放。
- 宠物 / 元素格详情、攻击力 / 生命 / 护盾 / 即将受伤、持久操作日志。
- command snapshot 复用、相同 snapshot 零格更新、单格增量渲染及拖拽按需 process。
- trace 从行动前状态逐事件播放，动画全部结束后才落最终 snapshot 和下一轮预览。

## 当前需要做的工作

| 优先级 | 操作 | 当前真实状态 | 对齐后要求 |
|---|---|---|---|
| P0 | 每回合开始 | 真实核心 trace 没有 `round_start`；当前控制器只主动播放首回合横幅，后续回合主要只改左侧标题。 | 每个真实回合都播放美术横幅；补美术包的全屏暗化 / 模糊、缩放淡入淡出和输入阻断，播完后才进入本轮自动布置 / 行动。 |
| P0 | 敌方攻击 | 当前是直线前冲到目标距离的 68%，咬击与回位并行；伤害在整段咬击结束后才落。 | 保留已接前冲优化，但改为美术包的弧线跳跃、约 8% 放大、命中后回跳；伤害必须在美术命中帧落下，而不是动画结束后。 |
| P0 | 自动战斗轮间节奏 | 内部自动布置 snapshot 后立即提交 `RUN_COMBAT_ROUND`，缺少明确的“布置结果已显示”阶段。 | 每轮保留可见布置结果：单位移动 / 稳定落位、攻击范围、行动顺序和伤害预览出现后，再开始行动；不得降低现有自动战斗速度和完整结算能力，可用短动画或最小可见帧实现。 |
| P1 | 自动布置后预览 | 当前能显示实际站位、红色作用格和“即将受伤”文字，但没有按真实计划完整显示美术死亡预览与全部行动顺序标记。 | 保留当前真实 threat / 伤害数据，在其上补死亡预览图和基于真实 action plan 的顺序标记；不使用 demo 固定三方向规则。 |
| P1 | 元素铺设后的稳定显示 | 已有弹道、格子彩色 pulse 和元素详情；空格稳定态主要是小色块，未完全复用美术 `Element_Buff` 圆环及层数 / 伤害承载。 | 保留现有 pulse、中文元素层和详情，再补美术圆环稳定态；层数 / 伤害继续读核心，不照搬 demo trap 数值。 |
| P1 | 后续波次敌方图片 | `enemy_image_map.json` 只显式覆盖 `pal_001 / 棉悠悠`、`pal_042 / 炽焰牛`；五波实际还有 `pal_006 / 冲浪鸭`、`pal_003 / 皮皮鸡`、`pal_022 / 遁地鼠`，目前走元素占位降级。 | 为剩余 3 类敌人建立显式图片映射，并在真实完整战斗逐波确认；保留当前 fallback 仅作诊断兜底。 |
| P1 | 死亡过程 | snapshot 提前落地已修复；当前有命中抖动和死亡标记，但仍需核死亡标记停留与最后一击、单位移除、结算切换之间的节奏。 | 固定“最后一次命中 → 生命归零 → 死亡标记 / 动画 → 单位移除 → 结算”的可见顺序，不丢当前伤害与护盾反馈。 |
| P2 | 同一行动的 VFX 分组 | 当前按 `ELEMENT_APPLIED` 与每条 `DAMAGE_APPLIED` 逐事件播放，复杂形状 / 三次独立伤害会产生连续弹道；没有独立 barrage / 同方向齐射分组。 | 保留三次独立伤害规则，增加 action-block 视觉分组：一次铺元素、三次命中如何分镜要清楚，避免看成重复或错序；Boss barrage 仅在核心事件明确时播放。 |
| P2 | 视觉禁用态 | trace 期间核心输入会被拒绝，两个主按钮会 disabled；左侧行动面板仍主要靠控制器拦截，视觉上不够明确。 | 动画和横幅期间统一显示不可操作状态，结束后统一恢复；保留详情显示但禁止生成新命令或高亮残留。 |
| P2 | 旧缺口报告 | `reports/battle_ui_connection_gaps.md` 仍写“开始行动只执行单回合”等旧口径，也把已接的敌方前冲列为未接。 | 完成上述实现后刷新旧报告，避免后续按过时边界回退现有优化。 |

## 已基本对齐，无需重做

- 美术背景、按钮位置与资源入口。
- 8x8 棋盘和 64 个 prefab 格子（这是当前确认优化，优先于 demo 8x7）。
- hover / selected 美术框；当前 0.82 缩放和居中是已确认优化，保留。
- 拖拽跟手、红色攻击预览、行动顺序图片资源、合法吸附 / 非法回弹。
- 四元素 0.32 秒抛物线弹道和 0.45 缩放。
- 伤害数字上浮淡出、护盾蓝字 / 生命红字、受击抖动。
- 敌方 0.22 秒移动、五帧咬击资源、波次召唤淡入缩放。
- 最终胜负结算和返回路线闭环。

## 建议实施顺序

1. 补真实每回合横幅与轮间可见自动布置阶段。
2. 精确对齐敌方跳跃、咬击命中帧、死亡顺序。
3. 补自动布置后的死亡 / 顺序预览和元素稳定态。
4. 补齐三类敌方显式图片，再做 action-block / barrage 分组和旧报告清理。
