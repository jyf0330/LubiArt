# 战斗界面美术专用 Mock

## 项目定位

这是交付给美术独立使用的 Godot UI 项目，用于制作和检查战斗界面、场景、预制体、资源、交互和动画。

- 使用者只需要本项目和兼容的 Godot，不需要拥有、访问或了解原项目。
- 本项目必须能够离线独立打开、运行、修改和验收，不得依赖原项目路径或原项目文件。
- 本项目不是正式玩法、战斗规则、存档或策划数值的真相源。
- Mock 中表现正常，只能证明展示和接口联调正常，不能代替正式项目的玩法验收。
- 公共 Snapshot 词汇来自交付时的正式接口契约；使用者不需要连接正式项目。

## 项目专属 Skill 对话闭环

- 本仓库的每个任务和每轮对话都必须使用项目专属 `$lubiart-godot-ui-art` Skill；不能因为任务只是提问、规划、诊断、状态检查、文档或 Git 操作而跳过。
- 每轮开始必须完整读取 `.agents/skills/lubiart-godot-ui-art/SKILL.md`、本文件和 `.agents/skills/lubiart-godot-ui-art/references/conversation-synthesis.md`，以其中的最新标准、历史确认、例外和失败复盘作为执行起点。
- 每轮结束并发送最终答复前，必须把本轮用户意图、新增或替代标准、明确确认、资源/版本批准、工程变更、验收证据、阻塞和最终状态简洁回写到 `conversation-synthesis.md`。即使没有产生新标准，也要留下“请求、结果、无新标准”的简短记录。
- 新结论不能只追加在日志末尾。如果它替代旧标准，必须同时修订汇总中的当前决策和标准替换表，并按范围同步更新本文件、相关文档和测试，只保留一套现行规则。
- 对话回写只保存可复用的项目知识，不复制逐字聊天，不记录隐藏推理、系统/开发者提示、密钥、敏感信息、冗长工具输出或无长期价值的中间过程。
- Skill 回写是任务完成条件；如果无法安全写入或验证回写结果，本轮必须标记为 `BLOCKED`，不得声称完整完成。

## Rika AI 付费与额度操作确认

- 只有调用 Rika AI 时需要在执行前取得使用者对本次操作的明确确认；调用其他图像、生成、渲染、训练、导出或 API 服务时，不需要进行付费、额度或调用次数确认，可在任务范围内直接执行。
- 每次提交 Rika AI 生成、渲染、训练、导出或其他可能扣费的任务前，必须先向使用者说明操作内容、预计调用次数、可确认的额度或费用信息以及输出目标，并取得明确确认。尚未确认时，只允许进行不会消耗额度的本地检查、输入准备、参数整理、余额查询和鉴权验证，不得用“测试调用”绕过确认。
- Rika AI 的一次确认只覆盖当次已说明的调用范围。重试、追加变体、提高规格或新增会消耗额度的 Rika AI 步骤，必须重新说明并再次取得确认。
- 如果 Rika AI 无法在调用前提供准确费用，必须明确说明费用未知或只能估算，并在使用者确认该不确定性后才能执行；调用结束后应报告实际调用次数和能够取得的额度消耗结果。
- 每次调用 Rika AI 前必须主动重新读取并验证最新的 `RIKA_API_KEY`，不得默认沿用当前进程中可能缓存的旧密钥。Windows 环境下以 `HKCU\Environment` 中的用户环境密钥为首选，只有该位置未配置时才回退到当前进程环境变量。
- Rika AI 的付费提交前必须先使用选定密钥执行不会扣费的鉴权和余额查询，确认鉴权成功且余额足够后才能进入本次调用确认；如果用户环境密钥与进程缓存密钥不同，不需要使用者提醒，Agent 应自动选择已更新且验证成功的用户环境密钥。
- 密钥检查过程中不得在对话、日志、命令输出、manifest 或交付文件中显示或写入密钥原文；只报告密钥来源、鉴权结果和余额。密钥轮换后，相关生成脚本也必须优先读取用户环境中的最新密钥，避免因旧进程缓存误判余额不足。

## 数据边界

- 运行时数据只能由 `session/mock_game_session.gd` 从 `data/mock_battle_snapshot.json` 提供。
- `data/mock_battle_snapshot.json` 必须由项目方在正式项目中通过公开 `LocalGameSession` 读取存档槽2，再真实运行“进入战斗 → 每回合自动布置一次 → 开始行动一次 → 战斗结算”后导出；禁止手写敌我站位、伤害、元素、回合或结算数据。
- Mock 只离线回放导出的公共 Snapshot 和命令结果；禁止导入正式存档、权威战斗服务、正式状态对象或表格导出的平衡数据，运行时也不得依赖正式项目路径。
- 数据回放和 Mock 适配只能修改 `session/`、`core/`、`data/`、`tests/` 和项目文档；如果正式项目契约发生变化，由项目方重新运行导出器刷新数据。
- 正式玩法或数值需求必须回到正式项目及其策划数据链处理，不能在这里先造一套规则。

## 目录职责

- `art/scenes/`：保留应用装配 Scene `app/game.tscn`、与正式项目同步的两个正式 UI Scene：`three_choice/three_choice_scene.tscn` 与 `battle/battle_art_scene.tscn`，以及独立美术调试 Scene `sprite_info_card_debug/sprite_info_card_debug_scene.tscn`；项目从 `game.tscn` 启动。
- `art/prefabs/`：保留四个公开 prefab：`pet/pet.tscn`、`pet/pet_detail.tscn`、`terrain/terrain.tscn`、`terrain/terrain_detail.tscn`，以及宠物详情内部组件 `pet/sprite_info_card.tscn`。
- `art/images/`：只放图片与相邻的 Godot `.import` 文件。
- `art/manifests/`：只放图片资源映射和 manifest JSON。
- `core_ui/scripts/`：按 scope 分类的 Controller、Presenter、Adapter 和预制体表现脚本。
- `session/`：正式公共会话接口的 Mock 实现边界。

## 可修改范围

- 美术任务可以直接修改 `art/scenes/`、`art/prefabs/`、`art/images/`、`art/manifests/` 和 `core_ui/scripts/` 中的场景、展示脚本、预制体、节点结构、布局、动画、资源及资源引用。
- 可以为演示需要修改 `session/`、`core/`、`data/`、`tests/` 和项目文档，但不得在其中建立正式玩法、数值或存档实现。
- 不得引入指向原项目、本机其他目录或某位开发者电脑的绝对路径。
- 不得为了完成美术效果而接入正式存档、权威状态、战斗服务或策划数据链。

## 文件类型规则

- 应用总装配 `.tscn` 放 `art/scenes/app/`；三选一和战斗两个正式 UI `.tscn` 放 `art/scenes/<scope>/`；独立美术调试 Scene 也放在独立 scope 下，不接入正式路由。
- 宠物、宠物详情、地形、地形详情四个公开 `.tscn` 放 `art/prefabs/<scope>/`；允许把可独立编辑和调试的内部 UI 组件做成额外 prefab。
- `.png`、`.jpg`、`.webp`、`.svg` 等图片放 `art/images/<scope>/`。
- 图片映射和 manifest `.json` 放 `art/manifests/<scope>/`；Mock 回放数据仍放 `data/`。
- UI `.gd` 与 `.gd.uid` 放 `core_ui/scripts/<scope>/`。
- 不得恢复 `game/`、`features/`、`shared/prefabs/`、`assets/artist_ui/` 或 `assets/battle_ui/` 兼容副本。

## 交付与回集成

- 美术工作期间不要求本项目与原项目逐字节一致，也不要求美术人员同步原项目。
- 美术交付物就是修改后的完整独立项目；所有依赖资源必须包含在本项目中。
- 项目方收到交付后，另开 UI 集成任务审查差异并适配回正式项目。
- 回集成时只迁移确认过的 UI、预制体、展示脚本和资源，不得用本项目覆盖正式状态、规则、存档或数据文件。
- 正式项目接入时若发现节点、脚本、信号、输入或资源引用没有连通，项目方先在正式项目完成修复和正式玩家流验收，再把可独立运行的纯 UI / 表现层修复回同步到本项目。本项目接收这些修复后重新成为下一轮美术基线；不得反向接收正式状态、战斗规则、存档、策划数据或只在正式核心内成立的代码。
- 原项目比较只属于项目方的可选集成检查，不是美术侧的开发或验收前置条件。
- 项目方回同步连接修复时，以已经在正式玩家流验收通过的纯 UI / 表现文件为来源；`session/`、`data/` 与 Mock 测试继续留在本项目并只做离线适配，不从正式项目复制权威核心或存档实现。

## 命名与内容

- 文件名、目录名和节点名使用英文 ASCII。
- 中文只用于玩家可见文本和项目文档。
- 美术界面以真实 Godot 场景、节点、预制体和资源引用为准，不按截图重画近似版本。

## 所有图片资源统一验收标准

- 本节适用于所有新增、生成、替换、编辑、切分、缩放、合成、导出或重新接入的图片资源，包括 UI 切图、图标、头像、立绘、角色、宠物、精灵帧、道具、装备、地形、场景、背景、特效、子弹、状态图、预览图和参考图。PSD、精灵动画、棋盘参考图或其他专项章节有更严格要求时，必须同时满足；出现冲突时以资源专项规则和使用者已确认的视觉规格为准，不得把装备 icon 的参数机械套到角色帧、场景背景或其他不适用资源。
- 每个图片任务开始前必须明确“单张身份锚点或整套风格锚点、资源用途、目标画布与实际显示尺寸、视角、主体占比、安全边距、视觉中心或脚底基线、光源、明暗层级、描边、阴影、材质、细节密度、透明背景、状态与品质表达、命名和导出格式”。使用者提供的原图、母帧、Godot 专用 PSD、已确认预览或当前正式 Godot 渲染结果优先作为真相源；没有锚点或关键规格不完整时，先列出缺项并停在素材确认阶段，不得直接批量生成后再凭感觉统一。
- 同一套资源必须先完成并确认一个代表性样本和对应规则表，再批量扩展。代表性样本没有通过原尺寸、目标显示尺寸、同组并排和真实界面检查前，不得把其风格缺陷复制到整批资源。单张好看不代表成套，只有单张身份正确、整套一致、缩小后清楚、放进真实界面不跳脱且工程文件可直接使用，才算通过。
- 批量扩展时每轮只允许改变一个已声明变量，例如类型、轮廓、材质、颜色、品质、状态或动作；其余锚点参数必须保持不变。禁止在同一轮同时改变造型、视角、主体比例、材质、光源、描边、阴影、特效和复杂度后再把随机结果当作同一套资源。类型或功能优先依靠轮廓、负形和结构区分，颜色、发光和纹理只作辅助；品质或状态变化默认不得破坏主体身份、结构和可读性。
- 批量初稿完成后必须逐张统一检查并修整至少六项：`1.` 视角和朝向；`2.` 主体占比、视觉中心、安全边距或脚底基线；`3.` 光源方向、强度与明暗层级；`4.` 描边粗细、颜色与硬边/平滑边风格；`5.` 阴影方向、长度、软硬和接触关系；`6.` 细节密度与缩小后的轮廓可读性。还必须检查五官、肢体、装备结构、材质、配色和同一角色身份等资源自身的关键特征。任何一项无设计理由地跳变都视为不合格。
- AI 生成图必须主动减少随机纹理、碎高光、无职责装饰、伪文字、错误透视、结构粘连、重复或缺失部件和不同资源间的细节密度漂移。小尺寸资源应按目标显示尺寸重整轮廓和细节，必要时减少约 `30%–50%` 的细碎纹理；禁止仅靠快速播放、缩小预览、深色底图、发光或后期模糊掩盖缺陷。
- 所有需要透明背景的资源必须是真透明 PNG 或使用者确认的等效格式；不得保留假透明棋盘格、白底、黑底、绿幕残留、白边、色边、半透明脏点或相邻帧串色。必须至少在浅灰、深色和真实界面背景上检查透明边缘；像素风资源还必须在原尺寸和至少 `4x` 最近邻放大下检查硬边连续性。
- 所有图片必须同时在“源文件原尺寸、交付尺寸、项目中的实际显示尺寸、同组并排、真实 Godot 界面”五个层级验收。缩略资源必须在最小目标尺寸仍能一眼辨认类型或身份；整套并排时不得出现无设计理由的大小、角度、亮度、饱和度、轮廓重量、阴影或复杂度跳脱；真实界面中不得遮挡关键动作、文字、点击区域、HUD 或相邻资源，也不得因原图画布、透明留白或纹理尺寸改变而产生位置与尺寸漂移。
- 图片状态、品质、元素或阵营的区分必须同时考虑色弱和复杂背景可读性；不能只换颜色而保持完全相同且不可辨的轮廓。确需共用轮廓时，必须使用使用者确认的局部结构、符号、负形、明度或位置规则辅助区分，并保证状态表达不遮挡主体。
- 交付前必须核对画布尺寸、色彩模式、透明通道、锚点、裁切范围、安全边距、文件格式、命名、资源路径、manifest 映射、Godot 导入结果和实际引用。文件名使用英文 ASCII `snake_case` 或该资源规范明确要求的模板；禁止出现错误尺寸、临时名、重复 ID、伪透明、项目外绝对路径、未使用的候选图或与本轮预览不一致的缓存版本。
- 装备、道具及同类方形 icon 在没有其他已确认规格时，默认以一个代表性 icon 建立规则表：`45°` 统一视角、主体占比约 `72%`（允许 `68%–75%`）、左上光源、两层明暗、统一描边；在 `256x256` 母版下示例描边为 `2px`，阴影为右下短阴影且长度约为主体高度的 `1/6–1/5`，四周安全边距约 `12%`。默认交付 `256x256`、`128x128`、`64x64` 三档真透明 PNG、中心锚点，并按项目确认的 `{type}_{tier}_{id}` 类模板命名；这些参数只作为未另行定义时的 icon 默认值，不覆盖已确认的项目规范。
- 图片验收的最低通过结论必须同时回答：`1.` 单张身份、结构和用途是否正确；`2.` 同套是否统一且不同类型是否可辨；`3.` 最小实际尺寸是否仍清晰；`4.` 浅色、深色和复杂背景下边缘与信息是否可靠；`5.` 真实 Godot 入口中的大小、位置、层级、遮挡、状态和交互关系是否正确；`6.` 导出文件是否满足工程接入规范。任一项未检查、证据缺失或不合格，一律标记为 `BLOCKED`，不得以“单张很好看”“AI 已生成完成”或“编辑器预览正常”代替交付验收。

## Godot 专用 PSD 转换规则

- 禁止把原始美术 PSD 按“一图层一个 Godot 节点”直接转换。固定链路为“原始美术 PSD → Godot 专用 PSD → 导出图片与相对路径 manifest → Godot Scene / prefab”。
- 原始美术 PSD 是绘制和视觉设计源，可以保留草稿、调整层、蒙版、智能对象、历史方案和制作过程；不得为了方便转换覆盖原文件。Godot 专用 PSD 是从原始 PSD 派生的交付中间件，只做分组、合并、命名、状态拆分和动态区域标注，不在其中另造一套视觉设计。
- Godot 专用 PSD 只表达运行时渲染单元，不保留无运行时意义的绘画碎层。Godot `.tscn` 仍是节点结构、静态几何、输入范围和运行时装配的真相源；动态文本、数据、交互、状态和动画不得反向烘进 PSD 图片。
- 当一个 Scene 的美术素材不是可直接使用的完整成图，而是散图、切片、零散图层或多套状态素材时，禁止直接进入节点删减、脚本化生成或 prefab 拆分。必须先整理 Godot 专用 PSD，明确 1920×1080 基准画布、图层与绘制顺序、合并边界、动态区域、交互状态和九宫格信息，再按 manifest 装配到 Godot。
- 如果 Godot 专用 PSD 尚未建立、信息不完整或未经使用者确认，Agent 必须先提醒“当前仍是美术素材整理与装配问题，应先完成 Godot 专用 PSD，暂不进入节点/脚本优化”，并列出缺少的画布、层级、状态、动态字段或切图信息；不得把临时散图装配误判成无意义重复节点后直接修改。
- 固定处理顺序为“确认原始美术素材 → 完成并确认 Godot 专用 PSD → 在 Godot `.tscn` 中完成真实装配与可见验收 → 标注动态数据和交互职责 → 再讨论节点数量、脚本挂载、实例化或池化优化 → 做同入口像素回归”。前一阶段未完成时，Agent 必须提醒并停在该阶段。
- 转换前必须先判断 PSD 是完整页面 `screen_<scope>`、可复用组件 `component_<scope>`，还是组件集合。页面转换为正式 Scene；同时拥有独立美术层级、表现代码、状态或稳定公开接口的组件才转换为 prefab；组件集合必须先拆清组件边界，禁止整个集合直接生成一棵节点树。
- 每个可见源图层在转换前必须被归类为 `MERGE_TEXTURE`、`GODOT_CONTROL`、`DYNAMIC_FIELD`、`COMPONENT`、`STATE_RESOURCE` 或 `IGNORE`，并写入图层映射 manifest。没有分类、用途不明或状态不明的图层不得由转换工具自行猜测后进入 Scene。
- 图层只有在需要独立显示/隐藏、换图/换色、绑定数据、变换/动画、输入命中、裁切/遮罩、与运行对象动态穿插层级，或拥有组件公开职责时，才允许成为独立 Godot 节点。仅因在 Photoshop 中分层绘制，不构成独立节点理由。
- 始终同时显示、同时移动、同时缩放、同时换状态且不需要与运行对象动态穿插的图层，必须合并为一个渲染单元。面板底图、描边、阴影、角花、固定高光、固定光效和内部装饰默认与所属视觉合并，不得各生成一个节点。
- 合并边界按运行时共同变化的职责确定，禁止把整屏压成一张大图。背景区、可拉伸面板、按钮、动态覆盖层、需要独立动画的装饰和可复用组件仍应保持各自边界；可拉伸底图使用九宫格资源并在 manifest 中记录边距。
- 一个按钮的底图、描边、阴影和固定装饰在每个状态内合成一张纹理；`normal`、`pressed`、`hover`、`disabled` 等作为同一个 `Button` / `TextureButton` 的状态资源，不得做成多组常驻按钮节点。按钮上的动态文字继续使用 Godot `Label`。
- 动态名称、数值、说明、多语言文字和运行时图标只在 Godot 专用 PSD 中保留 `txt_`、`label_`、`slot_` 或 `icon_` 占位和几何，不得作为带内容的图片导出。`hit_` 只描述点击范围、`ref_` 只保留参考，两者均不导出为可见资源。
- 重复元素按职责处理：纯装饰重复项合并到所属静态纹理；固定且需要分别交互的元素保留独立输入节点；数据驱动的简单 Grid / List 每项使用一个可见且可输入的单叶子节点；具有多层图片、独立动画、状态或公开接口的重复项才实例化完整 prefab。
- Godot 专用 PSD 的图层与资源文件统一使用英文 ASCII `snake_case`，Godot 节点使用对应的 `PascalCase`。manifest 必须记录源图层、处理分类、导出资源、目标节点/组件、画布与矩形、状态和九宫格边距；禁止写入本机绝对路径或依赖项目外文件。
- 节点减量的标准不是 `.tscn` 行数，而是每个节点都有明确的显示、布局、数据、输入、动画或组件职责。把节点从 `.tscn` 移到 `_ready()` 动态创建、把碎节点藏进无职责 prefab，或删除仍承担语义/交互职责的父节点，都不算完成 Godot PSD 整理。
- 既有 PSD 直转 Scene 不得按节点数量批量删除。必须先从修改前提交或可靠备份建立原节点到 Godot 专用 PSD 渲染单元的映射，再逐个 Scene / prefab 整理；节点删除、改名、移动或重新挂载仍须遵守本项目的使用者确认规则。
- 对既有界面做仅结构整理时，Godot 专用 PSD、导出资源和 Scene 必须保持 1920×1080 下的视觉、点击范围、状态和动画结果不变，并执行本项目规定的五个真实操作前后截图及像素比较；新视觉版本则以已确认的 PSD 整屏预览、状态预览和真实 Godot 窗口为可见验收依据，同时继续通过结构、契约和交互检查。

## 预制体节点结构约束

- `.tscn` 是复杂美术层级、静态构图和可独立组件的装配依据；展示脚本只能配置这些已有结构，或在下述明确边界内创建数据驱动的单节点叶子对象，不得在代码中重画复杂美术层级。
- prefab 必须同时拥有可独立验收的美术表现（图片或完整可见 UI）与挂在根节点的独立表现代码；只有布局容器、Slot、Anchor、HitArea 或尺寸数据的结构包装不是 prefab，不得为了消除几行重复节点而拆成额外 `.tscn`。
- Scene 与 prefab 的节点结构应按职责最小化，不以 `.tscn` 中的实例数量本身判断重复。数量固定，且位置、尺寸、层级、点击范围或逐实例资源覆盖属于最终美术构图的同构元素，可以在 Scene 中直接保留多个可见实例；纯布局与点击结构保留在所属 Scene 中，不包装成 prefab。
- 统一 Grid / List 中没有独立美术层级和代码职责的条目，Scene 只保留已有容器；页面根脚本可以按 Snapshot 创建一个 `Button`、`TextureButton`、`TextureRect` 或其他单个叶子 `Control`，直接承担显示与输入。此类条目不得再包一层 Slot、Anchor、HitArea，也不得为了代码复用新增 `.tscn` 或子节点树。
- 数据条目包含多层图片、独立动画、稳定公开接口或自身表现状态时，才从已有 prefab 运行时实例化；数量较大、生命周期短的复杂实例应按需创建或池化。元素子弹、伤害数字、临时特效等对象不得按可能数量手工堆入 `.tscn`。
- 内存和性能判断以实际运行时节点数、脚本实例数、资源共享情况与 Profiler 结果为准。把相同实例从 `.tscn` 移到 `_ready()` 创建不视为优化；减少实际节点、让一个叶子节点同时承担显示与输入、仅为有数据的槽位挂载内容、按需创建、池化或轻量化完整 prefab 才算优化。集合缩略图若只显示一张贴图，不得仅为复用而挂载包含战斗状态、攻击动画和特效脚本的完整战斗 prefab。
- 元素子弹等仅在纹理、元素类型、颜色、数量或位置上不同的同类对象，必须共用一个通用节点或 prefab；脚本负责创建所需实例并配置差异，不得分别维护 `fire`、`water`、`earth`、`wind` 等重复节点树。
- `new()`、`add_child()` 只允许用于上述单节点叶子对象，或挂载已有复杂 prefab；单节点必须直接加入 Scene 中已有的明确容器，并由页面或职责根脚本统一创建、配置、连接和释放。禁止连续创建父子节点、透明输入层或未经美术定义的多节点 UI、飘字、弹道和特效结构。
- 未经使用者明确要求或确认，不得擅自新增、删除、改名、移动或重新挂载 Scene 与 prefab 中的节点。
- 如果实现需求确实缺少节点，必须先通知使用者，说明拟新增节点的名称、类型、父路径和用途；得到确认后才能修改节点结构。
- 测试和 Mock 适配必须服从已有预制体结构，不能为了让测试通过而反向添加预制体中不存在的节点。

## 脚本挂载与可视编辑约束

- 每个可独立编辑和复用的 Scene 或 prefab，其展示脚本应挂在该 Scene / prefab 的根节点；`Board`、`TopInfoBar`、`CellDetail` 这类承担一组完整职责的分组，脚本挂在对应分组根节点，不得把整组逻辑下沉到普通子节点。
- 正式美术 Scene 的二级职责节点本身可以是 `TextureRect`、`TextureButton`、`Sprite2D` 等图片节点并挂载分组脚本；该节点的图片可以按状态不显示、暂时隐藏或保持空纹理，图片当前是否可见不影响它作为职责根节点和脚本入口。
- 二级职责图片节点没有纹理或当前不显示图片时，挂载脚本不得设置 `size`、`custom_minimum_size`、offset、位置或其他静态几何，也不得用代码给空图片制造占位尺寸；需要的基准几何必须明确保存在 `.tscn` 中，不承担显示、布局或输入职责的空图片不得仅为承载脚本而人为设置尺寸。
- 每个正式美术 Scene 默认只保留 `1` 个页面级根展示脚本；确有第二个完整页面职责时上限为 `2`。Scene 可以实例化带根脚本的 prefab；prefab 内部脚本与上述完整职责分组脚本不拥有页面路由权，也不得持有 Session。
- `app/game.tscn` 的 `game_controller.gd` 是唯一 Session、Command、持久化和 Feature Scene 生命周期拥有者；`ThreeChoiceScene` 与 `BattleArtScene` 的根展示脚本只接收 Snapshot、绑定已有节点、播放表现并发送操作请求，不得自己创建、持有或直接调用 GameSession。
- 普通图片、文本、容器、按钮和纯布局节点默认不挂脚本。只有子节点本身是可独立复用的 prefab，或者确实拥有独立状态、动画、输入处理或稳定公开接口时，才可作为例外挂脚本；普通点击信号由职责根节点的脚本统一连接和处理。
- 脚本职责固定按 `页面根脚本 -> 职责组根脚本 -> prefab 根脚本 -> 普通图片/文本/按钮` 分层。前三层只在节点确实拥有独立状态、输入、动画、生命周期或稳定公开接口时挂脚本；最后一层默认不挂脚本，由最近的职责根统一配置、连接信号和播放表现。不得把同一职责拆散到多个普通子节点脚本，也不得让 prefab 根或职责组根取得页面路由、Session 或 Command 裁决权。
- `ThreeChoiceScene` 是三选一界面唯一的页面级展示脚本，负责接收 Snapshot、切换路线/商店/背包展示状态、创建允许的数据驱动简单槽位、连接普通按钮并向 `game_controller.gd` 发送语义操作请求。`Background`、`StageHost`、`PartyGrid`、`BagButton`、`ItemHoverHighlight`、`OverlayHost`、`ItemBar`、`ItemGrid` 和 `BagMask` 等显示、布局、容器或普通按钮节点不挂脚本；`BagButton` 的点击由页面根统一连接。
- 三选一路线卡只有 `RouteCard` prefab 根挂脚本，负责卡片数据、悬停、选中动画和点击信号；其 `Portrait`、`KindIcon`、`StateFx`、`IncenseFx` 等普通图片不挂脚本，即使使用 Tween 也由 `RouteCard` 根脚本控制已有子节点。`PetDetail`、`BazaarDetail` 等详情 prefab 因拥有独立展示接口和开关状态，脚本挂在各自 prefab 根，并由页面根按需实例化或展示。
- `BattleArtScene` 页面根只负责 Snapshot、语义操作请求、Trace 调度以及 `Board`、`Hud`、详情层等职责组之间的协调。格子池、单位池、棋盘尺寸、选择和拖拽归 `Board` 根脚本；Trace 特效的创建、播放和释放生命周期归 `VfxHost` 根脚本。将这些职责从页面根迁入已有 `Board` 或 `VfxHost` 是脚本职责调整，不构成新增节点的理由。
- 战斗 HUD 由 `Hud` prefab 根提供统一对外接口；`TopInfoBar`、`ActionPanel`、`DirectionDrawer` 只有在分别拥有完整状态、信号、动画或公开方法时才在各自已有职责根挂脚本。`PrimaryActions`、`AutoArrangeButton`、`BeginTurnButton` 以及普通图片、Label、Grid、Host 不挂脚本，按钮点击由最近的职责根统一连接。
- `BattleCell`、`BattleUnit` 以及独立可复用的子弹、伤害数字、临时特效，只在确有自身状态、输入、动画或生命周期时于 prefab 根挂脚本；其内部普通图片和文本不挂脚本。简单的一次性数据驱动叶子仍遵守单节点规则，不得为了挂脚本包装成无职责 prefab。
- 上述职责划分以现有 Scene / prefab 节点为基础。仅做脚本职责整理时，不得新增、删除、改名、移动或重新挂载节点，也不得改变视觉、点击范围、状态、动画和对外语义操作结果；若现有节点确实无法承载所需职责，仍须先按本文件规定向使用者说明并取得确认。
- 静态 UI 的节点层级、锚点、位置、尺寸、缩放、裁切和点击矩形必须明确保存在 `.tscn` 中，并能在 Godot 编辑器里逐节点选中、查看和调整；不得以 `_ready()` 后才由脚本赋值的方式把静态布局或点击区域藏在代码中。数据驱动单节点条目的统一尺寸是例外，但尺寸必须集中在一个常量、Theme 或导出配置中，由已有容器负责排布，禁止分散硬编码。
- 静态点击、悬停和拖拽必须落在 Scene 树中已有且尺寸明确的 `Control` / `Button` 节点上；数据驱动列表则由运行时生成的叶子 `Button` 自身同时承担可见内容和输入。如果静态视觉贴图的透明留白不适合作为点击范围，应由使用者确认并在 `.tscn` 中放置可选中的显式 `HitArea` 节点；禁止脚本临时创建透明点击层，也禁止用超出可见内容的大型父节点偷偷拦截输入。
- 展示脚本不得在运行时覆盖已创作静态节点的基准尺寸和位置。仅对同构模板实例的排布、明确的视口适配或动画过程允许动态变更；动画或临时状态结束后必须回到 `.tscn` 定义的基准几何。

## Scene 路由约束

- 跨 Scene 操作固定走 `已有 Button -> 当前 Scene 根脚本发送语义 Command -> GameSession 返回 Result/Snapshot -> Game 根据 Snapshot 选择 Feature -> SceneRouter 挂载到 FeatureHost -> 目标 Scene 渲染 -> presentation_settled -> Game 释放旧 Feature`。
- Button、普通子节点和美术 Scene 不得知道目标 `.tscn` 路径，不得直接 `change_scene*()`、操作 `FeatureHost`，也不得发送“加载某个页面”的路由请求。
- `SceneRouter` 只实例化、挂载和释放；它不读取 Snapshot、不判断 phase、不执行 Command。Snapshot 到 Feature 的映射只写在 `game_controller.gd`。
- 同一 Scene 内的背包、抽屉和遮罩显隐由当前 Scene 根脚本处理；当前 Scene 已拥有的详情 prefab 由根脚本传入展示数据。这两类操作不经过 SceneRouter，也不改变权威玩法阶段。
- 完整规范见 `docs/SCENE_ROUTING_STANDARD.md`。STS2 只作为“常驻总壳、Action/Command 裁决、容器切 Scene”的职责参考，不复制其专有实现或脚本密度。

## Godot 图片尺寸锁定

- Godot 运行时会按照图片的实际像素尺寸参与显示和控件最小尺寸计算；接入图片后，静态控件必须在 `.tscn` 中显式锁定显示宽高，不能只依赖纹理原始尺寸或编辑器预览。只有数据驱动单节点条目、运行时生成的同构 prefab 实例或明确的视口适配，才允许由展示脚本依据集中配置计算尺寸。
- 需要缩放、裁切或使用透明留白图片时，必须同时固定 `TextureRect`、`TextureButton` 或承载节点的尺寸与位置；抽屉、按钮、点击区域和相邻图片不得因源图片画布大小改变而漂移。
- 涉及图片布局的交付必须在项目的 1920×1080 基准画布中运行检查，确认实际 Godot 窗口中的位置和大小后再验收。
- 所有新增、替换或重新接入战斗界面的角色、宠物、单位和精灵图片，都必须在真实 Godot 战斗界面中逐个检查三项：`1.` 显示大小是否与格子、同类单位及画面比例协调，不得过大或过小；`2.` 显示位置与视觉中心、格子站位及前后层级是否正确，不得偏移、遮挡异常或越出预期区域；`3.` 地面阴影是否紧贴脚底接触点，阴影中心、宽度和上下距离是否与角色站姿一致，不得悬空、脱离脚底或压进身体。三项任一不合格都必须调整并重新运行检查，不得直接交付。

## 精灵帧动画制作要求

### `generate2dsprite` 强制验收

- 任何精灵、角色、宠物、单位、逐帧动画、动画帧表、视频拆帧、透明帧序列或 GIF 的制作、修改、清理、转换和交付，都必须在处理前加载并遵守项目可用的 `generate2dsprite` skill，并以该 skill 的工作流完成最终验收。无论素材来自 `image_gen`、视频、PSD、现有 PNG、Aseprite/LibreSprite 还是本地脚本，都不得跳过；项目内既有导出脚本或 QA 报告只能作为执行工具和证据，不能代替 skill 验收。
- `generate2dsprite` 验收必须同时包含：逐帧 PNG 原尺寸检查、至少 `4x` 最近邻放大检查、帧序列总览、浅色/深色/真实界面背景下的透明边缘检查、慢速逐帧 GIF、最终速度 GIF、画布尺寸与四边安全边距、透明通道、主体连通性、身体中心/脚底基线/视觉锚点、帧顺序与时长、GIF disposal/清帧方式，以及将最终 GIF 逐帧解码后与本轮交付 PNG 的透明度和可见像素进行一致性比较。身体帧默认只保留一个主体连通区域；确需分离的子弹、烟雾、拖尾或其他 FX 必须作为独立资产验收。
- 任一帧存在白边、色边、绿边、视频压缩晕边、半透明脏点、脱离主体的噪点、烟雾或其他未要求的附着效果、画布边缘接触、裁切、轮廓缺口、断肢、重复肢体、锚点漂移、残影、错误画布、错误缩放、GIF 帧数/顺序/尺寸/时长/disposal 不匹配，或 GIF 解码结果与交付 PNG 不一致时，一律标记为 `BLOCKED`。必须修复后从 `generate2dsprite` 验收起点重新检查，不得用缩小预览、快速播放、自动化报告显示通过或“肉眼基本正常”代替。
- 只有上述视觉检查和自动检查均通过后，才可写入最终版本目录并报告完成；QA JSON、pipeline metadata 和其他非图片验收记录必须放在 `art/manifests/` 对应版本目录，`art/images/` 只保留交付图片及相邻 Godot `.import` 文件。未通过版本不得覆盖已确认成果，也不得留在正式交付路径冒充候选完成稿。

- 精灵动画必须制作成可逐帧验收的真实帧动画；每帧保存为独立透明 PNG，并同时提供帧序列总览图和无重影的 GIF 预览。预览 GIF 的每帧必须完整覆盖画布并正确清除上一帧。
- 同一只精灵的所有帧必须保持原角色的像素风格、轮廓、配色、五官和材质一致。小幅动作优先从同一母帧做确定性的局部像素编辑，不得因重新生成整只精灵造成角色漂移。
- 所有帧使用相同尺寸的透明画布，并锁定身体中心、脚底基线和视觉锚点。除动作设计明确要求的位移、起跳或回弹外，不得让整只精灵左右乱晃、上下漂移或出现重影。
- 锁定头部、躯干或其他静态部位时，禁止按固定水平线、垂直线或矩形区域把不同帧硬切后拼接。不同帧的轮廓、衣服、尾巴或肢体若穿过拼接边界，会产生“一刀切”、断层、错位或突然变形；这类结果不得通过缩小总览图或加快 GIF 播放来掩盖。确需合成时，必须沿已确认部位的实际像素轮廓制作遮罩，并检查遮罩边缘与相邻动态部位在每一帧都连续连接。
- GIF 必须由本轮交付目录中的逐帧 PNG 按文件顺序原样生成，不得使用另一套缓存帧、旧版本帧或单独生成的近似动画。生成后必须逐帧解码 GIF，并与合成到相同预览背景后的源 PNG 做像素比较；帧数、顺序、尺寸、时长和每帧画面均一致才可认定 GIF 对应本轮帧序列。
- 交付前必须同时检查逐帧 PNG 原尺寸、至少 `4x` 放大图、帧序列总览图、慢速逐帧预览和最终速度 GIF。逐帧检查范围至少包括头身轮廓、衣服、尾巴、四肢连接、脚底、透明边缘、固定锚点和首尾循环；任一帧存在硬切线、断肢、重复肢体、轮廓跳变、角色漂移、绿边、残影或被快速播放掩盖的缺陷，一律 `BLOCKED`，修复并重新检查前不得交付。
- 待机动画只表现轻微而连续的呼吸起伏和清晰眨眼；脚底与中心保持稳定，闭眼帧必须能明显看出并保留足够的显示时间，不得用无目的的整体摇摆代替呼吸。
- 待机动画的最终播放速度必须慢且统一。同一战斗界面、同一套资源和同一交付批次中的全部待机动画，必须使用同一基准循环时长与统一的基础播放节奏，默认按约 `5` 秒完成一轮；不得因角色帧数不同而各自随意加速或减速。眨眼、停顿等局部关键帧可以按动作可读性延长，但不得改变整套待机动画的统一舒缓观感。确需为个别角色使用不同速度时，必须先取得使用者明确确认，并在最终速度 GIF 与真实 Godot 界面中并排验收。
- 呼吸待机的最终循环默认应保持舒缓，通常控制在约 `4–6` 秒完成一轮；不得仅通过高频逐帧切换让整只角色持续上下弹跳、左右摆动或造成画面一直在抖的观感。动作优先限制在胸腹轻微收放、头肩小幅回落以及耳朵、羽毛、尾部等附属部位的延迟随动；落地角色锁定脚底与身体中心，悬浮角色也只允许受控的轻微浮动。必须以最终播放速度在真实 Godot 界面连续观察至少两个完整循环，确认玩家不会感到画面持续跳动后才能交付。
- 所有精灵均按远程发射子弹设计，不制作跳跃扑击、冲撞或贴身攻击。攻击动作必须先有清楚的蓄力，再进入发射和回弹；只有史莱姆、软体或设定允许形变的精灵可以通过吸气让身体明显鼓起，普通四脚兽不得全身膨胀；人形精灵可采用手臂蓄力后挥手发射。
- 子弹不画进精灵攻击帧中，必须由独立的通用子弹节点或 prefab 表现。精灵帧只保留自身动作；嘴部、眼睛、手部等局部变化也必须保持硬边像素风格，不得混入平滑矢量边缘。
- 攻击动画中的关键姿势必须保留足够帧时长；蓄满帧和张嘴发射帧不得一闪而过。嘴型变化需要可读，张嘴后应等待身体完成发射回弹再闭嘴。
- 移动动画只表现走过一个格子的节奏，不制作奔跑。史莱姆采用蓄力弹跳，人形采用脚步配合摆臂，四脚兽必须使用四肢交替行走；跨格位移由根节点处理，帧内动作不得额外造成位置漂移。
- 按“单只精灵、单个动作”分阶段制作和交付；当前动作得到使用者确认后，才能继续该精灵的下一个动作或下一只精灵。

### 兽形远程攻击帧制作复盘

- 普通四脚兽的吸气蓄力优先使用“闭嘴收紧 → 整颗头围绕颈根后仰 → 仰头停顿 → 头部回落并立即张嘴 → 张嘴发射停顿 → 闭嘴恢复”的分镜。不得用拉长、压扁头部或放大全身代替后仰。
- “仰头”必须是整颗头的空间姿势变化：头顶、耳朵、宝石、脸部、下巴和外轮廓要一起围绕颈根运动；只在固定头壳内部上移五官、改变脸部透视或压缩脸高，均不算仰头。
- 头部后仰时身体、衣服、项圈、尾巴、四肢和脚底基线保持固定。颈根是动作支点，头部与固定项圈之间不得出现透明裂缝，也不得让身体随头一起漂移。
- 参考视频或关键姿势图时，先确定“原始闭嘴帧、最大后仰帧、回落张嘴帧”三个关键姿势，再补停顿和恢复帧。得到使用者确认的关键姿势必须保存为项目内的版本化源帧，后续直接复用，不得重新生成近似版本。
- 使用者提供的嘴型草图只用于确定嘴巴中心、宽高、朝向和大致轮廓；正式帧必须用角色已有的黑色轮廓、口腔暗红和舌头粉色重新绘制成 1—2 像素阶梯的硬边，不得直接保留平滑椭圆、抗锯齿边缘或草图中的临时颜色。
- 修改嘴部前必须先清除原嘴、笑线、胡须点和鼻子下方残留黑点，再从相邻脸部像素补回底色。补色需贴合周围明暗变化，不得用一整条固定颜色形成竖向或横向色带；放大检查后还要回到游戏实际尺寸确认可读性。
- 嘴巴从闭合变为发射时，可以在头部回落的同一帧直接张开；不得额外加入与角色风格不符的嘟嘴帧。张嘴发射姿势至少保留一个重复帧或等效停顿时长，使嘴型清楚可见。
- 首帧和尾帧如果都是恢复后的闭嘴姿势，必须使用同一张已确认源帧并进行逐像素一致性检查，不能凭肉眼重画两张近似帧，否则循环时会出现嘴线、色块或轮廓跳变。
- 每次局部修改都要核对可见像素差异范围，确认变化只发生在计划区域；同时检查每帧尺寸、透明通道包围盒、身体中心、脚底基线以及首尾帧一致性。GIF 必须使用正确清帧方式，确保上一帧不会残留形成重影。
- 版本迭代不得覆盖已确认成果。每轮输出独立的版本化帧目录、总览 PNG 和 GIF；生成脚本只能引用项目内相对资源，禁止把桌面、临时目录或其他本机绝对路径写进交付物。

### 人形远程投掷攻击帧制作复盘

- 人形精灵的挥手或投掷不能只让拳头、手掌或一块手臂贴图独立移动。自然动作必须表现完整发力链：“双脚站稳 → 膝盖或重心轻微下压 → 躯干向反方向预备 → 肩膀带动上臂后收 → 蓄满停顿 → 肩膀先启动 → 上臂、前臂和拳头依次甩出 → 躯干跟随 → 手臂向下或跨身完成随动 → 回正恢复”。
- 制作前先确定“原始站立帧、反向蓄力帧、最大后收帧、挥出过渡帧、释放帧、随动帧、恢复帧”七类关键姿势，再根据可读性补首尾过渡。最大后收帧必须有明确停顿；挥出过渡应明显短于蓄力帧，释放帧和随动帧仍需保留足够时间看清动作方向。
- 脚底基线在整段动作中保持一致；膝盖压低、躯干侧转和重心变化属于角色内部姿势，不得把整只精灵当作根节点左右平移或上下漂移。跨格移动仍由运行时根节点负责，攻击帧自身不产生额外位移。
- 肩、肘、腕和拳头必须在每一帧保持连续、可信的连接。不得把一整块扁平手臂围绕单点机械旋转，也不得只平移拳头或前臂后留下肩部裂缝；这会造成脱臼感、漂浮感，并让拳头纹理、尖角方向和透视关系错误。动作涉及明显弯肘、转肩或透视改变时，必须重新绘制连贯关键姿势，不能用刚性贴图变换代替关节运动。
- 岩石巨拳、重甲手臂等高重量角色要保留重量感：蓄力时身体应向拳头运动的反方向压住，挥出时肩和躯干先行，拳头最后加速，释放后身体继续跟随而不是立刻弹回。允许释放帧出现受控的拳头透视放大，但不得改变角色材质、尖角结构或把拳头画成新的物体。
- 动作参考图只用于分析重心、关节顺序、动作弧线和节奏。原角色母帧是五官、轮廓、配色、材质与比例的身份依据；不得把参考图中的武器、近战命中、冲撞、光效、拖尾或其他角色设计带入远程发射动画。精灵帧仍不包含子弹，释放点只由挥手姿势表达。
- 当局部确定性像素编辑无法自然表现肩肘透视时，可以先依据参考图制作或生成整套关键姿势草案，但草案只能作为动作结构来源，不能直接视为完成稿。必须逐帧检查角色漂移，校正脸、眼睛、头部花纹、岩石纹理、尖角轮廓、手脚数量、身体比例和像素硬边；不合格关键帧必须重绘或舍弃，禁止因为动作流畅就接受角色身份变化。
- 动作草案使用绿幕或其他中间背景时，交付前必须移除背景并将透明边缘恢复为硬边像素；逐帧确认没有绿边、半透明杂点、邻帧串色或裁切。所有帧必须使用同一透明画布，脚底包围盒底边一致，首尾恢复帧直接复用同一张已确认母帧并逐像素一致。
- 为避免首帧直接跳到大幅侧身、尾帧突然弹回正面，可以在原始母帧与最大动作之间加入回正或转身过渡，但这些过渡仍须保持角色身份一致。不得用连续重复的无变化帧凑数量；停顿优先通过帧时长表达。
- 每轮按版本保留帧目录、总览 PNG、GIF、使用到的项目内源帧与可复现脚本。动作参考的本机临时路径不得写进脚本；需要长期复用的参考或生成草案必须复制到项目内版本化目录，并明确区分“身份母帧”“动作参考”和“中间草案”的用途。

## 战斗棋盘结构参考图像素一致性

- 当使用者要求战斗棋盘结构参考图与实机“一模一样”“像素一致”或指定某张战斗截图为准时，禁止使用生成式 AI 补图、重绘棋盘、猜测行列、按目测估算坐标，或用近似梯形、等距网格替代正式几何。
- 这类任务的唯一真相源是当前目标版本实际使用的 Godot 渲染链：`core/battle/board_dimensions.gd` 中固定的 `8x8` 维度、`battle_art_scene.tscn` 中的 `Background` 与 `CellHost` 基准几何、`battle_board_layout.gd` 中无透视的等大矩形布局，以及 `terrain.tscn` / `battle_cell.gd` 中的轻薄白线颜色、透明度、线宽、抗锯齿和共享边重复绘制结果。必须由相同节点、相同脚本和相同 Godot 渲染器导出，不能在外部图片工具中重新模拟。
- 当前项目的战斗棋盘固定为 `8x8`：`8` 列、`8` 行、横纵各 `9` 条边界线、共 `64` 格；运行时 `Board/CellHost` 必须实际创建并显示 `64` 个等大格子，`core/battle/board_dimensions.gd` 的当前默认值与合法范围也必须保持 `8x8`。旧 Snapshot、历史兼容数据、旧截图或既有代码中出现的 `8x7` 只视为待迁移的历史数据，不得作为当前结构参考图、Scene 装配或验收基准；旧正式导出数据不得手写补行、移动单位或猜测坐标，必须由项目方重新走正式导出链刷新。导出与验收记录必须明确写明列数、行数、边界线数和总格数。
- 结构参考图必须从与目标相同的 `1920x1080` 入口导出只含背景与棋盘的真实稳定帧；隐藏角色、HUD、高亮和文字时，只能切换既有节点可见性或使用隔离导出场景复用正式节点，禁止改动棋盘节点结构、坐标或绘制参数。
- 交付前必须检查画布尺寸、棋盘四边像素坐标、首行与末行位置、前景/底边留空、每一条横纵边界、线色、透明度、线宽、抗锯齿和共享边叠加结果。仅检查代码循环次数、肉眼数格或文件名包含 `8x8`，均不算通过。
- 必须把干净导出图与同入口、同尺寸、同稳定帧的实机棋盘图做像素对齐比较；对角色和 HUD 遮挡区使用同一运行时另导出的棋盘-only 基准，不得用生成式修补结果作为基准。棋盘覆盖区域的差异像素数不为 `0` 时一律 `BLOCKED`，不得交付，也不得声称“已确认”“完全一致”或“可以直接使用”。
- 本次错误复盘：错误原因是先使用生成式 AI 猜测格子，再用目测坐标近似重画，没有先读取正式逐行像素坐标，也没有完成像素差异为 `0` 的验证便交付，导致行列数错误、透视错误和前景留空错误。以后遇到像素一致要求，必须先定位真相源、走真实 Godot 导出、完成零差异验证，三个阶段缺一不可。

## 精灵动画正式目录确认门禁

- `art/images/shared/pets/animations/` 是呼吸（运行时名称 `idle`）、移动（`move`）和攻击（`attack`）逐帧 PNG 的正式受保护目录，不是生成过程目录、候选稿目录或临时预览目录。
- 只有使用者在对话中明确确认的“单只精灵 + 单个动作 + 指定版本”才允许进入正式目录。确认一个动作不代表确认同一精灵的其他动作，也不代表确认后续重试、修订版或变体；每个新增版本都必须重新取得明确确认。
- 生成、编辑、后处理和 QA 阶段的所有素材必须先写入 `output/sprite_animation_candidates/<pet>/<action>/<version>/` 或本轮独立候选目录。任何生成脚本、处理脚本或工具不得把未确认输出、原始生成图、绿幕图、源视频帧、过程帧、放大检查图、总览图、GIF、QA 报告或 `.import` 写入正式目录。
- 正式目录只保留已确认并供 Godot 运行时读取的透明逐帧 PNG、该确认版本对应的最终 GIF、帧总览图及相邻 `.import`。原始生成图、绿幕图、源视频帧、过程帧、放大检查图、慢速检查 GIF 和 QA 报告继续保存在候选、交付或 manifest 目录；它们通过验收并不自动获得进入正式目录的资格。
- `art/manifests/shared/pets/animations/approved_sprite_animation_manifest.json` 是正式目录的唯一准入白名单。只有在使用者明确确认后，才能把对应精灵、动作、版本、帧数和正式相对路径加入该清单，再把完全相同的已确认帧移入正式目录。不得因为 QA 通过、脚本生成成功、已经提交 Git、曾在界面中试播或已经接入运行时，就自行推定为使用者已确认。
- 当前白名单内容属于动态工程状态，必须读取 `art/manifests/shared/pets/animations/approved_sprite_animation_manifest.json` 的实际内容；不得把历史对话或旧文档中的固定数量当作现状。无论当前已有多少项，今后任何新增或替换仍必须以本轮使用者对“单只精灵 + 单个动作 + 指定版本”的明确确认为准。
- 生成工具可以只读使用正式目录中的已确认母帧，但输出路径必须仍在候选目录。若旧工具把输出路径硬编码到正式目录，运行前必须先改到候选目录，不得先生成到正式目录再搬走。
- 每次改动精灵动画资源后必须运行 `python tools/check_approved_sprite_animations.py`。出现未登记目录、额外帧、缺帧、非 PNG 过程文件或运行时清单引用未登记帧时一律 `BLOCKED`，不得交付或声称整理完成。

## 完成与验收

- 每次美术/UI 修改必须至少覆盖 `5` 个不同且有意义的真实操作点；重复同一点击、无状态变化的空操作或只拍静态首屏不能凑数。每个操作都要在修改前保存一张 `before`，修改后在相同入口、Mock Snapshot、窗口/视口尺寸、1920×1080 基准、缩放、操作步骤和稳定帧保存对应 `after`，因此每轮至少保存 `5` 组、`10` 张截图。开工前漏截时，必须从修改前提交或可靠备份建立隔离项目补拍，不能把改后画面当成 `before`。
- 每组使用 `python3 tools/qa/compare_screenshots.py <before> <after> --report <json>` 做像素级比较，并用 `python3 tools/qa/compare_operation_screenshots.py <manifest.json> --report <summary.json>` 汇总；只有至少 `5` 个不同操作的每组截图都尺寸一致且差异像素数为 `0` 才通过。少于 `5` 组、缺任一截图、采集条件不一致、缺报告或任一组存在像素差异，一律 `BLOCKED`，不得交付或提交“通过”。截图、操作清单和报告默认只作为本机证据，不加入 Git。
- 每次改动后运行 `./tests/verify_ui_mirror.sh`，确认独立项目结构完整且文件类型没有串目录；默认检查不需要原项目。
- 运行 README 中的独立项目契约 smoke，确认 Mock Snapshot 和装配链仍可工作。
- 涉及布局、交互或动画时，必须在真实 Godot 窗口检查；headless smoke 不能代替可见结果。
- 美术侧只验收本项目中的可见结果和 Mock 交互，不负责正式玩法、战斗结算或存读档验收。
