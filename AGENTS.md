# godot-latest AI 工作约定

本文件适用于整个仓库。用户当前指令优先于本文；进入子目录后，如存在更具体的 `AGENTS.md`，以更具体的规则补充本文。

## 开工顺序

1. 读取 `tasks/ai/README.md`、`tasks/ai/QUEUE.md` 和 `tasks/ai/STATUS.md`。
2. 读取 `tasks/doing/*.md`，检查其中的 `write_scopes`、`exclusive_files` 和 `existing_wip`。
3. 运行 `git status --short --untracked-files=all`，所有既有修改均视为用户或其他任务资产。
4. 修改前建立或更新一张任务卡，声明目标、停止条件、写入范围、独占文件和验证方式。

## 持续工作边界

- 只有用户明确启动持续目标或后台巡检时，才从 `tasks/ai/QUEUE.md` 自主选择工作；普通对话只处理用户当前请求。
- 每轮只推进一个可独立验收的任务。优先级依次为：阻断启动/测试的问题、可复现玩家问题、确定性回归、已确认技术债、纯建议。
- 先定位真正责任层，再修改：策划数据、权威状态、Command/Result/Trace/Snapshot、表现层、UI 和工具层不得混为一谈。
- UI 只发送语义意图并渲染 Snapshot/Result/Trace；正式权威仍为 `GameSession -> Command -> YsbzsState -> Result/Trace/Snapshot`。
- 不为了“保持工作”而制造重构、功能或内容；没有可靠下一项时，更新巡检报告并停止本轮。

## 冲突与安全

- 写入范围与既有任务卡、未提交文件或其他工作槽重叠且无法确认归属时，执行 `FILE_CONFLICT_STOP`，记录冲突并请求用户处理。
- 不覆盖、不回退、不顺带格式化、不提交不属于当前任务的改动；精确暂存，禁止 `git add -A`。
- 未经明确授权，不推送、不部署、不发布、不改玩法方向、不改正式策划数据、不删除资产或历史、不扩大网络与凭证权限。
- 上游 workbook/CSV 有冲突时不得绕过数据真相源直接改 Godot 下游快照。
- 真实窗口验收使用本 AI 独立虚拟屏、项目副本、`.godot`、`user://`、进程和输出目录；不得关闭或清理其他 AI/用户的 Godot、录屏或显示槽。

## 完成标准

- 先跑与改动直接相关的专项测试；风险较高时再跑 `python3 tools/qa/run_qa.py --suite fast` 或对应更大套件。
- 涉及可见 UI 时必须从正式入口进行真实窗口验证；连续时序风险才录屏。
- 完成后在任务卡中记录修改、验证命令、证据路径、遗留风险和下一步，并更新 `tasks/ai/STATUS.md` 与 `tasks/ai/QUEUE.md`。
- 每轮收尾由 AI 根据改动是否完整、可独立解释、已达到验收条件且不混入其他 WIP，自主判断是否应形成 Git 提交；满足条件时精确暂存本任务文件并自动提交，无需再次询问。改动仍在进行、验证未通过、存在文件归属冲突或只能连同他人 WIP 提交时不得提交，并在交付说明中写明原因。
- 只有验收条件真正满足才标记完成；测试失败、视觉未验证或仍有已知阻断时不得宣称完成。
