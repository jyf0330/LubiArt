# LubiArt 当前执行基线

本文件是每轮快速恢复用的精简索引。详细项目规范以仓库根 `AGENTS.md` 为准；`conversation-synthesis.md` 保留标准演进、历史证据和逐轮审计，不再要求每轮全量加载。

## 每轮启动

1. 完整读取项目 Skill、仓库根 `AGENTS.md` 和本文件。
2. 用本轮需求关键词、相关文件路径、功能名和已知问题名检索 `conversation-synthesis.md`，读取所有命中条目的完整上下文；同时检查当前相关文件和 Git 状态。
3. 只有发生规则冲突、跨主题历史审计、检索不足以确认现状，或使用者明确要求时，才完整读取 `conversation-synthesis.md`。
4. 当前使用者最新明确要求优先，其次是现行 `AGENTS.md`，再其次是项目文档和较早历史。资源数量、路径、白名单和启用状态始终以当前工作区为准。
5. 每轮结束仍须把请求、结果、标准变化、工程变更、验证和阻塞简洁回写到 `conversation-synthesis.md`；如果形成新的长期规则或活动阻塞，还要同步更新本文件。回写失败则本轮为 `BLOCKED`。

## 风险分级验收

风险按实际影响范围判断，不能为了省时主动降级；使用者可随时要求提高等级或完整验收。

### L：低风险

- 适用：不改变节点结构、数据、交互、图片或动画的文案、颜色、间距、静态样式和小范围参数调整。
- 至少验证 `1–2` 个直接相关且有意义的真实操作或状态。
- 迭代期只跑专项检查，完成前集中做一次最终验证。

### M：中风险

- 适用：布局变化、交互行为、UI Presenter/Controller 的局部表现逻辑，以及可能影响相邻状态但不跨越 Session/数据契约边界的修改。
- 至少验证 `3` 个真实操作：目标流程、一个相邻状态、一个回归状态。
- 迭代期跑专项检查，完成前集中做一次最终验证。

### H：高风险与正式交付

- 适用：图片或其他美术资源、动画、节点增删改名移动或重新挂载、PSD 转换、Scene 路由、应用装配、Session/Core/Data/公共契约、批量修改、正式交付，以及使用者明确要求完整验收的任务。
- 至少验证 `5` 个不同且有意义的真实操作，并执行受影响范围内的完整门禁。
- 节点结构整理即使预期视觉不变，也属于 H 级。

## 截图和像素比较

- 只为本风险等级选定的操作保存同条件的 `before` / `after`；修改前漏截时，仍须从修改前提交或可靠备份补拍。
- 结构整理或明确要求视觉不变的修改，所选操作必须整图差异像素为 `0`。
- 有意改变视觉时，开工前声明预期变化区域；允许该区域出现差异，但预期不变区域必须为 `0` 差异，或用等价的边界化证据证明差异没有外溢。不得因预期内像素变化把已完成任务误报为 `BLOCKED`。
- 涉及布局、交互或动画时，必须在真实 Godot 窗口验证；headless 结果不能替代可见验收。

## 测试调度

- 迭代期间优先运行直接相关的专项 smoke、静态检查和真实入口操作；互不依赖的只读检查可并行，固定动画相位后再截图。
- `verify_ui_mirror.sh` 在相关 Scene、prefab、脚本、图片或 manifest 修改的最终阶段运行一次；纯文档流程修改不要求运行。
- README 全量 Mock smoke 仅在 H 级、正式交付，或修改应用装配、路由、Session/Core/Data/公共契约时运行一次；L 级不运行，M 级只有影响上述职责时才运行。
- 对已有日期和完整失败签名、且当前修改不可能影响其职责的全量失败，可以引用最新证据而不重复等待。失败签名改变、修改触及其职责、H 级或正式交付时仍须运行一次并如实记录。
- 本任务启动且超时不退出的进程按精确 PID 结束；不结束用户或其他任务的进程。

## 当前活动阻塞和已知基线

- 当前应用正式 Feature 为三选、独立商店和战斗三类 Scene。商店由 `art/scenes/shop/shop_scene.tscn` 独立承载，三选与商店共享 `art/prefabs/route/route_shared_ui.tscn`；旧 Bazaar prefab/脚本和三选开发工具栏已删除。Game 仍是唯一 Session、Command 与 Feature 生命周期拥有者。
- 商店美术往返只接受正式项目经公共 `Session -> Command -> Snapshot` 导出的有序操作与有界可见图片闭包；Mock 只离线重放该投影。正式宠物图缺少批准映射时，同步清单必须明确标记 `missing_mapping_fallback` 并复制正式界面实际使用的同一缺图替代图，不得在 Mock 端伪造宠物素材。退出后的两仓路线画面只做状态/交互验证，不声称像素一致，也不得据此复制彼此的 Scene、脚本或固定卡位。
- 最新正式项目 `C:\Users\jyf\Documents\godot-latest-latest-20260817-103552\` 的三选商店增量命令链已完成：正常本地 `CHOOSE_ROUTE(shop) / BUY_OFFER(宠物或缺失候选拒绝) / ROLL_SHOP / EXIT_SHOP / BACK_TO_ROUTE` 只校验 `stateVersion` 并返回小 Result/domain delta，不再为每次点击构造完整 Snapshot、完整状态哈希或逐命令磁盘历史；首次连接、读档、历史恢复、显式存储/回放、复杂/战斗命令和异常校准继续走完整 Snapshot/hash。正式 Vulkan 五操作为进店 `112ms`、拒绝 `14ms`、购买 `112ms`、刷新 `107ms`、离店 `13ms`；问题状态由“完整结果仍约半秒”替换为已完成约 `0.1s` 档验收。老板整合版已以提交 `6fd880cc` 发布到 `origin/codex/formal-integrated-20260817`；Git 整合基线已完成。当前 Windows headless 增量命令专项的宠物购买两次为 `108ms` / `104ms`，高于该测试 `100ms` 门槛；其他增量命令断言和三项发布专项通过，因此产品全绿验收仍保留该性能门禁缺口。
- 正式项目三选路线旧卡、离店队伍丢显和第三段红标尺寸错位均已修复：`EXIT_SHOP / BACK_TO_ROUTE` 返回路线时会按已确认 Snapshot 重新绑定共享金币 HUD、当前 roster 与 `route_options`，不再提交前一节点的旧 `option_id`，也不会丢失商店刚购买的队员。现行路线节奏为前两段普通商店/奇遇三选，第三段固定显示三张 `kind=battle` 战斗入口；三张均使用 `three_fight_logo.png` 红色交叉武器小标，并在既有 `KindIcon` authored `40×62` 矩形内等比居中缩放，不再由 `80×104` 源图撑大或溢出。选择后仍通过正式 `CHOOSE_ROUTE → GameSession → Snapshot → FeatureHost` 直接进入 `BattleArtScene`。Godot 4.7 Vulkan `1920×1080` 实窗与专项回归通过；未修改节点结构、正式日程、玩法或数据。标准 fast runner 仅因当前 Windows 用户无符号链接权限在首项测试前阻断，不属于产品失败。
- 右侧宠物信息栏已选择第三方案进入审批预览：保留现行完整信息量，以旧棕金卡片美术语言重做外观并减少 AI 感。使用者已确认黄金三星代表样本可继续扩展，现已在 `output/ui_preview_candidates/battle_pet_info_option3/` 形成青铜一星、白银二星、黄金三星、钻石四星四张同套概念预览；该确认只授权继续出图，不等于老板批准实装。老板正式同意前不得修改或接入 Godot prefab、脚本、Theme、正式图片、manifest、测试或运行时引用；现行工程实现保持不变。
- 战斗元素地块死亡格补画已修复：旧槽 2 捕获虽然没有独立 `hitEnemy` / `empty` 字段，但同一公共 Trace 中 `ELEMENT_APPLIED.deferToAttackStrike` 后的匹配 `ATTACK_STRIKE.targets` 已提供命中当刻的坐标和单位 ID。Session 旧捕获适配只按这组公共事件对账：非空单位 ID 格不增加元素，空 ID 格保留元素；不读取最终死亡/移位状态，也不处理缺少匹配攻击事件的元素变化。专项三轮数据断言、真实 OpenGL 六操作回放和独立工程门禁已通过。
- `2026-08-16` 合并后，项目级 `smoke_mock_battle_project.gd` 仍复现新单位资源、防御属性语义、旧伤害预览 ProgressBar `.text` 和 prefab 特效职责断言，并在输出后不自行退出；`smoke_battle_art_scene.gd` 仍复现 `minimal battle hierarchy mismatch`，当时为 `scene_nodes=12`、`board_children=4`、`grid_children=64`。复用该基线前必须核对完整签名和本轮影响范围，不能把不同失败笼统视为同一问题。
- `verify_ui_mirror.sh` 和动画批准检查属于动态工程状态；读取当前结果，不把历史通过或失败永久化。
- 当前正式待机白名单只保留角翼龙 `SPR_014 / idle / 001`，其既有 `4×400ms = 1600ms` 版本继续有效；其余旧待机已撤销正式资格并恢复为静态展示，等待逐只重做。
- 招聘三题版中的 `02` 功能弹窗返工已完成：使用者已确认新的日常棕木/深绿像素 UI 方向；候选包已改为功能线框、风格/尺寸说明和六个独立 RGBA 同源组件，领导参考图只使用这些输入拼装；七份 Word 已完成 `22` 页全页渲染检查，候选与领导 ZIP 已重建并通过边界检查，现行正式目录可交付。
- 后续重做待机固定交付 `16` 张 `128×128` 透明帧，只允许翅膀、耳梢、尾梢、羽毛、手指等局部小动作；脸部、五官、躯干、身体中心、外轮廓和脚底逐帧锁定。十六帧全部作为连续循环交付，不再筛减为六帧；正式速度按具体版本预览确认。
- 当前 `SPR_005` 土石莱姆重做是单资源候选例外：使用者指定 `6` 张 `128×128` 帧，只让画面右侧举起的白色小手做几像素轻微晃动，其余像素固定。Rika 已调用 `1` 次并实际消耗 `5` credits（`737 → 732`）；`hand_locked_six_frame_v1` 六帧候选的目标区域外逐像素差异为 `0`，仍待使用者视觉确认，未获正式版本批准，也不改变其他新待机的 `16` 帧默认值。
- `SPR_003` 蓝色电翼贝壳的 `identity_locked_local_motion_v1`、`identity_locked_wing_motion_v2` 均已被使用者否决；一次 Rika v3 重做实际消耗 `5` credits，余额 `742 → 737`，没有后续新增调用。Rika 原始帧提供完整翼姿态但脸和胸腹漂移；v3/v4/v5 因固定脸/身体覆盖层仍压住不同范围的内侧翼根而不合格，v6 的擦除与 v7 的局部旋转又产生像素缺口和黑色断口，均不得准入。当前返工候选为 `identity_locked_inner_root_overlay_exclusion_v8`：保留 Rika 原生完整翼姿态，把使用者最后圈出的两枚细小内侧翼根从所有固定脸/身体覆盖层和固定 QA 区中排除，只由动态翼层提供；左右最终翼根后续 `15/15` 帧均变化，五官、下腹和脚固定区差异为 `0`，十六张 `128×128` 真透明帧技术门禁通过。该版本仍待使用者视觉确认，未进入正式目录、白名单或运行时。

## 当前战斗交互基线

- 战斗 Trace 演出和输入锁定期间仍允许点击敌我精灵做只读查看：本地显示脚底黄色选中高亮并打开或切换右侧信息栏；同精灵再点、纯空地板或棋盘外未占用空白可本地取消。该阶段不提交玩法命令、不显示规划格线、不创建拖拽预览也不改变站位；演出结束后正式选择与拖拽继续沿公共契约工作。

## 长期边界速查

- 本项目是可离线独立运行的 Godot UI/美术 Mock，不是正式玩法、存档、战斗规则或策划数值真相源。
- 对外招聘面试材料不得包含或复制当前游戏的角色、宠物、场景、界面、截图、切图、数值、专有名词或其他实际内容；只使用原创虚构练习内容考察能力，但视觉语言必须与项目现有像素游戏美术一致。代表样张须先经使用者确认，再扩展成完整素材包和候选人题目。
- 招聘练习现行视觉锚点为项目已有像素资源的整体语言，而不是具体游戏内容：硬边像素轮廓、有限色阶、清楚剪影、统一光源、低随机纹理和缩小后可读。此前“云港远征”通用手绘卡通风格已退役。
- 招聘实操现行三题为 `01` 局部改稿精修、`02` 单个功能弹窗视觉设计、`03` 角色表情小稿。`02` 旧红金参考图已退役；现行方向固定为棕木、深青绿、低饱和青灰与少量旧金属细边的日常功能 UI，所有边框、标题、徽章、奖励格和按钮严格水平、左右镜像并共用同一中轴。使用者已确认代表样张，正式候选同源素材、领导参考图、Word 与两个 ZIP 均已完成替换和验收；正式目录不再 `BLOCKED`。
- 三题均已给齐完整原创虚构输入并与领导参考同源：改稿题含接近完成的透明角色原图和材质目标；弹窗题含功能线框及全部面板、按钮、徽章和奖励图标；表情题含四个完全相同的无表情角色底稿及表情目标卡。
- 每位候选人原则上只发一题，领导参考与候选人素材严格分包；候选人任务说明不得出现领导参考答案、内部使用或防误发等内部交付话术。领导审阅和候选人外发说明只使用可直接打开的常用 Office/PDF 格式，最终交付目录和压缩包不得再放 Markdown `.md` 作为需要阅读的正式说明文件。
- Runtime 数据只由 `session/mock_game_session.gd` 从 `data/mock_battle_snapshot.json` 提供；正式契约变化由项目方重新导出。
- `.tscn` 保存可编辑静态结构；复杂美术层级不得在代码中重画。节点结构变更必须先取得使用者确认。
- 后续新制或修订的精灵待机以角翼龙 `SPR_014` 为局部动作锚点：固定主体躯干、身体中心、外轮廓、脚底、脸部和全部五官，只让翅膀、耳梢、尾梢、羽毛、手指等局部部位小幅运动；局部动作必须在项目实际显示尺寸下肉眼可读，只有 `1–2px` 技术差异但看起来静止仍不合格；活动附肢必须从真实关节或根部开始连续运动，不得把根部静态覆盖后只动外端；前景活动附肢的合成顺序固定为“静态身体 → 完整动态附肢 → 仅必要的固定五官”，不得用大块静态脸/身体层压住动态根部；禁止眨眼、动嘴、表情变化，以及整只拉伸、压缩、缩放、弹跳、摆动或全身呼吸形变。
- 图片、动画、PSD、正式资源准入和真实界面验收继续执行 `AGENTS.md` 的专项严格规则；快速模式不降低这些门禁。
- 使用者已授权后续 Rika AI 在任务范围内直接调用，不再逐次确认；每次提交前仍须重读用户环境密钥、免费鉴权查余额，调用后报告实际次数和消耗。其他生成或渲染服务同样可在任务范围内直接执行。
