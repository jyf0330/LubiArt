# CHANGELOG

## 2026-08-17 — 路线卡按节点推进到固定战斗三选

- 离开商店时用已确认的路线投影重新绑定三选卡，第二、第三节点不再提交第一节点的旧 `option_id`。
- 固定战斗三选恢复使用原交付的红色交叉剑标；选择后仍通过权威 Command/Snapshot/FeatureHost 链进入 `BattleArtScene`。
- 新增连续两段普通路线/商店后进入第三段固定战斗的专项与真实窗口回归。

## 2026-08-17

- 三选商店高频命令改为 `stateVersion + Result + domain delta`；正常玩家流不再为每次点击生成完整 Snapshot、完整状态哈希或逐命令历史写盘。首次连接、读档、历史恢复、显式存储/回放、复杂/战斗命令与异常校准保留完整 Snapshot/hash。正式 Vulkan 五操作收敛到约 13–112ms（冷进店 112ms、购买 112ms、刷新 107ms、离店 13ms、无效购买拒绝 14ms）。

## 2026-07-31

- 以正式总表导出的 `31_battle_rules.csv` 和单机数据快照为准，宠物重置重新统一为开局 `0` 次、第 `5/10/15...` 回合各增加 `1` 次；同步修正代码 fallback、工作流文案和玩家/敌方重置专项测试。

## 2026-07-24

- 按最新规则恢复正式双人战斗的开局宠物重置次数：双方开局各有 1 次，之后仍在第 5/10/15... 回合各补 1 次且可累计；敌方在完整回合结束满足动态存活门槛时自动生成固定阵容，我方继续通过行动面板按钮确认。

## 2026-07-19

- 纠正 7 月 18 日把三次攻击误压成单次伤害的回归：一次形状施放仍只向全部作用格同时铺一次元素，但每个被覆盖的敌方单位恢复承受三次独立攻击力伤害；逐击防御、反伤、残血触发、伤害 trace、自动布置评分和真实执行统一读取 `settle_count = 3`。攻击力 3 对无防御目标重新表现为 `3/3/3`、总伤害 9，之后才进入元素结算。
- 敌方 `DAMAGE_APPLIED` 表现补充攻击来源平移：实际敌方棋子会沿 actor→target 方向前冲，在命中位置播放原有咬击后回到原格；伤害数字、护盾/生命更新与核心结算顺序保持不变。
- 修复自动布置只追求敌方击杀和有效伤害、忽略下一次敌方行动实际生命损失的问题：敌方击杀数仍是第一优先级；同击杀时先避免我方死亡和生命掉血，再比较有效伤害。搜索会保留行动数与站位不同的候选，避免相同行动槽副本挤掉安全阵型。读档2首战现在把捣蛋猫移至右侧攻击炽焰牛，让高护盾火绒狐承受棉悠悠三次反击；有效伤害仍为16、仍击杀1只，我方生命掉血由7降为0。
- 修复同一自动布置中叶泥泥因 AP 已分配给另外三只宠物而留在左下角的问题：有即时攻击候选的宠物也会保留少量安全待机位，并在搜索早期计算被击杀敌人的死亡爆炸。读档2首战叶泥泥现在从 `(0,6)` 前移至 `(3,2)`，不会站到炽焰牛相邻格承受8点自爆；实际回合保持 HP20，仍击杀1只、有效伤害16、全队生命掉血0。
- 修复敌方自动站位可把宠物移入 R1C8 敌方 Boss 固定格、导致 Boss 被宠物棋子覆盖消失的问题：双方存活主将格现在都从宠物自动站位候选中排除，实际应用移动时也会再次拒绝主将格。真实读档2第二回合中，冲浪鸭改为从 R2C8 移到 R2C7，进入第3回合后 Boss 仍显示在 R1C8。

## 2026-07-18

- 纠正元素施放为“一个形状、一次覆盖”：三格形状会同时向三个作用格播放投射与落点效果，不再把同一个格子的元素投射连续播放三遍；空格、敌方格和我方宠物所在格都会铺元素，我方宠物不受行动伤害。敌方占格的攻击次数由 2026-07-19 纠错恢复为三次独立攻击。
- 修复读档2点击“开始行动”后敌方宠物不行动的问题：玩家一次完成完整形状施放后，继续执行敌方攻击或移动并进入下一玩家回合。一次点击只推进一个完整回合，不会自动打完整场战斗。
- 读档2直接点击“开始行动”且作用格没有敌人时，现在会为每个空地元素写入正式 `ELEMENT_APPLIED` trace，并复用美术元素 projectile 从施放宠物飞向目标格；投射结束后播放短促元素色落点脉冲并显示元素标记，让相邻格施放也能明确看见，不再只有静态结果刷新。
- 修复读档2后直接点击“开始行动”看起来无反应的问题：核心已铺出的 `火/水/土/风` 中文元素键现在会适配到战斗格子现有的 fire/water/earth/wind 可见标记；不点自动布置时也能立即看到空地铺元素结果。
- 修复“开始行动”把“作用格内必须有敌人”误当作施放条件的问题；现在行动槽会像 H5 核心一样对完整作用格结算，有敌人则伤害并铺元素，只有空地也会消耗行动并正常铺元素。

## 2026-07-17

- 修复“开始行动”无可攻击目标时把批量试算中的最后一只宠物泄漏成玩家选中态、只留下红色作用格的问题；批量出击现在恢复点击前的真实选择，并明确提示先调整站位、方向或行动槽。规则仍与 H5 一致：不自动移动、不重新摆位。
- 叶泥泥等 `settle_count = 3` 行动改为三次真实攻击与三段动画；每段基础伤害等于面板攻击力，品质额外伤害仍只加一次。所有我方攻击完成后，动画队列等待 1 秒，再统一结算怪物所在格达到 3 层的元素伤害；玩家格不参与本次结算。
- 纠正叶泥泥伤害口径：`settle_count = 3` 表示每个作用格结算三次；攻击力 3 对无防御目标总伤害恢复为 9，不再错误压成一次 3 点伤害。
- 自动布置收敛为一个确定的全队方案，第一目标比较本回合敌方宠物击杀数量，击杀数相同时再最大化实际有效伤害；因此能完成一次击杀的低伤害方案必定优先于未击杀的高伤害方案，最终仍以固定签名保证重复点击不漂移。
- 修复远距离阵型连续点击“自动布置”会把上次结果当成新移动起点的问题；自动布置第一次即搜索全盘合法空格以取得击杀/伤害最大阵型，战斗状态未变化时直接复用已应用方案，手动拖拽仍保留移动力限制。

## 2026-07-16

- 自动布置改为按实际行动槽 AP、技能、品质与元素结算预览全队伤害，开始行动会执行规划好的槽位和方向；无即时目标时保留多条接近路线，并支持我方宠物同步让位，避免搜索结果在执行时被覆盖或多人挤同一格。
- 战斗基础伤害改为直接使用面板攻击力；形状表的 `settle_count` 不再把单目标伤害统一放大 3 倍，叶泥泥攻击力 3 对无防御目标造成 3 点伤害。
- 提高战斗“自动布置”搜索精度：每只宠物候选站位由 16 提升到 32，同目标组合保留数由 3 提升到 6，全队 beam 宽度由 40 提升到 128；评分优先级和移动合法性保持不变。
- 修复战斗“开始行动”会直接跑完整场战斗的问题：按钮现在只执行当前玩家回合的全部出击，不自动跨过敌方回合、后续波次或进入结算；推进回合仍由“结束玩家回合”负责。
- 美术战斗页“开始行动”产生的正式 damage trace 改为按队列顺序播放：玩家元素弹道 `0.32s` 后再显示伤害数字、受击/HP/死亡表现，事件间保留 `0.16s` 动作间隔；弹体复用美术包 `0.45` 缩放，死亡单位已被最终快照移除时临时复用 `battle_unit` prefab 完成表现，不改核心伤害或 AI 规则。
- 普通战斗波次对齐 H5：敌方 Boss 在第 1-5 回合依次追加召唤 2/3/4/5/6 只宠物，未死亡的旧波敌人继续留场；新敌优先使用右上 3x3 空格，满后扩展到 4x4、5x5，第 6 回合起不再追加波次。
- 修复战斗“自动布置”后的“即将受伤”信息：布置完成后立即按新站位重跑真实摆位沙盘，棋盘格与宠物详情统一读取同一份伤害汇总。
- 战斗“自动布置”改为小规模全队组合搜索，优先扩大本轮可击杀的不同敌方宠物数量；击杀数相同时再比较可击杀 Boss、有效伤害、溢出和移动成本，避免多只宠物重复规划同一条击杀线。
- Godot 普通商店统一最多生成并公开 5 件商品，保留美术原始五槽布局。
- 同宠仅在 `pet_id` 与公开品质同时一致时合并；跨品质副本使用独立、确定性的运行时实例 id。
- 宠物 roster 保存最新 `acquired_from` / `acquired_at`，并以 `acquisition_history` 记录每次来源及局内时刻；字段随现有 roster 存档与读档链持久化。
- 付费刷新费用调整为 `2 / 4 / 6 / 8`，达到 8 后封顶。
- 普通商店候选池排除已经拥有钻石品质的同 `pet_id`。
- H5 仅作为有用实现参考，本轮未修改 H5 文件，也不要求跨运行时逐项一致。
# 2026-07-21

- 正式战斗废除五波累计召唤，改为双方英雄与最多四宠固定阵容对战；第 1/2/3 次重置分别要求存活宠物不超过 2/1/0 只，第 3 次后继续要求 0 只。开局双方均为 0 次，进入第 5/10/15... 回合时各增加 1 次，未使用次数可累计；我方通过行动面板按钮确认，敌方满足条件后自动执行。
- 宠物重置优先在英雄 3x3 安全区确定性随机生成；区域内存在元素陷阱或安全空位不足时依次扩大到 4x4、5x5，始终避开英雄、单位和元素陷阱格。重置恢复本场原始固定阵容满血并清除临时行动状态。
- 双方英雄成为真实可攻击目标；己方仍有宠物时获得“宠物护卫”，每个独立伤害包最多承受 4 点。任一英雄生命归零立即结算；否则完整执行第 12 回合后按英雄生命高低判胜负，同血平局。
- 按追加视觉要求隐藏战斗场景的 `BoardBG` 8x8 棋盘底图；保留 8x8 格子节点、单位、元素、按钮及交互表现。
